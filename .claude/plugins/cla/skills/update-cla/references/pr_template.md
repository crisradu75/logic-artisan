# pr_template — PR body template rendered by `apply --mode pr`

The Markdown body `update-cla`'s Phase 3 `apply --mode pr` fills in and opens the sync PR with. Placeholders are substituted from the run's adaptation results; the shape below is the contract.

## Summary

Pulled `.claude/` assets from the source repo, adapted to local conventions and
preserving any local-specific content. Generated by `update-cla`.

## Assets in this PR

| Asset | Adaptation summary |
|---|---|
{{ASSET_ROWS}}

## Per-asset notes

{{CHANGE_SUMMARIES}}

## Review checklist

- [ ] Adapted content matches this repo's `CLAUDE.md` conventions, paths, and tool references.
- [ ] Any local-specific content that existed pre-sync is still present (or its removal is intentional and called out above).
- [ ] Helper files referenced by the adapted assets (`references/`, `scripts/`, `prompts/`) still resolve in this repo.

Generated on {{TIMESTAMP}}.
