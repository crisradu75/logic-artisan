## 1. Re-derive the anchors, and check what the sibling change left

- [ ] 1.1 Re-run the anchor measurements and record the values inline, because every line number in `design.md` was measured before the sibling change landed: `wc -l .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md .claude/plugins/cla/skills/spec-to-pr/references/concurrent-runs.md .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the three counts>`
- [ ] 1.2 Locate the edit sites by content, not by line number: `grep -n '^### [0-9]\.' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md`, `grep -n 'Do NOT run .git checkout' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md`, `grep -n '^## Concurrent runs' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the hits>`
- [ ] 1.3 Read `subagent-brief.md` §5 as it stands **now** and record which terminal-contract blocks it contains. If `fix-brief-binding-defect` has landed, §5 holds two blocks (the default contract and the fix-brief contract) and this change's three lines apply to both; if it has not, §5 holds one and the classification line is written against the two-status set. State which case applies — measured: `<paste the §5 heading-to-end excerpt>`
- [ ] 1.4 Re-run the blast-radius checks: `grep -rn 'subagent-brief' .claude/plugins/cla/` (expect two citing sites, both by slot name) and `grep -rn 'concurrent-runs' .claude/plugins/cla/` (expect two, both by path, neither by heading text) — measured: `<paste the hits>`. If any site cites the section by its heading text, task 4.3's retitle needs that site updated too.

## 2. The brief's liveness and evidence rules (`subagent-brief.md`)

- [ ] 2.1 In §3 ("Do not touch"), extend the existing forbidden-verb block with the verbatim text from `design.md` "Pinned implementation parameters" — adding `reset`, `commit` and `push` to the enumeration, plus the process and build/cache-directory prohibitions. **Extend, do not replace:** the existing `git diff <base>...<branch>` read-only guidance and the paragraph explaining why "no edits" is not enough both stay.
- [ ] 2.2 In §3, state that this block is standard in every dispatch rather than added per brief, and give the reason: every dispatch runs in the orchestrator's own checkout, so there is no dispatch kind for which it is inapplicable.
- [ ] 2.3 In §5 ("Done when"), add the delegate liveness line verbatim from `design.md` "Pinned implementation parameters" — foreground gates, no turn ending with a command still running, the reason (a backgrounded command does not survive the return), and the `blocked`-naming-the-gate branch for a gate too long to run in one call.
- [ ] 2.4 In §5, add the no-invented-values line verbatim from `design.md`, positioned **adjacent to the evidence requirement** rather than elsewhere in the slot. Placement is a pinned decision, not a formatting choice.
- [ ] 2.5 In §5, add the classification rule verbatim from `design.md`, written as what the contract means for the reader of a return: a return carrying no status token and no evidence field is `blocked`, whatever its prose says. Write it so it applies to every contract block in the slot, not inside one of them.
- [ ] 2.6 State in §5 that no terminal status is added by any of the above — the set is `done` / `blocked` / (where the sibling change has landed) `remedy-rejected` — and that an evidence-free return is classified by the orchestrator rather than declared by the agent.
- [ ] 2.7 Confirm no slot was renamed, renumbered or added: `grep -n '^### [0-9]\.' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md` returns the same five headings task 1.2 recorded — measured: `<paste the hits>`.

## 3. The orchestrator's side of the return contract (`spec-to-pr/SKILL.md`)

- [ ] 3.1 At the Implement-phase delegation site — the "Evidence-based terminal contract" bullet and the "Contract firings leave a trace" paragraph beside it — add the classification rule and the on-a-miss ladder: classify `blocked` and record a contract firing → read-only quiet check → re-dispatch once → take over only after the quiet check is clean.
- [ ] 3.2 In the same place, state the prohibition explicitly: take-over **before** the quiet check is not permitted, and name why — it is the move that produced the recorded incident.
- [ ] 3.3 Confirm the re-dispatch cap matches what the phase already states for a `blocked` return rather than inventing a second number: `grep -n 're-dispatch once' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the hits>`. If the existing wording differs, reconcile to one cap, stated once.
- [ ] 3.4 Confirm no cap moved anywhere: `grep -n 'test-rounds\|pr-rounds\|review-rounds' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` shows the same defaults as before the edit — measured: `<paste the hits>`.

## 4. Single-writer discipline for one checkout (`concurrent-runs.md` + its stub)

- [ ] 4.1 In `references/concurrent-runs.md`, add the orchestrator-facing case as a second case beside the worktree-per-session recipe, using the verbatim prohibition from `design.md` "Pinned implementation parameters", including the second half — a failure observed in a checkout the orchestrator disturbed while its own dispatch was live is not evidence of a regression and is re-derived from a quiet tree first.
- [ ] 4.2 State in that file why the two cases share one home: both are the same invariant (two agents writing to one checkout), and one invariant split across two documents drifts. Keep the existing worktree recipe, its base-branch trap and its cleanup steps intact — diff old against new and state what, if anything, was dropped.
- [ ] 4.3 Retitle the file's H1 and the `## Concurrent runs (worktree-per-session)` section heading in `SKILL.md` so both name two cases rather than only the worktree one. **Do not change the file path** — both citing sites reference it by path.
- [ ] 4.4 Update the `SKILL.md` §Concurrent runs stub so it carries the one orchestrator-facing rule worth holding without reloading the reference (the checkout is not yours while a dispatch of yours is live), alongside the branch-off-`origin` rule it already carries.
- [ ] 4.5 Update the References-list line for `concurrent-runs.md` in `SKILL.md`, which currently describes the file as worktree-per-session setup only — measured before and after: `<paste both lines>`.

## 5. Verify

- [ ] 5.1 Confirm the added prose is portable synced core — no repo token, no dev-tree path, no absolute developer path, and specifically **no naming of the consuming repo's gate command** (the incident's own command must not appear in shipped prose): `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `<paste the exit status and summary line>`.
- [ ] 5.2 Run the area suite for the skill touched: `pytest plugin-tests/tests/skills` — measured: `<paste the summary line>`.
- [ ] 5.3 Run the conformance and consistency areas, which police synced-core portability and `SKILL.md` reference integrity — the retitle in task 4.3 is the change most likely to trip a reference check: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `<paste the summary line>`.
- [ ] 5.4 Check the non-contradiction with `fix-brief-binding-defect` against the edited files rather than against the design: read §5 end to end and confirm each of the five rows in `design.md`'s interaction table resolves to exactly one status, and that no row resolves to proceeding with a guess — measured: `<paste the §5 excerpt and the row-by-row verdict>`.
- [ ] 5.5 Dry-read the rules against the recorded incident: take the evidence-free return shape from #113 (a return announcing it is standing by for a completion notification) and confirm the classification rule as written puts it at `blocked` with no judgement call; then walk the on-a-miss ladder and confirm it does not reach take-over before the quiet check. If either walk needs a reading of the prose's tone to come out right, the rule is wrong and this task fails rather than the walk being adjusted.
- [ ] 5.6 Before the PR: `pytest plugin-tests` and `node --test plugin-tests/node/mechanical-checks.test.mjs`, each once — measured: `<paste both summary lines>`.
