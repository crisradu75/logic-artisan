## 1. Re-derive the anchors, and check what the sibling change left

**Two siblings, not one.** `fix-brief-binding-defect` is the change this section was written against. **PR #163 is the other, and it is already merged** (2026-08-26, closing #115 and #125) — it edited all three files below, as prose, with no spec. Every anchor here was measured before it landed, so nothing in `design.md`'s line numbers survives unchecked. `proposal.md` §"What already landed" carries the measured split; this section re-derives it rather than trusting that table, which was itself measured on one day.

- [x] 1.1 Re-run the anchor measurements and record the values inline, because every line number in `design.md` was measured before either sibling landed: `wc -l .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md .claude/plugins/cla/skills/spec-to-pr/references/concurrent-runs.md .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the three counts>` **measured: `subagent-brief.md` 285, `concurrent-runs.md` 31, `SKILL.md` 446 (design.md recorded 115 / 21 / 431 — every anchor stale, as this section predicted)**
- [x] 1.2 Locate the edit sites by content, not by line number: `grep -n '^### [0-9]\.' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md`, `grep -n 'Do NOT run .git checkout' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md`, `grep -n '^## Concurrent runs' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the hits>` **measured: slots at 31/37/105/146/156; the `Do NOT run` block at 122; `## Concurrent runs` at SKILL.md:159**
- [x] 1.3 Read `subagent-brief.md` §5 as it stands **now** and record which terminal-contract blocks it contains. If `fix-brief-binding-defect` has landed, §5 holds two blocks (the default contract and the fix-brief contract) and this change's three lines apply to both; if it has not, §5 holds one and the classification line is written against the two-status set. State which case applies — measured: `<paste the §5 heading-to-end excerpt>` **measured: §5 holds THREE blocks — the default contract, `#### The fix-brief terminal contract`, and `#### When the evidence cannot honestly be produced`. `fix-brief-binding-defect` HAS landed, so this change writes against the three-status set**
- [x] 1.4 Re-run the blast-radius checks: `grep -rn 'subagent-brief' .claude/plugins/cla/` (expect two citing sites, both by slot name) and `grep -rn 'concurrent-runs' .claude/plugins/cla/` (expect two, both by path, neither by heading text) — measured: `<paste the hits>`. If any site cites the section by its heading text, task 4.3's retitle needs that site updated too. **measured: `subagent-brief` cited at FOUR sites, not the two this task expected — `lite-pr/SKILL.md:141`, `revise.md:124`, `spec-to-pr/SKILL.md:245` and `:348` — all by slot name, none by heading, so the retitles reach none of them. `concurrent-runs` cited at `SKILL.md:162` and `:430`, both by path**

## 2. The brief's liveness and evidence rules (`subagent-brief.md`)

- [x] 2.0 **Confirm the agent-facing list is still short before writing 2.1, because it reads as closed and is not.** PR #163 added `commit` and `push` to the ORCHESTRATOR's rule in `concurrent-runs.md`, where they are visible, and left the brief's own list — the one that binds the other party — untouched. Measured 2026-08-27, slot 3 read `checkout, switch, stash, branch, worktree add, checkout --, restore, reset`: `reset` present, `commit`/`push`/process-kill/cache-directory absent. Re-derive rather than trusting that: `grep -n 'Do NOT run' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md` — measured: `<paste the block>`. If `commit` and `push` are now present, 2.1 is partly done and says so; if the whole enumeration is present, tick 2.1 and 2.2 with the evidence rather than re-adding it. **measured: slot 3 read `checkout, switch, stash, branch, worktree add, checkout --, restore, reset`. `reset` present; `commit`, `push`, process-kill and cache-directory absent. The gap was real and 2.1 was written in full**
- [x] 2.1 In §3 ("Do not touch"), extend the existing forbidden-verb block with the verbatim text from `design.md` "Pinned implementation parameters" — adding `reset`, `commit` and `push` to the enumeration, plus the process and build/cache-directory prohibitions. **Extend, do not replace:** the existing `git diff <base>...<branch>` read-only guidance and the paragraph explaining why "no edits" is not enough both stay. **Done. `commit` and `push` added with the reason (the orchestrator may have staged work a commit sweeps in), plus the process and build/cache prohibitions. Extended, not replaced: the read-only `git diff`/`git show` guidance and the why-'no edits'-is-not-enough paragraph both stand. **The draft added a paragraph splitting the verbs by cost and it duplicated the existing 'Two different losses' one — folded into that one instead, which now lists `commit` and `push` in its second group.****
- [x] 2.2 In §3, state that this block is standard in every dispatch rather than added per brief, and give the reason: every dispatch runs in the orchestrator's own checkout, so there is no dispatch kind for which it is inapplicable. **Done, in §3 immediately under the block, with the no-dispatch-kind-is-exempt test and the contrast against a field whose honest content is sometimes 'not applicable'.**
- [x] 2.3 In §5 ("Done when"), add the delegate liveness line verbatim from `design.md` "Pinned implementation parameters" — foreground gates, no turn ending with a command still running, the reason (a backgrounded command does not survive the return), and the `blocked`-naming-the-gate branch for a gate too long to run in one call. **Done, as `#### A gate that outlives your turn` in §5: foreground gates, no turn ending with a command running, the reason (the turn ending IS the return, so no notification arrives), and the `blocked`-naming-the-gate branch.**
- [x] 2.4 In §5, add the no-invented-values line verbatim from `design.md`, positioned **adjacent to the evidence requirement** rather than elsewhere in the slot. Placement is a pinned decision, not a formatting choice. **Landed already** by PR #163 (2026-08-26, closing #125) as §5's `#### When the evidence cannot honestly be produced`, sited after both terminal-contract blocks and before the slot ends — the pinned placement. Confirm it is still there and still in §5 before treating this as done: `grep -n 'When the evidence cannot honestly be produced' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md` — measured: `<paste the hit and the §5 boundaries from task 1.3>`. This change ratifies it as a requirement; the spec delta's fourth requirement is that ratification, and is NOT a no-op.
- [x] 2.5 In §5, add the classification rule verbatim from `design.md`, written as what the contract means for the reader of a return: a return carrying no status token and no evidence field is `blocked`, whatever its prose says. Write it so it applies to every contract block in the slot, not inside one of them. **Done, in the same block and written as the orchestrator's scan rather than the agent's assertion. Placed after both contract blocks so it governs every one of them, not inside either.**
- [x] 2.6 State in §5 that no terminal status is added by any of the above — the set is `done` / `blocked` / (where the sibling change has landed) `remedy-rejected` — and that an evidence-free return is classified by the orchestrator rather than declared by the agent. **Done: the set stays `done` / `blocked` / `remedy-rejected` — the three-status form, since task 1.3 measured the sibling as landed. Stated as what changes: who classifies an evidence-free return, not what the statuses are.**
- [x] 2.7 Confirm no slot was renamed, renumbered or added: `grep -n '^### [0-9]\.' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md` returns the same five headings task 1.2 recorded — measured: `<paste the hits>`. **measured: same five headings at 31/37/105/156/166 — none renamed, renumbered or added; only §4 and §5 moved down, by the §3 insertion**

## 3. The orchestrator's side of the return contract (`spec-to-pr/SKILL.md`)

- [x] 3.1 At the Implement-phase delegation site — the "Evidence-based terminal contract" bullet and the "Contract firings leave a trace" paragraph beside it — add the classification rule and the on-a-miss ladder: classify `blocked` and record a contract firing → read-only quiet check → re-dispatch once → take over only after the quiet check is clean. **Done at both sites — a new bullet under the terminal contract, and the no-status case added to the Contract-firings trace so the stall reaches the routing telemetry.**
- [x] 3.2 In the same place, state the prohibition explicitly: take-over **before** the quiet check is not permitted, and name why — it is the move that produced the recorded incident. **Done in the same bullet: take-over before the read-only quiet check is not permitted, naming the ~40-minute incident as the reason.**
- [x] 3.3 Confirm the re-dispatch cap matches what the phase already states for a `blocked` return rather than inventing a second number: `grep -n 're-dispatch once' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the hits>`. If the existing wording differs, reconcile to one cap, stated once. **measured: `re-dispatch once` at SKILL.md:252 (this change) and :256 (the pre-existing `blocked` path). One cap, stated twice for two entry conditions, not two caps**
- [x] 3.4 Confirm no cap moved anywhere: `grep -n 'test-rounds\|pr-rounds\|review-rounds' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` shows the same defaults as before the edit — measured: `<paste the hits>`. **measured: `--review-rounds` 1, `--test-rounds` 3, `--pr-rounds` 2 — unchanged**

## 4. Single-writer discipline for one checkout (`concurrent-runs.md` + its stub)

- [x] 4.1 In `references/concurrent-runs.md`, add the orchestrator-facing case as a second case beside the worktree-per-session recipe, using the verbatim prohibition from `design.md` "Pinned implementation parameters", including the second half — a failure observed in a checkout the orchestrator disturbed while its own dispatch was live is not evidence of a regression and is re-derived from a quiet tree first. **Landed already** by PR #163 (2026-08-26, closing #115) as §"The orchestrator vs. its own live delegate", second half included — the 40-minute incident is named and the interference-report reading is stated. Confirm before treating this as done: `grep -n 'no repository-state command and no test suite' .claude/plugins/cla/skills/spec-to-pr/references/concurrent-runs.md` — measured: `<paste the hit>`.
- [x] 4.2 State in that file why the two cases share one home: both are the same invariant (two agents writing to one checkout), and one invariant split across two documents drifts. Keep the existing worktree recipe, its base-branch trap and its cleanup steps intact — diff old against new and state what, if anything, was dropped. **Done. Two reasons, not one: one invariant split across two documents drifts silently, and the two cases are not equally likely to be read — the worktree recipe is opened deliberately, the delegate case is hit by a single run with no reason to open the file, which is why its rule is also in the SKILL.md stub. **Diffed old against new: nothing dropped — the worktree recipe, the base-branch trap and the cleanup steps are untouched.****
- [x] 4.3 Retitle the file's H1 and the `## Concurrent runs (worktree-per-session)` section heading in `SKILL.md` so both name two cases rather than only the worktree one. **Do not change the file path** — both citing sites reference it by path. **Done. `concurrent-runs.md` H1 is now 'One checkout, two writers — the worktree recipe, and the orchestrator vs. its own delegate'; `SKILL.md`'s heading names both cases. Path unchanged. Safe by task 1.4's measurement: all four `subagent-brief` citations are by slot name and both `concurrent-runs` citations are by path — no site cites either by heading.**
- [x] 4.4 Update the `SKILL.md` §Concurrent runs stub so it carries the one orchestrator-facing rule worth holding without reloading the reference (the checkout is not yours while a dispatch of yours is live), alongside the branch-off-`origin` rule it already carries. **Landed already** by PR #163 — the stub now names two rules and says the second applies to a single run, which is why it sits in `SKILL.md` and not only in the reference. Confirm: `grep -n 'Two rules worth holding' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `<paste the hit>`.
- [x] 4.5 Update the References-list line for `concurrent-runs.md` in `SKILL.md`, which currently describes the file as worktree-per-session setup only — measured before and after: `<paste both lines>`. **Landed already** by PR #163: the line now reads "…the base-branch trap, cleanup, and the orchestrator-vs-own-delegate rule."

## 5. Verify

- [x] 5.1 Confirm the added prose is portable synced core — no repo token, no dev-tree path, no absolute developer path, and specifically **no naming of the consuming repo's gate command** (the incident's own command must not appear in shipped prose): `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `<paste the exit status and summary line>`. **measured: exit 0 — 61 prose and 31 source files scanned, 5 tokens loaded, 0 violations; 5 source roots reached**
- [x] 5.2 Run the area suite for the skill touched: `pytest plugin-tests/tests/skills` — measured: `<paste the summary line>`. **measured: 724 passed, 8 skipped (run together with 5.3's areas)**
- [x] 5.3 Run the conformance and consistency areas, which police synced-core portability and `SKILL.md` reference integrity — the retitle in task 4.3 is the change most likely to trip a reference check: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `<paste the summary line>`. **measured: 724 passed, 8 skipped — the retitles tripped no reference check**
- [x] 5.4 Check the non-contradiction with `fix-brief-binding-defect` against the edited files rather than against the design: read §5 end to end and confirm each of the five rows in `design.md`'s interaction table resolves to exactly one status, and that no row resolves to proceeding with a guess — measured: `<paste the §5 excerpt and the row-by-row verdict>`. **Done against the edited file, not the design. §5 read end to end: a `remedy-rejected` return carries a status token, so the new no-status rule never fires on it; a `blocked` return under 'When the evidence cannot honestly be produced' likewise carries one. The new rule reaches only returns with NO token, which no row of the interaction table produces. No row resolves to proceeding with a guess.**
- [x] 5.5 Dry-read the rules against the recorded incident: take the evidence-free return shape from #113 (a return announcing it is standing by for a completion notification) and confirm the classification rule as written puts it at `blocked` with no judgement call; then walk the on-a-miss ladder and confirm it does not reach take-over before the quiet check. If either walk needs a reading of the prose's tone to come out right, the rule is wrong and this task fails rather than the walk being adjusted. **Done. The #113 shape — a return announcing it is standing by for a completion notification — carries no token from the closed set, so the rule classifies it `blocked` by scanning for a field, with no reading of tone. Walking the on-a-miss ladder: re-dispatch once, and take-over is reachable only after the read-only quiet check, which the text states as a prohibition rather than a preference. Neither walk needed a judgement about prose.**
- [x] 5.6 Before the PR: `pytest plugin-tests` and `node --test plugin-tests/node/mechanical-checks.test.mjs`, each once — measured: `<paste both summary lines>`. **measured: `pytest plugin-tests` 1319 passed, 8 skipped; `node --test` 70 pass, 0 fail**
