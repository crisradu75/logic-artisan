## 1. Promote `check_fact_paths.py` (the simpler guard first)

- [ ] 1.1 Before writing anything, enumerate the originals by grepping — not from memory: `grep -nE '^def ' .claude/plugins/cla/conformance-checks/tests/test_project_facts_paths.py`. Record the full list of `def test_*` functions; it is the checklist task 1.6 audits against.
- [ ] 1.2 Create `.claude/plugins/cla/skills/sync-context/scripts/` (the skill is `SKILL.md`-only today, so the directory does not exist).
- [ ] 1.3 Write `.claude/plugins/cla/skills/sync-context/scripts/check_fact_paths.py`, carrying every non-test module-level constant and function across **byte-identical, with the same names and signatures** — `_repo_root_from_here`, `_strip_wrapping`, `_strip_line_col`, `_strip_emphasis`, `_clean_candidate`, `_looks_like_placeholder_or_glob`, `_clean_git_env`, `_tracked_top_level_names`, `_top_level_names`, `_keep_if_path`, `extract_path_candidates`, `_iter_scanned_files`, `scan`, `find_stale_paths`, and every regex/constant they read. These are accumulated edge-case heuristics; do not "improve" them while moving them.
- [ ] 1.4 Add `main()` carrying the assertion from `test_no_stale_paths_in_project_facts_or_overlays`, plus `if __name__ == "__main__": sys.exit(main())`. Implement the pinned exit contract from design.md: **0** clean (printing what was scanned and the file count), **1** stale paths found (every one named, one per line, never stopping at the first), **2** could-not-run (bad args, or a present-but-unreadable input) to stderr. An absent `cla.io/project-facts.md` exits **0** with a stated reason.
- [ ] 1.5 Add the `--repo-root <path>` optional argument (default: existing self-resolution) so the CLI layer is testable against `tmp_path`. Confirm `_repo_root_from_here` is carried across unchanged — it is already fully position-independent (git, then a `.claude` walk that refuses the global `~/.claude`, then cwd) and needs no depth fix.
- [ ] 1.6 **Rewrite audit (required — this is the "rewrote a file rather than edited it" check).** Diff old against new and produce a table classifying **every** `def` from task 1.1 as: carried into `main()` / kept as a unit test / deliberately dropped with a reason. State the result explicitly, including "nothing was dropped" if that is the finding. An empty dropped-column is a claim, so name the command that produced the enumeration.
- [ ] 1.7 Re-point `conformance-checks/tests/test_project_facts_paths.py` at the new module via `importlib.util.spec_from_file_location` (per design.md — not a `pythonpath` entry in `conformance-checks/pyproject.toml`). The file keeps only its `test_*` functions; its implementation now lives in the promoted script. Keep `test_the_repo_root_is_the_process_repo_not_the_users_home` as a test.
- [ ] 1.8 Run `pytest .claude/plugins/cla/conformance-checks/tests -q`. Then run the promoted script directly against this repo and confirm it exits 0 **and prints a non-zero scanned-file count** — a zero-file scan reporting "0 stale paths" is the vacuous-pass bug, not a pass.

## 2. Promote `check_no_project_tokens.py` (four repo-level checks, not one)

- [ ] 2.1 Enumerate the original the same way: `grep -nE '^def ' .claude/plugins/cla/conformance-checks/tests/test_no_project_tokens.py`. Confirm against design.md D2 that exactly four functions assert against the **real repo**: `test_no_project_tokens_in_synced_core`, `test_no_project_tokens_in_synced_source`, `test_no_absolute_developer_paths_in_synced_source`, `test_every_scanned_file_is_actually_readable`.
- [ ] 2.2 Write `.claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py`, carrying every non-test constant and function across byte-identical — including both scanners and their distinct constant sets (`SOURCE_SCAN_ROOTS`, `CACHE_DIRS`, `EXCLUDED_SUBTREES`, `ABS_PATH_EXEMPT_MARKER`, `PLACEHOLDER_USERS`, `MANGLED_WIN_PATH`, `WIN_ABS_PATH`, `HOME_ABS_PATH`, `PLACEHOLDER_PATH_HINTS`, `MAX_EXCERPT`, `TOKEN_LIST_RELPATH`) and every function they read.
- [ ] 2.3 **Fix `_plugin_root()`'s positional fallback (design.md D3).** From the new home `skills/_shared/scripts/`, `parents[2]` is `skills/`, not the plugin root — the correct depth is `parents[3]`. Update the depth **and** the stale `# Fallback ... tests/ -> conformance-checks/ -> cla/` comment together; the comment is what makes the depth checkable. The primary walk still succeeds normally, so this defect is silent and surfaces only in the unexpected-layout case the fallback exists for.
- [ ] 2.4 Add `main()` running **all four** checks from task 2.1, accumulating violations across them and **not** short-circuiting on the first that fails. Same pinned exit contract: 0 clean (with a scanned-file count), 1 violations (each named with repo-relative path, matched token, line number and excerpt), 2 could-not-run. Absent token list → exit 0 with a stated reason; **present-but-empty token list → exit 1** with the message that deleting the file is how to intentionally disable it.
- [ ] 2.5 Do not drop check (d), `test_every_scanned_file_is_actually_readable`. It reads like plumbing and is the one check keeping the other three from passing vacuously — an unreadable file is silently "clean" to every scanner. Add `--repo-root` as in 1.5.
- [ ] 2.6 **Rewrite audit**, same form as 1.6: table of every `def`, classified carried / kept-as-test / dropped-with-reason, with the enumerating command named. Pay particular attention to the four repo-level checks — this is the specific defect this task exists to catch.
- [ ] 2.7 Re-point `conformance-checks/tests/test_no_project_tokens.py` at the new module via `importlib.util.spec_from_file_location`, keeping every unit test — including the non-vacuity ones (`test_the_scanner_is_not_vacuous`, `test_source_scan_of_the_real_plugin_is_non_vacuous`, `test_the_readability_check_can_actually_fail`).
- [ ] 2.8 Run `pytest .claude/plugins/cla/conformance-checks/tests -q`. Then run the promoted script directly against this repo: confirm exit 0 with a non-zero scanned-file count, and confirm it reports on files from **more than one** scan root (a scan reaching only `skills/` would pass while covering a fraction of what it claims).

## 3. Rename the two colliding `aggregate.py` pairs

- [ ] 3.1 Rename to the names pinned in design.md: `codify-retro/scripts/aggregate.py` → `codify_aggregate.py`; `spec-to-pr-retro/scripts/aggregate.py` → `spec_to_pr_aggregate.py`; `codify-retro/tests/test_aggregate.py` → `test_codify_aggregate.py`; `spec-to-pr-retro/tests/test_aggregate.py` → `test_spec_to_pr_aggregate.py`. Use `git mv` so the rename is visible as a rename.
- [ ] 3.2 Update the `SCRIPT = Path(__file__).resolve().parents[1] / "scripts" / "aggregate.py"` line in each renamed test file, and the two module docstrings that open `"""Tests for aggregate.py …"""`.
- [ ] 3.3 Update the collision comments in both test files (`codify-retro/tests` line ~260, `spec-to-pr-retro/tests` line ~534) — each says the other skill's "identically-named `aggregate.py` can't collide", which is no longer the situation. Reword to reflect that the names are now distinct.
- [ ] 3.4 Update the two skill invocation lines: `skills/codify-retro/SKILL.md:29` and `skills/spec-to-pr-retro/SKILL.md:27`.
- [ ] 3.5 Update `consistency-checks/scripts/check_script_drift.py` — **two** path lists (lines ~52–53 and ~60–61), the check `name` string `"retro aggregate.py record loading"`, and the module docstring's "retro skills' `aggregate.py`" mention.
- [ ] 3.6 Update `consistency-checks/tests/test_ledger_names_agree.py` — two `_PLUGIN_ROOT / "skills" / … / "scripts" / "aggregate.py"` constructions plus its docstring — and `consistency-checks/tests/test_check_script_drift.py`'s expected-paths fixture.
- [ ] 3.7 Update `run_tests.py`'s module docstring, which justifies running each scope as its own process by naming the collision ("multiple scopes ship a top-level `scripts/aggregate.py`"). Note the justification is now weaker; do not restructure the runner — change (b) deletes it.
- [ ] 3.8 Update `lib/log_run.py:86`'s "the consumers (the retro skills' `aggregate.py`)" comment and the identical line inside each renamed aggregator (~line 113 / ~134).
- [ ] 3.9 Sweep for survivors: `grep -rn "aggregate\.py\|test_aggregate\|import aggregate" .claude/plugins/cla` must return no hit naming the bare old name as a path. Check `_shared/references/retro-skeleton.md` and `run-log-schema.md` separately — their "the calling skill's `aggregate.py`" mentions are **role**-shaped, not path-shaped; confirm that reading rather than assuming it, and change only what is path-shaped.
- [ ] 3.10 **Run `check_script_drift.py` and confirm it reports on the renamed files**, not merely that it exits 0 (design.md D5 + Open Questions). Its subject is a divergence that is silent by construction — `CLAUDE.md` records "the retro reports zero runs, which reads as a cold start" — so a drift checker whose path list points at nothing would pass vacuously. If it does pass vacuously on a missing path, record that as a finding for change (c).
- [ ] 3.11 Run the three affected scopes: `pytest .claude/plugins/cla/skills/codify-retro/tests`, `.../skills/spec-to-pr-retro/tests`, `.../consistency-checks/tests`.

## 4. Rewrite the mutation gate as prose

- [ ] 4.1 Copy the current step 2b blocks verbatim to scratch before editing: `skills/lite-pr/SKILL.md:129` and `skills/spec-to-pr/references/revise.md:94`. They are the baseline for the 4.4 diff.
- [ ] 4.2 Rewrite `lite-pr/SKILL.md` step 2b: remove `python3 ${CLAUDE_PLUGIN_ROOT}/mutate.py <batch.py>` and only the words that exist to operate it, replacing them with the manual procedure — break what the fix touches, run the affected test, confirm it fails, restore. Keep the heading "**Mutation gate (required before the commit in step 3).**"
- [ ] 4.3 Rewrite `spec-to-pr/references/revise.md` step 2b identically — but keep its **Handoff report** wording where `lite-pr` says **final report**. The two blocks differ there deliberately; do not normalise them.
- [ ] 4.4 **Verbatim-preservation diff (required).** Compare old 2b against new 2b line by line in both files and confirm every sentence except the invocation survives: the step-3 ordering; "a change like any other and earns the same evidence the original code needed"; "'the reviewer's finding is now handled' is not that evidence"; "what the fix touches, not only what it targets"; the default-path/overlay-path no-op precedent; "fix a surviving mutant, or name it in the … report with a reason"; and "a clean run is evidence about the mutants you thought of and nothing else — two commits in this repo each recorded 'three mutations checked, all caught' and each shipped a critical a later review found." State which sentences changed and why.
- [ ] 4.5 Confirm no `${CLAUDE_PLUGIN_ROOT}/mutate.py` reference remains in any shipped skill: `grep -rn "mutate\.py" .claude/plugins/cla/skills/` returns nothing. (`README.md`'s layout line and `mutate.py` itself stay — change (b) removes them.)

## 5. Doc lines this change falsifies

- [ ] 5.1 `skills/sync-context/SKILL.md:151` — re-point the staleness-guard reference from `${CLAUDE_PLUGIN_ROOT}/conformance-checks/tests/test_project_facts_paths.py` to `${CLAUDE_PLUGIN_ROOT}/skills/sync-context/scripts/check_fact_paths.py`. More than a path swap: the checker is now this skill's own script, so say so.
- [ ] 5.2 `skills/_shared/references/skill-authoring.md:39` — replace `python3 -m pytest ${CLAUDE_PLUGIN_ROOT}/conformance-checks/tests/test_no_project_tokens.py -q` MUST pass with `python3 ${CLAUDE_PLUGIN_ROOT}/skills/_shared/scripts/check_no_project_tokens.py` MUST **exit 0**. This is a shipped file instructing a consumer to run a pytest scope that is about to vanish — the coupling this change exists to cut.
- [ ] 5.3 `CLAUDE.md:259` — update the "Every script, and why it exists" row that names `` `codify-retro`, `spec-to-pr-retro` `scripts/aggregate.py` `` to the two new distinct names. **Scoped deliberately to lines this change falsifies: the layout/scope-count reconciliation is deferred to `guard-shipped-asset-boundary` (change c).** Specifically leave alone `CLAUDE.md:93` ("portable core that reaches consuming repos"), `CLAUDE.md:96` ("several scopes ship same-named helper modules" — weakened by task 3 but describing a 12-scope tree that still exists until change b), the layout tree at line 234, `DEVELOPER-GUIDE.md`, the plugin `README.md`, root `.gitattributes`, and `TODO.md`.
- [ ] 5.4 Consider whether the two promoted checkers now warrant rows in `CLAUDE.md`'s script table (they are scripts, and the table's rule is that each earns its place in one line). If added, keep to one line each; if deferred, say so and leave it to change (c).

## 6. Verify and hand off

- [ ] 6.1 Run `python3 .claude/plugins/cla/run_tests.py` once — the shipping gate. Every scope plus the Node suite.
- [ ] 6.2 Confirm the scope inventory is unchanged: this change moves nothing, so `run_tests.py` must still discover the same 12 pytest scopes plus the Node suite, with no near-miss failures from the new `skills/sync-context/scripts/` directory (a dir with scripts but no `tests/` and no pytest-configured `pyproject.toml` is not a scope; confirm the discovery logic agrees).
- [ ] 6.3 Re-read the two rewrite audits (1.6, 2.6) and the verbatim diff (4.4) as a block before opening the PR. These three are the change's real gate; the test suite cannot catch a dropped assertion that was rewritten away along with its test.
- [ ] 6.4 Confirm the scope boundary held: `git status` shows **no file moved out of `.claude/plugins/cla/`**, `run_tests.py` and `mutate.py` both still exist, the `release` skill has not moved, and the three plugin-data conformance guards (`test_no_hardcoded_plugin_paths.py`, `test_skill_lint.py`, `test_guards_are_not_vacuous.py`) are still in `conformance-checks/tests/`.
- [ ] 6.5 Note in the PR body that `extract-dev-tree-from-plugin` depends on this change landing first, and carry forward any finding from 3.10 (drift-check vacuity) for `guard-shipped-asset-boundary`.
