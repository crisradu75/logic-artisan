## 1. Re-derive the anchors, and settle the same-file ordering

- [ ] 1.1 Re-measure the target file, because every line number in `design.md` is stale the moment the sibling change edits this file: `wc -l .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the count>`. `design.md` recorded 316 on 2026-08-25.
- [ ] 1.2 Locate all six edit sites **by content, not by line number**: `grep -n '^### Grounding contract\|^### Context brief\|^0k\. \*\*Deferred-guess\|^### Applies when the change touches\|generic 0a–0e and 0j–0k\|^\*\*Cost offload for large changes\|^## Step 6\|^Deduplicate findings' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hits>`.
- [ ] 1.3 Settle the ordering against the sibling change `fix-brief-binding-defect`, which edits this same file. Check whether its tasks 4.1–4.3 have landed: `grep -n 'agent-reported\|orchestrator-verified' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hits, or "none">`. If none, **do that change's cost-offload edits first**; this change's task 3.3 appends after them and must not rewrite the adjudication sentence.
- [ ] 1.4 Read §"Grounding contract" (heading through the paragraph that ends before `### Context brief`) as it stands right now, not as `design.md` describes it — the sibling's task 4.5 may have added a sentence about provenance tags travelling into findings. Record what is there — measured: `<paste the paragraph's first and last sentence>`.

## 2. The claim-shape list inside the Grounding contract

- [ ] 2.1 Insert a `####` subsection — heading verbatim from `design.md` "Pinned implementation parameters": `#### Claim shapes — four sentences that are claims and do not look like claims` — immediately after §"Grounding contract"'s paragraph and before `### Context brief — standard format`. `####` not `###`, so the list sits inside the contract; that nesting is the change's central decision, not a formatting preference.
- [ ] 2.2 Open the subsection with the one-sentence reason: these are recognition failures, not procedure failures — in each reported instance the reviewer held the grounding rule and the license to read source, and did not engage either, because the sentence presented as an explanation, a comparison, an output specification, or a trade.
- [ ] 2.3 Write **Shape 1 — Producible state**, carrying `design.md` D2 verbatim: trigger, resolution steps (a)/(b)/(c), the failure-mode asymmetry (an unnameable producing path resolves **not producible**, not `unresolved`), and the Critical floor with its reason — such a requirement does not fail loudly, it gets satisfied dishonestly.
- [ ] 2.4 Write **Shape 2 — Precedent strictness** from D2, including the sentence saying why no other check finds it: every other check asks whether the artifact is strong *enough*; this one asks whether it is stronger than it needs to be.
- [ ] 2.5 Write **Shape 3 — Guarantee class** from D2, including that leaving the guarantee unclassified *is* the failure, and the two-level severity floor (deployment property with no named trigger → Important; deployment property described as a code property → Critical).
- [ ] 2.6 Write **Shape 4 — Compensating coverage and exclusion reach** from D2, both triggers under one shape, including the asymmetry sentence: the given-up half is visible in the diff, the replacement is a promise.
- [ ] 2.7 Close with the openness paragraph: the common signature (truth depends on something outside the artifact even where the grammar is not assertive) plus the four hiding grammars, ending with the statement that a matching sentence is in scope whether or not it is listed. Write it as a signature, not as a trailing disclaimer.
- [ ] 2.8 Confirm every shape meets the executability bar — trigger, resolution steps, failure mode, severity floor, all four present: `grep -c 'Trigger:' .claude/plugins/cla/skills/review-change/references/checklist.md` and the same for `Resolution:`, `Failure mode:`, `Severity floor:` — measured: `<paste the four counts>`. Each must be 4. A count below 4 means a shape is stated as "check that the claim is grounded" and this task fails rather than the shape being waived.

## 3. The one numbered check, and its non-delegability

- [ ] 3.1 Insert `0l. **Claim-shape sweep**` immediately after `0k` and before `### Applies when the change touches allocation math, mock data, or i18n`. Its content points at the contract's shape list; it does **not** restate the shapes. Model its brevity on `0f–0i`, the existing one-line entry that delegates four checks elsewhere.
- [ ] 3.2 In `0l`, state that unlike `0a–0h` it is not delegable to `fact-gatherer`, because each shape returns a judgement — a comparison of two mechanisms, a classification, an assessment of one test's strength against another's — rather than a pass/fail row.
- [ ] 3.3 **Append** one sentence to the cost-offload paragraph naming `0l` as outside the delegable set. Append only — do not rewrite the sentence beginning "You still adjudicate every ✗ row yourself", which belongs to `fix-brief-binding-defect` task 4.2. After the edit, confirm both are present: `grep -c 'adjudicate every' .claude/plugins/cla/skills/review-change/references/checklist.md` and `grep -n '0l' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste both>`.
- [ ] 3.4 Update the check enumeration so `0l` is named where the orchestrator's checks are listed: `generic 0a–0e and 0j–0k here` becomes `generic 0a–0e and 0j–0l here`. Confirm the old string is gone: `grep -c '0j–0k' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the count>`, expected 0.
- [ ] 3.5 Confirm no existing check was renumbered or reworded: `grep -n '^0[a-z]\. \|^0f–0i\.' .claude/plugins/cla/skills/review-change/references/checklist.md` returns the same entries task 1.2 recorded, plus exactly one new `0l` — measured: `<paste the hits>`.

## 4. The severity tie-break at Step 6

- [ ] 4.1 After Step 6's existing "Deduplicate findings" sentence, add one paragraph: when two dispatched reports carry the same finding at different severities, the merged severity comes from the report whose evidence for that severity is implementation-level (a source line, a schema, a migration, a query result) over the report whose evidence is the spec delta or artifact text alone.
- [ ] 4.2 In the same paragraph, state **why it keys on evidence rather than on agent role**: all three Step-4 agents are told "You MAY read source files to verify claims", so the role does not identify who read the implementation; the evidence does, and it is present only because the Grounding contract requires every finding to carry it.
- [ ] 4.3 State the fallback — where neither report's evidence is implementation-level, or both are, the higher severity stands — and mark it explicitly as the fallback, not the rule. Add the one-clause reason: applied unconditionally it turns every disagreement into an escalation, which is the severity-count inflation the verdict rubric already warns against at "Do not shortcut this to 'any Critical → RETHINK'".
- [ ] 4.4 Require the tie-break to be recorded on the finding it resolved, and state that it adds no report line — §"Report constraints" budgets one line per finding and about one screen total.
- [ ] 4.5 Write the evidence sentence honestly and in the file, not only in `design.md`: one overlapping finding out of 18, one change, one chain, one consuming repo; the observed instance was a two-reviewer split under a different workflow, not this checklist's three-agent dispatch; low overlap is the split working as designed, so the rule is expected to fire rarely. Do not write "measured" anywhere in it.
- [ ] 4.6 Confirm the report budget did not move: `grep -n 'One line per finding\|~40 lines max' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hits>`, unchanged from task 1.2's reading.

## 5. Verify

- [ ] 5.1 Confirm the added prose is portable synced core — no consuming-repo mechanism, product name, or infrastructure identifier from the five source issues, and no dev-tree or absolute developer path: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `<paste the exit status and summary line>`.
- [ ] 5.2 Read the four shapes back and confirm by hand that none of them names the reporting incident's own mechanism rather than the shape — the four to be absent by name are the concurrency-control flag, the accessibility scanner, the registry code column, and the route-exclusion constant. `check_no_project_tokens.py` will not catch these unless they are on this repo's token list, so this is a read, not a grep.
- [ ] 5.3 Run the area suite for the skill touched: `pytest plugin-tests/tests/skills` — measured: `<paste the summary line>`.
- [ ] 5.4 Run the conformance and consistency areas, which police synced-core portability and SKILL.md reference integrity: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `<paste the summary line>`.
- [ ] 5.5 Dry-run the shape list against a real change: take the most recent OpenSpec change in this repo, walk `0l` over its `proposal.md` and `design.md`, and record which shapes fired and what each resolved to. If all four fire zero times on a change with a `design.md` full of explanations and precedent citations, the triggers are written too narrowly and this task fails rather than being recorded as a clean pass — measured: `<paste the per-shape hit count and one resolution>`.
- [ ] 5.6 Dry-run the tie-break: take two dispatched review reports from this repo's run notes, find any finding both carry, and apply the rule. If no such finding exists, record that and state which fallback branch would have applied — measured: `<paste the finding or the "none found" record>`.
- [ ] 5.7 Confirm the sibling change's edits to this file survived: `grep -c 'agent-reported' .claude/plugins/cla/skills/review-change/references/checklist.md` and `grep -n 'adjudicate every' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste both>`. A dropped sibling edit is this change's defect, since both land in one PR and this change edits later.
- [ ] 5.8 Before the PR: `pytest plugin-tests` and `node --test plugin-tests/node/mechanical-checks.test.mjs`, each once — measured: `<paste both summary lines>`.
