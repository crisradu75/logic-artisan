## 1. Preconditions and the baseline the whole change is graded against

- [ ] 1.1 Confirm change (a) `decouple-skills-from-dev-assets` has landed on this branch's base. Its tasks 3.7 and 6.1 **edit and run `run_tests.py`**, which this change deletes — running them afterwards is impossible, so the ordering is a hard dependency, not a preference. Verify concretely: `skills/sync-context/scripts/check_fact_paths.py` and `skills/_shared/scripts/check_no_project_tokens.py` exist, `skills/codify-retro/scripts/codify_aggregate.py` and `skills/spec-to-pr-retro/scripts/spec_to_pr_aggregate.py` exist, and `grep -rn "mutate\.py" .claude/plugins/cla/skills/` is empty.
- [ ] 1.2 Confirm a clean tree and that `python3 .claude/plugins/cla/run_tests.py` is green **once**, before touching anything. This is the last time that command is ever run in this repo; record its summary line.
- [ ] 1.3 **Capture `collected_before`.** For each of the 12 scopes run `python -m pytest --collect-only -q .claude/plugins/cla/<scope>/tests` and record the per-scope number and the total. Record the Node count from `node --test .claude/plugins/cla/skills/project-review/scripts/mechanical-checks.test.mjs` (`ℹ tests N`). Write both into the PR body. **Do not reuse design.md's 1105/70** — measured before change (a) landed, so they are the reference range, not the assertion. If the new total is not within a handful of 1105, stop and find out why before moving anything.
- [ ] 1.4 **Record the three subtractions individually**, by the same command against each: `consistency-checks/tests/test_source_only_markers.py` (design.md pins **9**), `consistency-checks/tests/test_runner_stream_encoding.py` (**5**), and `test_doc_facts.py::test_the_scope_discovery_rule_here_matches_what_run_tests_finds` (**1**). Confirm each is still what design.md pins. These three, and nothing else, are what the post-move assertion is allowed to subtract. **Do not carry a total from a previous draft of this change** — the `− 9` figure an earlier version pinned counted one deletion and missed two.
- [ ] 1.5 Record the tracked-file counts to check the arithmetic against later: `git ls-files .claude/plugins/cla | wc -l` (design.md predicts **173** post-(a)), and the dev-only subset count (predicts **71**).
- [ ] 1.6 **Capture the full measurement block** every later doc edit is graded against, each command and its verbatim output into a scratch file: `git ls-files .claude/plugins/cla | wc -l`; `find . -name pyproject.toml -not -path './.git/*'` (the scope count); `ls -d .claude/plugins/cla/skills/*/SKILL.md | wc -l` (**21** today, **20** once `release` leaves); `git ls-files '.claude/plugins/cla/**/scripts/*' .claude/plugins/cla/lib` (the scripts that must each have a row in `CLAUDE.md`'s script table); `git ls-files .claude/plugins/cla | grep -c '\.py$'` (**80**, the input to task 7.4's floor).
- [ ] 1.7 Enumerate, into a scratch file, **every** `Path(__file__).resolve().parents[N]` occurrence with its file, line, N, and what it resolves to today: `grep -rn "Path(__file__).resolve().parents\[" .claude/plugins/cla --include=*.py`. design.md predicts **31**. Add `conformance-checks/tests/test_no_project_tokens.py`'s ancestor walk (a `cla`-under-`plugins` search with a `parents[2]` fallback, which grep for `parents[` will not find as a walk). **Do not add `test_project_facts_paths.py` to this list** — design.md D6 records that it resolves via `git rev-parse --show-toplevel` with a `.claude`-walk fallback that refuses `~/.claude`, so it survives the move in a git checkout; confirm that by reading it rather than assuming either way. This list is the checklist task 6.2 audits against; it cannot be reconstructed after the move.
- [ ] 1.8 **Answer design.md's one open question before moving anything.** List the `*.py` basenames across all nine plugin `scripts/`-style directories that the union `pythonpath` will hold, and check for duplicates. The test-basename measurement does **not** answer this — it is about test files; this is about the scripts they import. If a collision exists, stop: the single scope is not viable as designed and the change needs re-shaping, not a workaround.

## 2. Create the dev tree and its single scope

- [ ] 2.1 Create `<repo>/plugin-tests/` with subdirs `tests/`, `mutants/`, `scripts/`, `node/`. Confirm the name is free at the repo root before creating it.
- [ ] 2.2 Write `plugin-tests/pyproject.toml` with **exactly** the `testpaths` and **10-entry** `pythonpath` pinned in design.md. Do not add entries for `conformance-checks` or `skills/release` (neither declared one) and do not carry `launcher-checks`'s `pythonpath = ["scripts"]` — it names a `scripts/` directory that **does not exist** (`git ls-files .claude/plugins/cla/launcher-checks` returns 4 files, none under `scripts/`); reproducing a dead entry is how a wrong path survives a move.
- [ ] 2.3 Head the file with a comment stating why there is now one scope: the split rested on exactly two colliding module basenames (`conftest.py`, which pytest special-cases per directory, and `test_aggregate.py`, renamed by change (a)), measured by `git ls-files '.claude/plugins/cla/**/tests/*.py' | xargs -n1 basename | sort | uniq -d`. State the command in the comment — the claim is a measurement and must name the thing that produced it. Do **not** repeat `run_tests.py`'s own docstring reason: it names a second collision (`scripts/log_run.py`) that `git ls-files | grep log_run` shows does not exist.
- [ ] 2.4 Confirm `pytest` run from the repo root against `plugin-tests/` picks up this config as rootdir (`--collect-only -q` on the still-empty tree should report a clean 0-collected run, not a config error).

## 3. Move the tests — `git mv` only, no content edits in these commits

- [ ] 3.1 `git mv` every `tests/` directory under `.claude/plugins/cla/` into `plugin-tests/tests/` under the layout pinned in design.md (`conformance/`, `consistency/`, `launcher/`, `hooks/`, `lib/`, `skills/<name>/`). **`git mv`, never delete-and-write** — 71 files move, and a plain rewrite renders as unrelated delete+add, which is how a genuinely dropped file becomes invisible in review.
- [ ] 3.2 `git mv` both `mutants/` directories into `plugin-tests/mutants/`, **mirroring the `tests/` subdirectory names** (`mutants/conformance/test_skill_lint.py`, `mutants/consistency/test_doc_facts.py`). Keep them **outside** `tests/`: they are `test_*.py` by name, so under `tests/` pytest would collect and run them, and `mutants/test_skill_lint.py` would collide with `tests/conformance/test_skill_lint.py`. The mirroring is not cosmetic — `test_guards_have_mutant_batches.py` maps guard↔batch positionally in both directions, and a flat `mutants/` against a nested `tests/` breaks both mappings (task 7.2).
- [ ] 3.3 `git mv .claude/plugins/cla/mutate.py plugin-tests/mutate.py`.
- [ ] 3.4 `git mv .claude/plugins/cla/consistency-checks/scripts/check_script_drift.py plugin-tests/scripts/`. It is a script, not a test, and it moves because its entire subject is this repo's own source. It is the **only** `scripts/` directory in the three `*-checks/` scopes.
- [ ] 3.5 `git mv .claude/plugins/cla/skills/project-review/scripts/mechanical-checks.test.mjs plugin-tests/node/`. Its sibling `mechanical-checks.mjs` **stays in the plugin** — it is shipped code a consuming repo configures and runs.
- [ ] 3.6 Confirm `conformance-checks/`, `consistency-checks/`, and `launcher-checks/` are now empty of tracked files apart from their `pyproject.toml` and markers, and `git rm` those three directories entirely in section 5.
- [ ] 3.7 Commit the moves **on their own**, with no content edit in the same commit. Verify with `git show --stat --find-renames` that the commit reads as renames; if any file shows as delete+add, redo it as a `git mv`.

## 4. Move `release` to a repo-local skill

- [ ] 4.1 `git mv .claude/plugins/cla/skills/release/SKILL.md .claude/skills/release/SKILL.md`. Only `SKILL.md` moves here; `scripts/check_shipped_tree.py` is added later, by task 9.1.
- [ ] 4.2 `git mv .claude/plugins/cla/skills/release/tests/test_release_preconditions.py plugin-tests/tests/skills/release/`. Its tests go to the dev tree with every other test — putting them under `.claude/skills/release/` would need a second `pyproject.toml` and re-create the multi-scope split this change removes.
- [ ] 4.3 **Rewrite all eight lines of `SKILL.md` that name a moved or deleted asset**, per design.md D5's table: the `description:` trigger string (3) and the `# /cla:release` heading (8) become `/release`; the five `${CLAUDE_PLUGIN_ROOT}/…` references (42, 59, 80, 89, 90) become repo-relative paths — that variable is defined only for a file loaded as part of a plugin, so all five resolve to nothing after the move.
- [ ] 4.4 **Fix the dangling `run_tests.py` precondition — this change owns it, because this change deletes the file.** Lines 42 and 89 invoke `python3 ${CLAUDE_PLUGIN_ROOT}/run_tests.py`; replace both with the new gate, bare `pytest` run from the repo root. The precondition-table row at line 50 ("`run_tests.py` fully green | There is no CI. This run is the only gate that exists.") names the same file in prose and changes with them — the *reason* is unchanged and must survive verbatim; only the command changes.
- [ ] 4.5 **Re-point lines 76 and 78**, which name `consistency-checks/tests/test_marketplace_manifest.py` and `consistency-checks/tests/test_doc_facts.py` as the tests that pin the three-file version agreement. Both leave the plugin tree in task 3.1. These are load-bearing rather than incidental: they are how the skill tells a release operator which check catches a half-done version bump.
- [ ] 4.6 Confirm the skill's refusal contract survived the move without an edit: step 1 already says *"If any fails, **stop and say which one** — do not offer to proceed anyway, and do not 'fix' a precondition as part of the release."* If it survived, do **not** restate it.
- [ ] 4.7 Confirm the skill loads and is invocable as `/release` in a session, and that `/cla:release` no longer resolves.

## 5. Delete what the move makes subjectless

- [ ] 5.1 `git rm .claude/plugins/cla/run_tests.py`. **Deleted, not moved** — Q4 supersedes Q2 for this one file. All three of its jobs vanish at once: multi-scope discovery (one scope now), near-miss detection (one `pyproject.toml` now), and source-repo-only skipping (5.2).
- [ ] 5.2 `git rm` the three `SOURCE-REPO-ONLY.md` files (`consistency-checks/`, `launcher-checks/`, `skills/release/`).
- [ ] 5.3 `git rm` `test_source_only_markers.py` (wherever task 3 moved it) **and** its mutant batch. The guard calls `run_tests.py::_is_source_repo()` directly, so it cannot even import after 5.1 — relocating it was never an option. Intended loss of coverage: **9 tests**.
- [ ] 5.4 `git rm` `test_runner_stream_encoding.py` (wherever task 3 moved it). Its entire content is assertions about `run_tests.py` pinning its own stdout: it `ast.parse`s the runner (line 69) and runs `import run_tests` in a cp1252 child (line 48). There is nothing to re-point it at. Intended loss of coverage: **5 tests**. State this deletion explicitly in the PR body — it was not in the original scope and it is the second of exactly two.
- [ ] 5.5 Delete `test_doc_facts.py::test_the_scope_discovery_rule_here_matches_what_run_tests_finds` (the function only, not the file). It reads `run_tests.py` and asserts `len(scopes) >= 8`; both halves are about the deleted runner and one scope cannot satisfy the floor. Intended loss of coverage: **1 test**. Leave a one-line comment where it stood recording why, so the next reader does not restore a check against a file that no longer exists.
- [ ] 5.6 `git rm` all 12 old `pyproject.toml` files. Net −11 (the new one is task 2.2).
- [ ] 5.7 Confirm the arithmetic: `git ls-files .claude/plugins/cla | wc -l` == **101**, `git ls-files .claude/skills/release | wc -l` == **1** at this point (**2** after task 9.1), and total deletions == **18**. If any differs, find the file before proceeding — a count off by one is a dropped or duplicated file, not a rounding error.

## 6. Re-point the resolvers (separate commits from the moves)

- [ ] 6.1 Re-point every `Path(__file__).resolve().parents[N]` from task 1.7's list. The depths all change: a test that was `<scope>/tests/x.py` reaching the plugin root at `parents[2]` is now `plugin-tests/tests/<area>/x.py` and must reach **out of the dev tree and into the plugin**. Watch the derived roots especially — six files compute `_REPO_ROOT = _PLUGIN_ROOT.parents[2]`, so a wrong plugin root silently yields a wrong repo root.
- [ ] 6.2 **Resolution audit (required — this is the check the change exists to survive).** For every entry in task 1.7's list, state: old N, new N, and the directory it resolves to now, **verified by running it**, not by counting segments. A wrong `N` does not raise — it lands on a real directory and the test reports clean over fewer files or none. For any test whose subject is a *scan*, assert a **non-zero count of files actually inspected**, not merely exit 0.
- [ ] 6.3 Re-point `conformance-checks/tests/test_no_project_tokens.py`'s ancestor walk separately (whatever change (a) left of it). It searches `Path(__file__).resolve().parents` for a directory named `cla` whose parent is `plugins`, falling back to `parents[2]`. From `plugin-tests/` there is **no such ancestor at all**, and the fallback lands on the repo root — a real directory, so it fails silently. It needs the plugin root resolved explicitly.
- [ ] 6.4 **`pythonpath` audit.** For each of the 10 entries in `plugin-tests/pyproject.toml`, confirm the directory exists and that at least one test actually imports a module from it. An entry pointing nowhere is silent (it just contributes nothing to `sys.path`) — which is exactly how `launcher-checks`'s dead `scripts` entry survived. Report the entry→importing-test mapping. Run this **once, over all 10** — the entry for the release scan (task 9.1) is pinned in design.md rather than added later, so there is no second audit.
- [ ] 6.5 `check_script_drift.py` — **two roots, not a corrected depth.** Its `PLUGIN_ROOT = parents[2]` (line 35) serves all three sibling groups, and the move splits them: groups 1 and 2 name `lib/log_run.py` and the two retro aggregators, which **stay in the plugin**; group 3 names `hooks/tests/test_block_worktree_path_escape.py`, `hooks/tests/test_block_unsafe_recursive_delete.py` and `skills/new-worktree/tests/test_manual_worktree.py`, which **all move to `plugin-tests/`** while their subjects stay behind. Make each group declare which tree its paths are relative to, and re-point its own `parents[2]` for its new home at `plugin-tests/scripts/`.
- [ ] 6.6 Record, and close, change (a)'s task 3.10 finding: **`check_script_drift.py` does not pass vacuously.** `check_group` appends `"{rel}: file not found (listed in …)"` to `problems` and continues, and `main()` exits non-zero on any problem. Confirm that is still true from its new home by running it against a deliberately broken path, then restoring. Either way the answer comes from running it.
- [ ] 6.7 Any remaining test that referenced `run_tests.py` other than the three deleted units — read each and state the decision with its reason: `test_mutate.py` (line 203) and `test_check_script_drift.py` (line 12) name it only in a docstring and keep their subject; `test_subprocess_encoding.py` names it as a live scan anchor and is task 7.4's. Do not silently leave a test importing or asserting on a file that no longer exists.

## 7. Repair the guards the move collapses — re-derive, never relax

Every floor below is `assert len(...) >= N` over a set this change re-homes. A floor whose subject moved has two honest outcomes: re-pointed and still holding, or genuinely changed **with the reason recorded**. Lowering one to make a red run green is worse than never having written it, because the assertion then certifies what it stopped checking.

- [ ] 7.1 Record the "before" for each floor **on the (a)-landed tree**, so every "after" is a comparison rather than a fresh guess: guard-test files, plugin `.py` files, hardcoded-path scan set, `SKILL.md` files. design.md's figures (**17**, **80**, **126**, **21**) were measured **pre-(a)** and have expired — (a) promotes two conformance guards to skill scripts and adds two scripts, so at least the first two move. Re-capture them; do not carry them.
- [ ] 7.2 **`test_guards_have_mutant_batches.py` — four separate repairs, not one.** (i) `_CHECK_SCOPES = ("conformance-checks", "consistency-checks")` (line 26) are literal directory names; re-point them at the dev tree's `tests/` subdirectories. (ii) `_guard_files()` (line 71) globs `_PLUGIN_ROOT / scope / "tests"` — `glob()` on a missing directory returns empty and does not raise, so this returns `[]` after the move. (iii) `batch = path.parents[1] / "mutants" / path.name` assumes the per-scope sibling layout; task 3.2's mirrored `mutants/` restores it as a one-step substitution — verify both directions, since `test_every_batch_is_loadable_and_declares_real_targets` maps the other way (`batch.parents[1] / "tests" / batch.name`). (iv) `_EXEMPT` has **14** keys, **12** of them `"grandfathered"` (count them by parsing the literal, do not trust a prose figure — an earlier review said 12 keys, which is the grandfathered subtotal, not the key count); every key is a `<scope>/tests/<file>.py` path that stops resolving, so re-key all of them. **`assert len(files) >= 6` must survive as a real floor** — the re-pointed discovery finds **13–15** guard files depending on what (a) left in the conformance set, so the floor holds unchanged across that whole range and has no reason to move. Confirm the actual number by running it; if it lands below 6, the discovery is still wrong and the answer is to fix the discovery, not the 6.
- [ ] 7.3 Deleting `test_runner_stream_encoding.py` (task 5.4) removes one `_EXEMPT` key: **14 → 13 keys, 12 → 11 grandfathered.** That is a shrink, so `test_the_grandfather_list_only_shrinks`'s `grandfathered <= 12` cap **does not move**. Confirm the count by running it; if re-keying changed it by anything other than this one deletion, something was dropped — the count is the check, not the keys.
- [ ] 7.4 **`test_guards_are_not_vacuous.py` — the identical collapse, named by neither original change.** `_GUARD_TEST_DIRS = ("conformance-checks/tests", "consistency-checks/tests")` (line 42) is globbed under `_PLUGIN_ROOT` (line 48). Re-point both. Note for the record that it is **not** floorless — it carries two floors (`len(files) >= 6` at line 132, `total_asserts >= 5` at line 139), so it fails loudly rather than going silently vacuous; a review note claiming otherwise is wrong, and neither floor needs to move (13–15 files after re-pointing, per 7.2).
- [ ] 7.5 **`test_subprocess_encoding.py` — the one a floor alone would not catch, because its named anchors go first.** Its subject is "every `.py` this repo owns that could spawn a subprocess", which after the split is **two trees**. (i) Make `_scanned_files()` scan the plugin **and** the dev tree. (ii) Its five named anchors include `"run_tests.py"` (deleted — drop it, with the reason recorded), `"hooks/tests/test_dispatch.py"` and `"skills/spec-to-pr/tests/conftest.py"` (both moved — re-point). Keep an anchor in each of the four subtrees it deliberately spans; that spread is the stronger half of the pair. (iii) Re-derive `assert len(names) >= 55` **by running it over the union** — the plugin alone falls to **27** `.py` files, so leaving the root unchanged would force exactly the false lowering this task forbids. Its own comment already states the rule: *"Lower it to the new real count, never to a number chosen to be safe from future deletions."*
- [ ] 7.6 **`test_no_hardcoded_plugin_paths.py`** — re-point `_PLUGIN_ROOT`; its `SCANNED_ROOTS = ("skills", "agents", "output-styles", "hooks", "lib")` all stay in the plugin, so its subject does not move. Its `len(files) >= 95` floor **holds, at 96** (in-scope files drop from 126 to 96 as the `tests/` trees leave). Record that margin of 1 in the PR body: it is now one deleted reference file away from a false alarm, which is a finding, not a reason to lower it.
- [ ] 7.7 **`test_skill_lint.py`** (`len(files) >= 17` SKILL.md files, 21 → 20) and **`test_overlays_are_reachable.py`** (`len(found) >= 8`, subject in `cla.io/`) and **`test_hooks_wiring.py`** (`len(refs) >= 3`) — re-point roots only; confirm each floor still holds rather than assuming it.
- [ ] 7.8 **`test_release_preconditions.py` straddles the split and all four of its tests fail on the move.** `_PLUGIN_ROOT = parents[3]` (31), `_REPO_ROOT = _PLUGIN_ROOT.parents[2]` (32), `_PLUGIN_JSON` (34) and `_SKILL_MD = _PLUGIN_ROOT/"skills"/"release"/"SKILL.md"` (36). After the move the test is in `plugin-tests/`, its `SKILL.md` subject is in `.claude/skills/release/`, and `plugin.json` is still in the plugin — three roots from one `parents[N]`. Resolve the repo root once and derive all three subjects from it; a corrected depth cannot express this.
- [ ] 7.9 Run `plugin-tests/` alone at this point and confirm the only remaining failures are the doc-fact ones section 10 fixes. A red run here that is **not** a `test_doc_facts.py` failure means a floor or a resolver is still wrong.

## 8. Fix the dangling references in shipped files

- [ ] 8.1 **Prune `SOURCE_SCAN_ROOTS` in `skills/_shared/scripts/check_no_project_tokens.py` from 8 entries to 5** — keep `skills`, `agents`, `hooks`, `output-styles`, `lib`; drop `conformance-checks`, `consistency-checks`, `launcher-checks`. Not cosmetic: the iteration is `if not root.is_dir(): continue`, so a vanished root is skipped **silently** and the shipped guard would scan 5 roots while claiming 8 and still exit 0. Update the accompanying comment in the same edit — the comment is what makes the list checkable.
- [ ] 8.2 Run the pruned guard against this repo and confirm it exits 0 **and reports a non-zero scanned-file count spanning more than one root**. A guard whose scan shrank silently is the exact defect 8.1 exists to prevent, so verify the count, not the exit code.
- [ ] 8.3 **`.claude/plugins/cla/skills/_shared/README.md` (~line 40) — a shipped file mandating the deleted runner.** It states that "`run_tests.py` fails the whole run as a 'near-miss' if a directory has a pytest-configured `pyproject.toml` without a `tests/`, or the reverse, so the three move together or not at all." The rule is deleted with the runner and the instruction it justifies is falsified by this very change (the tests move, `git_state.py` stays). Rewrite it to describe where `git_state.py`'s tests now live. This file is in the published tree and is reached by neither conformance scanner as a *command* check, so nothing but this task catches it.
- [ ] 8.4 **`.claude/plugins/cla/skills/_shared/references/skill-authoring.md` (~line 56) — the same class.** It offers "✅ ``run_tests.py`` exits 0 with no near-miss warning" as the model of a step whose done-condition is one command and one bit. The teaching point survives verbatim; only the example command changes to the bare-`pytest` gate. Also in the published tree.
- [ ] 8.5 Grep the whole shipped tree for any remaining reference to a moved or deleted path: `grep -rnE "run_tests\.py|conformance-checks|consistency-checks|launcher-checks|mutate\.py|SOURCE-REPO-ONLY" .claude/plugins/cla`. Every surviving hit is fixed or justified in one line. The plugin `README.md` is the one that reaches consumers.
- [ ] 8.6 Update `cla.io/overlays/codify-learnings.md` (5 hits: the `run_tests.py` commands ~34–35, the gate prose ~44/47, and `conformance-checks/tests/test_guards_are_not_vacuous.py` ~77). This is a **live overlay the harness reads at runtime**, not archive material — a stale command here is executed, not just read.

## 9. The shipped-tree scan

- [ ] 9.1 Write `<repo>/.claude/skills/release/scripts/check_shipped_tree.py`. Enumerate via `git ls-files .claude/plugins/cla`, strip the plugin-root prefix, and match each path against the **14 patterns and the one leaf-name exclusion pinned in design.md**. Enumerate tracked files, **not** a filesystem walk — the plugin is published from the repository, so tracked files are exactly what ships, and a walk would flag `__pycache__/` and `.pytest_cache/`, which never reach a consumer.
- [ ] 9.2 Carry the allowlist as a table with a **reason string per entry**, not a bare regex. The reason column is the discipline, not decoration — it is what makes a future addition arguable. Model it on `test_guards_have_mutant_batches.py`'s `_EXEMPT` block, whose comment states the same rule: *"Keep this list short and justified — it is the pressure valve that could quietly empty this test if it grew without argument."*
- [ ] 9.3 Implement the pinned exit contract: **0** clean (printing files-examined and patterns-applied counts), **1** violations (every offending repo-relative path, one per line, never stopping at the first), **2** could-not-run (`git ls-files` failed, **or it returned zero files**). The exit-2-on-empty row is load-bearing: a scan run from the wrong directory otherwise reports "0 violations" over a tree it never read.
- [ ] 9.4 Write `plugin-tests/tests/skills/release/test_check_shipped_tree.py` with **seeded-input** tests, each planting a violating file in a `tmp_path` fixture and asserting exit 1 **naming that file**. Plant, at minimum, the five shapes a denylist catches (`tests/` dir, `pyproject.toml`, `mutants/` dir, `*.test.mjs`, `conftest.py`) **and the four shapes the 11-pattern draft was measured to accept**: `skills/<x>/scripts/mechanical-checks.test.mjs`, `skills/<x>/scripts/conftest.py`, `hooks/tests/test_x.py`, `hooks/pyproject.toml`. Those four are the reason the pattern set is 14 and not 11; a test suite that omits them would pass on the draft that was wrong.
- [ ] 9.5 Add the **exit-2 test** (empty enumeration) and the **exit-0-on-the-real-tree** test, the latter asserting a files-examined count equal to `git ls-files .claude/plugins/cla | wc -l` rather than merely exit 0.
- [ ] 9.6 Add the companion **cap test**: the allowlist has exactly 14 entries and each carries a non-empty reason string. Mirror `test_the_grandfather_list_only_shrinks`'s message shape — state that the list was 14 when the convention landed and that a fifteenth pattern lands by raising the cap in the same commit that adds the pattern and its reason.
- [ ] 9.7 Write the mutant batch at `plugin-tests/mutants/release/test_check_shipped_tree.py`. `CLAUDE.md`'s rule 4 requires the full four checks for a new script, and this is the one that proves the tests can fail. **Mutate what the scan *touches*, not only what it targets.** At minimum: the exit code on the empty-enumeration branch, one pattern's anchor (`$` → nothing, which widens it), the `[^/.]+` stem in pattern 14 (→ `[^/]+`, which is exactly the widening D1a caught), and the "report all" loop's early exit.
- [ ] 9.8 Run the batch and record survivors. A survivor is a finding, not a formality — and per `CLAUDE.md`, a clean run is evidence about the mutants you thought of and nothing else.
- [ ] 9.9 Run the scan against the real tree and confirm **exit 0 with a files-examined count matching task 1.6's `git ls-files` number** (**101** after this change). A count that agrees with an independently-measured one is the check; exit 0 alone is not.
- [ ] 9.10 Confirm the scan is **not** inside the plugin: `git ls-files .claude/plugins/cla | grep check_shipped_tree` returns nothing. The check against shipping unusable assets must not itself be one.

## 10. Wire the scan into the release skill

- [ ] 10.1 In `.claude/skills/release/SKILL.md` step 1, add the scan invocation to the precondition command block (which after task 4 holds `git rev-parse --abbrev-ref HEAD`, `git status --porcelain`, `git fetch origin`, `git status -sb`, and the bare-`pytest` gate).
- [ ] 10.2 Add the fifth row to the precondition table, in the table's own idiom — a "Why it is not negotiable" clause, not a description. The reason is that `git-subdir` has no exclusion field, so a stray dev asset in the tree ships to every consumer and a published tag is never moved.
- [ ] 10.3 Confirm the skill's existing refusal contract (task 4.6) covers the new precondition without an edit. If it does, do **not** restate it for the new row.

## 11. The two false claims — deleted, not reworded

- [ ] 11.1 Root `.gitattributes`: delete the second of the "TWO BELTS" bullets — `hooks/tests/test_hooks_wiring.py::test_the_probe_has_no_carriage_returns` "ships with the plugin, so it also fires in a consuming repo." After this change it does not ship, and it never fired downstream anyway (a consuming repo has no pytest gate over the plugin cache). Reword the "TWO BELTS to this braces" line to match the one belt that remains. Keep the first belt (`.claude/plugins/cla/.gitattributes` carrying the same pin inside the published tree) — it is true and unaffected.
- [ ] 11.2 **Prove the `.gitattributes` rules are untouched.** `git diff .gitattributes` must show changes **only** inside comment lines. Assert the four attribute lines are byte-identical: `cla text eol=lf`, `cla.cmd text eol=crlf`, `.claude/plugins/cla/hooks/git/pre-push text eol=lf`, `.claude/plugins/cla/hooks/*.sh text eol=lf`. Then confirm **each of the four paths still resolves** — one names a path inside the plugin, and the whole point of checking is that this change moved a great deal out of that tree. (Expected: `hooks/` stays shipped in full, so all four hold — but expected is not verified.)
- [ ] 11.3 Root `.gitattributes`: `consistency-checks/tests/test_pre_push_is_installed.py` moved to the dev tree. This one is a **path fix, not a deletion** — the claim that a test catches an absent or drifted `pre-push` copy is still true. Re-point it, and do not let it be deleted alongside 11.1's genuine falsehood. The two edits are adjacent in the same comment block and read similarly; that is the trap.
- [ ] 11.4 Do not rewrite the surrounding comment block. It documents a measured, platform-divergent CRLF hazard (Cygwin/Git Bash strip the CR; `dash` syntax-errors) and it *already records a previous bad edit to itself* — an earlier version asserted a failure mode that does not occur on the platform it named "and got the mechanism backwards besides." Diff old against new and state what changed; anything beyond 11.1's bullet, 11.3's path, and 11.2's "TWO BELTS" count is out of scope.
- [ ] 11.5 `CLAUDE.md`: delete "`conformance-checks` is portable core that reaches consuming repos". It has no true reading after (a) and this change — the directory does not exist, two of its five guards are skill scripts in the shipped tree, three are tests in `plugin-tests/`. Replace with what is true: the two promoted checkers reach consumers because they are skill scripts inside the shipped tree; the three plugin-data guards do not reach consumers at all and are not meant to. This sentence sits in the **Commands** section's `*-checks/` explainer, **not** in the Architecture section — the parallel claims there are 12.7's.

## 12. `CLAUDE.md` — mechanical command fixes first

Line numbers are from the pre-change file; re-locate by text, since earlier edits shift later lines. This section is deliberately separated from section 13: these are line-level substitutions with an obvious right answer, and keeping them out of the rewritten blocks gives section 13's rewrite audit a clean unit to diff.

- [ ] 12.1 The three `run_tests.py` invocations (**60, 61, 62**) become the bare-`pytest` gate.
- [ ] 12.2 The per-scope `pytest` examples (**64–69**, "Run a single scope in isolation", `pytest .../skills/<name>/tests`, `pytest .../hooks/tests`) name paths that no longer exist — replace with running a subset of the one scope (`pytest plugin-tests/tests/hooks`, `-k <name>`).
- [ ] 12.3 **The bare-`pytest` prohibition (72–73) is now inverted.** "**Do not run bare `pytest` from the plugin root or repo root** — it will fail collection by design" becomes the opposite: bare `pytest` from the repo root **is** the gate. This is the single most consequential inversion in the file; a reader who follows the old line will not run the suite at all.
- [ ] 12.4 The `mutate.py` invocation (**~135**) — now at `plugin-tests/mutate.py`, and no longer `${CLAUDE_PLUGIN_ROOT}`-relative.
- [ ] 12.5 The per-change gate table (**155–160**): rows **157**, **158** have no per-scope target any more; row **160** ("Before opening a PR | `run_tests.py`, once") names a deleted command. Preserve the table's *intent* — match the checking to the change, run each gate once — and change only the commands. The lesson recorded beneath it (**165–167**, the five-scope-run anecdote) is **history and stays**; reframe its command names as past tense rather than deleting the lesson.
- [ ] 12.6 The Node-suite paragraph (**169–175**): "`run_tests.py` **does** run it — as a 13th entry alongside the 12 pytest scopes" is doubly false; the path at **175** also moved to `plugin-tests/node/`.
- [ ] 12.7 The `/cla:release` references (**274–277**): "Every skill is invocable as `/cla:<name>`" needs the carve-out this change added to the spec — `release` is repo-local and invoked as `/release`.
- [ ] 12.8 **The release line (31).** `**Current release: `cla--v0.10.0`.**` — leave the version alone. This change does **not** cut a release; `test_doc_facts.py::test_the_release_version_agrees_across_the_manifest_and_the_prose` asserts this line matches `plugin.json` **and** requires the literal phrasing `**Current release: `cla--v<version>`**` to survive. Confirm both at the end.

## 13. `CLAUDE.md` — the blocks that are rewritten, not edited

Each item here replaces a block rather than substituting a line, so each is covered by the rewrite audit (task 15.5): diff old against new and state what was dropped, including "nothing" where that is the finding.

- [ ] 13.1 **The 12-scope block (72–101), rewritten as one description of one scope.** It covers: "Each skill that ships tests (6 today)" (**73**); the three `*-checks` enumeration (**73–74**); "pytest scope — 12 in total" (**75**); the per-scope `pyproject.toml`/`testpaths`/`pythonpath` explanation (**75–78**); "`lib/` and the three `*-checks/` scopes are the odd ones out" (**80–81**); the `conformance-checks`/`consistency-checks` descriptions (**82–86**); the source-repo-only paragraph (**87**); `launcher-checks` (**87–89**); "All four sit outside the synced set" (**91–92**); and the same-named-helper-modules rationale plus `run_tests.py`'s discovery and "near-miss" rule (**95–101**). Note when rewriting that the same-named-modules justification rested on **one** real collision, not the two the runner's docstring claims.
- [ ] 13.2 **The No-CI section (178–194).** "`run_tests.py` is the whole verification story — every pytest scope plus the Node suite, in one command" (**180–183**) is false in both halves; "while iterating, run the one scope you are changing" (**183**) has no referent. **The section's argument stays** — no CI, verification is local, do not add a workflow (**185**) — and the two consequences at **187–194** (platform-divergent code is only exercised on your machine; merging is unguarded) are unaffected and must survive verbatim. Check the directory-alias claim at **189–192** against the moved tests rather than assuming the count.
- [ ] 13.3 **The fact/procedure-split section (201–226).** "A pytest **conformance guard** fails if…" (**206–210**) — the guards are no longer a pytest scope in the plugin. "eight roots — the four synced dirs plus `lib/` and the three `*-checks/` scopes" (**210–212**) is **five** after task 8.1; carry that pinned number rather than re-deriving it. "Four files still fall outside both scanners" (**212–214**) — `run_tests.py` and `mutate.py` were two of the four and no longer ship, so **re-measure the list, do not subtract two**; `hooks/probe-python.sh` stays on it. The staleness-guard sentence (**226**) now names `skills/sync-context/scripts/check_fact_paths.py`.
- [ ] 13.4 **The skill layout tree (230–244).** Delete the `run_tests.py` line (**233**); move `mutate.py` (**234**) out of the tree or annotate it as dev-tree; drop "+ tests" from the `hooks/` line (**236**); delete the `tests/` leaf under `skills/<name>/` (**243**). Add `plugin-tests/` and `.claude/skills/release/` as siblings so a reader can find the tests and the release skill at all — a layout diagram that omits where the tests went is worse than one that is merely stale.
- [ ] 13.5 **The script table (253–270).** Remove the `run_tests.py` row (**255**) — the script is deleted. Move or re-scope the `mutate.py` row (**256**) and the `check_script_drift.py` row (**258**) to their dev-tree paths. Update the `aggregate.py` row (**259**) if change (a) left it needing it, and **drop the "same-named" framing**, which was the justification for a split that no longer exists. Rows **257** (`lib/log_run.py`) and **261** (`mechanical-checks.mjs`) are unaffected — confirm rather than assume.
- [ ] 13.6 **Add the rows the table now owes.** Under the table's own rule (each script justified in one line): `skills/sync-context/scripts/check_fact_paths.py` and `skills/_shared/scripts/check_no_project_tokens.py` from change (a) — (a)'s task 5.4 explicitly deferred this decision here — plus this change's `check_shipped_tree.py`. Cross-check the finished table against task 1.6's `git ls-files` script listing: **every script present has a row, and every row names a script that exists.**

## 14. The other four documents

- [ ] 14.1 **`.claude/plugins/cla/README.md` — and remember this one ships.** Delete the `release` row from the skills table (**55**) — a consuming repo will never have that skill. Fix the conformance-guard prose (**103–106**) and the staleness-guard sentence (**113**). Rewrite the testing section (**136–154**): "6 today" and the scope enumeration (**136–142**), "12 in all" (**141**), the same-named-modules rationale (**141–142**), the `run_tests.py` commands (**145–146**), the SOURCE-REPO-ONLY sentence and the per-scope `pytest` path (**149**), the 13th-entry claim (**150–151**), the `node --test` path (**154**). Fix the layout block (**159–178**): lines **164, 165, 167, 170, 171, 172, 173, 177**. **Constraint specific to this file: it ships to consumers, so it must not instruct them to run anything in `plugin-tests/`** — describe the dev tree as the source repo's, or omit it. Covered by the rewrite audit.
- [ ] 14.2 **`DEVELOPER-GUIDE.md`.** The verification section (**318–346**): the lead-in (**318**), the `run_tests.py` and `node --test` commands (**321–322**), the skip-count instruction (**325–326**), the whole source-repo-only bullet (**328–332**, including the `test_source_only_markers.py` expected-set instruction, which now names a deleted file), the "**Never run bare `pytest`**" inversion (**333**), the scope enumeration (**333–336**), the per-scope path (**337**), the "pytest conformance guards" framing (**339–342**), and the `consistency-checks/` sentence (**344–346**). Then the contracts section: "**Five** contracts, each enforced by a test" (**352–356**) becomes **four**, because contract 4 — "A test scope needs `pyproject.toml` AND `tests/` together", enforced by `run_tests.py`'s near-miss rule (**367–369**) — has both its requirement and its enforcer deleted. Re-point `test_skill_lint.py` (**358–360**), the shared test at **363–366**, and `test_doc_facts.py` (**374–375**). Update the release section (**379–381**) and the cheat-sheet row (**429**) to `/release`. Covered by the rewrite audit.
- [ ] 14.3 **Root `README.md`.** Layout tree (**64–81**): `run_tests.py` (**72**), "21 workflow skills" → **20** (**73**, and the number must equal `ls -d .claude/plugins/cla/skills/*/SKILL.md | wc -l`, not an arithmetic guess), and the three `*-checks/` lines (**78, 79, 80**). Add `plugin-tests/` and `.claude/skills/release/` as repo-root entries. Fix the conformance-guard prose (**89–90**), the "whole verification story" sentence (**94–95**), the `run_tests.py` command and its "(12 today)" (**98**), the `node --test` path (**99**), the bare-`pytest` prohibition (**102–103**, inverted), and the per-scope paths (**103–105**).
- [ ] 14.4 **`TODO.md` — re-describe, do not discharge.** A re-layout does not pay a debt. Per entry, record a one-line reason for the disposition:
  - "Source-repo-only scopes" (**90–105**): the mechanism is deleted here — replace the entry with one sentence recording that source-repo-only became structural, so the marker mechanism has no subject. The 78-of-103 cost measurement (**99–105**) describes a skip that no longer exists.
  - "Remaining unscanned surface" (**107–136**): **re-measure, do not adjust by arithmetic.** The "120 ship, 116 scanned, these four are not" counts (**110–111**) and the four-file list (**117–118**, where `run_tests.py` and `mutate.py` are two of the four) are void. `hooks/probe-python.sh` (**122**) stays on the list — it is outside both scanners and does not move. The pinned line references (**122–136**) name files that moved and line numbers that shifted.
  - "Split `consistency-checks`" (**188–194**): **moot** — consumers never received `consistency-checks` after this change, so there is no portable half to preserve. Remove it with the reason recorded.
  - "Write mutant batches for the 12 grandfathered guards" (**222–233**): **live and unchanged in substance.** Re-point every path. The count becomes **11**, because `test_runner_stream_encoding.py` was one of the 12 and is deleted by task 5.4 — record *that* as the reason it shrank, not a re-count. "Three batches exist" (**226–228**) becomes **two**, because `test_source_only_markers` and its batch were deleted. Keep the `_EXEMPT` cap discipline exactly as it is.
- [ ] 14.5 **Flagged, not fixed:** root `README.md` **line 42** says `--model sonnet --effort medium`, while the `cla` launcher (lines 33–34) passes `--model opus --effort high`. This is a **pre-existing** falsehood unrelated to this change, found while sweeping. Raise it rather than folding it into this change's diff — an unrequested correction inside a large doc reconciliation is exactly the edit a reviewer cannot separate from the ones under review.
- [ ] 14.6 Leave every historical record untouched: `cla.io/decisions/`, `cla.io/lessons-learned/`, `cla.io/feedback/`, `cla.io/checkpoints/`, `cla.io/retro/*.jsonl`. They record what was true when written.

## 15. Verify

- [ ] 15.1 Run the new gate: `pytest` from the repo root against `plugin-tests/`. Record `collected_after`.
- [ ] 15.2 **Assert the count gate: `collected_after == collected_before − 15 + N_added`**, using tasks 1.3 and 1.4's recorded numbers, and state `N_added` (the tests task 9 adds) as its own figure rather than folding it into the subtraction. Show the arithmetic in the PR body. Any other delta is a dropped test and blocks the change — **a green suite is not the evidence here; the subtraction is.** If the delta is wrong, `git diff --stat --find-renames` against the base and reconcile file by file.
- [ ] 15.3 **`test_doc_facts.py`, in the order design.md D10 fixes.** Edit the docs first, then run the guard, then read *which* failure it is. A **derived-count** failure (`real_skill_count()` counts the tree; `release` leaving makes it 20) means the doc is wrong — fix the doc. An **anchor** failure ("no longer states…", from the 4-way and 3-way literal-phrase parametrizations and the `Current release:` check) means a rewrite dropped a claim the repo deliberately pins — restore a phrase the anchor can find, or move the anchor and say why in the same commit. **Never edit the guard before the docs**, which lands the doc against an expectation already loosened to accept it. Note the guard covers four documents and **`TODO.md` is not one of them** — `TODO.md`'s staleness is real (14.4) but is not what turns this file red.
- [ ] 15.4 Confirm the re-pointed `real_skills_with_tests()` decision held: the helper is re-derived over `plugin-tests/tests/skills/`, the docs keep the claim in re-pointed words, and all three anchors still resolve. If a reconciled document genuinely stopped making the claim and an anchor was dropped, **the reason is stated in the same commit** — the same rule that governs a floor.
- [ ] 15.5 **Rewrite audit (`CLAUDE.md` check 2).** Section 13's five blocks plus 14.1 and 14.2 replace blocks rather than editing lines. For each, diff old against new and state what was dropped — including "nothing" where that is the finding. Two deletions are **deliberate and named in advance** (11.1's `.gitattributes` bullet, 11.5's "portable core" sentence); anything else that vanished is visible against those two.
- [ ] 15.6 **Re-run the whole measurement block from task 1.6 and diff it against the numbers now written in the docs.** Every count in every edited file must equal a command's output recorded in this commit. A measurement taken at the start of a long prose change has expired by the end of it — this design has already produced two such stale numbers itself (the `− 9` gate and "eight of the twelve scopes").
- [ ] 15.7 Run the Node suite at its new path and confirm **70** tests, unchanged.
- [ ] 15.8 Run `check_shipped_tree.py` one final time and confirm exit 0 with a files-examined count matching `git ls-files .claude/plugins/cla | wc -l`. Then plant a `pyproject.toml` in the plugin tree, confirm the scan refuses and names it, and remove it — the end-to-end proof that the precondition actually blocks a release, which no unit test provides.
- [ ] 15.9 Confirm `python3 .claude/plugins/cla/run_tests.py` now fails with "no such file", and that nothing in the repo still invokes it: `grep -rn "run_tests" --exclude-dir=.git --exclude-dir=cla.io .` — every hit fixed or justified in one line. `cla.io/decisions/`, `lessons-learned/`, `feedback/`, `checkpoints/` and `retro/*.jsonl` are excluded deliberately (14.6).
- [ ] 15.10 Re-read task 6.2's resolution audit, task 6.4's `pythonpath` mapping, and section 7's floor table as one block before opening the PR. Those three, plus 15.2, are the change's real gate — the suite cannot catch a walk that now resolves to a plausible wrong directory and scans nothing, nor a floor that was lowered to fit.
- [ ] 15.11 Confirm the scope boundary held: no test was edited for any reason other than re-pointing a path or re-deriving a floor; no test was merged, split, or deleted except the three units named in tasks 5.3–5.5; `lib/log_run.py` is still in the plugin; `skills/project-review/scripts/mechanical-checks.mjs` is still in the plugin; the two guards change (a) promoted are still in the plugin; **no release was cut** and `CLAUDE.md`'s release line and `plugin.json` version are unchanged.
- [ ] 15.12 Note in the PR body: the collected-count arithmetic from 15.2 with its three named subtractions; the measurement block from 15.6 with its commands; the floor table from section 7 with before/after and a reason for every number that moved; the rewrite audit from 15.5; the resolution of both inherited findings (6.6, 7.2); the two deliberate deletions (11.1, 11.5); the `>= 95` margin-of-1 finding from 7.6; and the flagged-but-not-fixed item from 14.5. Record that `0.11.0` is the version the decision doc anticipates and that **cutting it is deliberately not part of this change** — a published tag cannot be corrected, so it is cut from reviewed and merged work, using the precondition this change just added.
