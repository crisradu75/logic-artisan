## 1. Preconditions and the baseline the whole change is graded against

- [ ] 1.1 Confirm change (a) `decouple-skills-from-dev-assets` has landed on this branch's base. Its tasks 3.7 and 6.1 **edit and run `run_tests.py`**, which this change deletes — running them after this change is impossible, so the ordering is a hard dependency, not a preference. Verify concretely: `skills/sync-context/scripts/check_fact_paths.py` and `skills/_shared/scripts/check_no_project_tokens.py` exist, `skills/codify-retro/scripts/codify_aggregate.py` and `skills/spec-to-pr-retro/scripts/spec_to_pr_aggregate.py` exist, and `grep -rn "mutate\.py" .claude/plugins/cla/skills/` is empty.
- [ ] 1.2 Confirm a clean tree and that `python3 .claude/plugins/cla/run_tests.py` is green **once**, before touching anything. This is the last time that command is ever run in this repo; record its summary line.
- [ ] 1.3 **Capture `collected_before`.** For each of the 12 scopes run `python -m pytest --collect-only -q .claude/plugins/cla/<scope>/tests` and record the per-scope number and the total. Record the Node count from `node --test .claude/plugins/cla/skills/project-review/scripts/mechanical-checks.test.mjs` (`ℹ tests N`). Write both into the PR body. **Do not reuse design.md's 1105/70** — those were measured before change (a) landed and are the reference range, not the assertion. If the new total is not within a handful of 1105, stop and find out why before moving anything.
- [ ] 1.4 Record `collected(test_source_only_markers.py)` by the same command against that one file. design.md pins it at **9**; confirm it is still 9. This is the only subtraction the post-move assertion is allowed.
- [ ] 1.5 Record the tracked-file counts to check the arithmetic against later: `git ls-files .claude/plugins/cla | wc -l` (design.md predicts **173** post-(a)), and the dev-only subset count (predicts **71**).
- [ ] 1.6 Enumerate, into a scratch file, **every** `Path(__file__).resolve().parents[N]` occurrence with its file, line, N, and what it resolves to today: `grep -rn "Path(__file__).resolve().parents\[" .claude/plugins/cla --include=*.py`. design.md predicts **31**. Add the two ancestor-walk resolvers that contain no `parents[N]` (`conformance-checks/tests/test_no_project_tokens.py`, `.../test_project_facts_paths.py` — whatever change (a) left of them). This list is the checklist task 6.2 audits against; it cannot be reconstructed after the move.

## 2. Create the dev tree and its single scope

- [ ] 2.1 Create `<repo>/plugin-tests/` with subdirs `tests/`, `mutants/`, `scripts/`, `node/`. Confirm the name is free at the repo root before creating it.
- [ ] 2.2 Write `plugin-tests/pyproject.toml` with **exactly** the `testpaths` and 9-entry `pythonpath` pinned in design.md. Do not add entries for `conformance-checks` or `skills/release` (neither declared one) and do not carry `launcher-checks`'s `pythonpath = ["scripts"]` — it names a `scripts/` directory that **does not exist**; reproducing a dead entry is how a wrong path survives a move.
- [ ] 2.3 Head the file with a comment stating why there is now one scope: the split rested on exactly two colliding module basenames (`conftest.py`, which pytest special-cases per directory, and `test_aggregate.py`, renamed by change (a)), measured by `git ls-files '.claude/plugins/cla/**/tests/*.py' | xargs -n1 basename | sort | uniq -d`. State the command in the comment — the claim is a measurement and must name the thing that produced it.
- [ ] 2.4 Confirm `pytest` run from the repo root against `plugin-tests/` picks up this config as rootdir (`--collect-only -q` on the still-empty tree should report a clean 0-collected run, not a config error).

## 3. Move the tests — `git mv` only, no content edits in these commits

- [ ] 3.1 `git mv` every `tests/` directory under `.claude/plugins/cla/` into `plugin-tests/tests/` under the layout pinned in design.md (`conformance/`, `consistency/`, `launcher/`, `hooks/`, `lib/`, `skills/<name>/`). **`git mv`, never delete-and-write** — 71 files move, and a plain rewrite renders as unrelated delete+add, which is how a genuinely dropped file becomes invisible in review.
- [ ] 3.2 `git mv` both `mutants/` directories into `plugin-tests/mutants/`. Keep them **outside** `tests/`: they are `test_*.py` by name, so under `tests/` pytest would collect and run them, and `mutants/test_skill_lint.py` would collide with `tests/conformance/test_skill_lint.py`. `testpaths = ["tests"]` is the whole exclusion mechanism, exactly as in the old scopes.
- [ ] 3.3 `git mv .claude/plugins/cla/mutate.py plugin-tests/mutate.py`.
- [ ] 3.4 `git mv .claude/plugins/cla/consistency-checks/scripts/check_script_drift.py plugin-tests/scripts/`. It is a script, not a test, and it moves because its entire subject is this repo's own source.
- [ ] 3.5 `git mv .claude/plugins/cla/skills/project-review/scripts/mechanical-checks.test.mjs plugin-tests/node/`. Its sibling `mechanical-checks.mjs` **stays in the plugin** — it is shipped code a consuming repo configures and runs.
- [ ] 3.6 Confirm `conformance-checks/`, `consistency-checks/`, and `launcher-checks/` are now empty of tracked files apart from their `pyproject.toml` and markers, and `git rm` those three directories entirely in task 5.
- [ ] 3.7 Commit the moves **on their own**, with no content edit in the same commit. Verify with `git show --stat --find-renames` that the commit reads as renames; if any file shows as delete+add, redo it as a `git mv`.

## 4. Move `release` to a repo-local skill

- [ ] 4.1 `git mv .claude/plugins/cla/skills/release/SKILL.md .claude/skills/release/SKILL.md`. Only `SKILL.md` moves here.
- [ ] 4.2 `git mv .claude/plugins/cla/skills/release/tests/test_release_preconditions.py plugin-tests/tests/skills/release/`. Its tests go to the dev tree with every other test — putting them under `.claude/skills/release/` would need a second `pyproject.toml` and re-create the multi-scope split this change removes.
- [ ] 4.3 Rewrite the five `${CLAUDE_PLUGIN_ROOT}` references in `SKILL.md` (lines 42, 59, 80, 89, 90) as repo-relative paths (`.claude/plugins/cla/.claude-plugin/plugin.json`, etc.). That variable is defined only for a file loaded as part of a plugin; after the move all five resolve to nothing.
- [ ] 4.4 **Fix the dangling `run_tests.py` precondition — this change owns it, because this change deletes the file.** Lines 42 and 89 invoke `python3 ${CLAUDE_PLUGIN_ROOT}/run_tests.py`; replace both with the new gate, bare `pytest` run from the repo root. The precondition-table row at line 50 ("`run_tests.py` fully green | There is no CI. This run is the only gate that exists.") names the same file in prose and changes with them — the *reason* is unchanged and must survive verbatim; only the command changes.
- [ ] 4.5 Update the skill's frontmatter: `description` says "Triggers on /cla:release"; it is now `/release`. Change the trigger string and nothing else about the description's content.
- [ ] 4.6 Update the `# /cla:release` heading and any in-body self-reference to the bare `/release` form.
- [ ] 4.7 Step 3's prose names `consistency-checks/tests/test_marketplace_manifest.py` and `consistency-checks/tests/test_doc_facts.py` as the tests that pin the three-file version agreement. Both moved in task 3 — re-point to their new paths. These are load-bearing: they are how the skill tells the operator which check catches a half-done bump.
- [ ] 4.8 Confirm the skill loads and is invocable as `/release` in a session, and that `/cla:release` no longer resolves.

## 5. Delete what the move makes subjectless

- [ ] 5.1 `git rm .claude/plugins/cla/run_tests.py`. **Deleted, not moved** — Q4 supersedes Q2 for this one file. All three of its jobs vanish at once: multi-scope discovery (one scope now), near-miss detection (one `pyproject.toml` now), and source-repo-only skipping (5.2).
- [ ] 5.2 `git rm` the three `SOURCE-REPO-ONLY.md` files (`consistency-checks/`, `launcher-checks/`, `skills/release/`).
- [ ] 5.3 `git rm` `consistency-checks/tests/test_source_only_markers.py` (wherever task 3 moved it) **and** its mutant batch `consistency-checks/mutants/test_source_only_markers.py`. The guard calls `run_tests.py::_is_source_repo()` directly, so it cannot even import after 5.1 — relocating it was never an option. This is the change's one intended loss of coverage: **9 tests**.
- [ ] 5.4 `git rm` all 12 old `pyproject.toml` files. Net −11 (the new one is task 2.2).
- [ ] 5.5 Check `test_guards_have_mutant_batches.py` (~line 147, `batches = [`): if its guard→batch list is a hardcoded literal, remove the `test_source_only_markers` pair from it; if it derives the list from the tree, confirm it still passes with the pair gone. **Determine which by reading it — do not assume.**
- [ ] 5.6 Confirm the arithmetic: `git ls-files .claude/plugins/cla | wc -l` == **101**, `git ls-files plugin-tests | wc -l` == **54**, `git ls-files .claude/skills/release | wc -l` == **1**, and total deletions == **17**. If any differs, find the file before proceeding — a count that is off by one is a dropped or duplicated file, not a rounding error.

## 6. Re-point everything the move broke (separate commits from the moves)

- [ ] 6.1 Re-point every `Path(__file__).resolve().parents[N]` from task 1.6's list. The depths all change: a test that was `<scope>/tests/x.py` reaching the plugin root at `parents[2]` is now `plugin-tests/tests/<area>/x.py` and must reach **out of the dev tree and into the plugin**. Watch the derived roots especially — six files compute `_REPO_ROOT = _PLUGIN_ROOT.parents[2]`, so a wrong plugin root silently yields a wrong repo root.
- [ ] 6.2 **Resolution audit (required — this is the check the change exists to survive).** For every entry in task 1.6's list, state: old N, new N, and the directory it resolves to now, **verified by running it**, not by counting segments. A wrong `N` does not raise — it lands on a real directory and the test reports clean over fewer files or none. This is the same silent-vacuity shape `CLAUDE.md` records for the ledger-dir resolver ("the retro reports zero runs, which reads as a cold start"). For any test whose subject is a *scan*, assert a **non-zero count of files actually inspected**, not merely exit 0.
- [ ] 6.3 Re-point the two ancestor-walk resolvers separately. `test_no_project_tokens.py` and `test_project_facts_paths.py` (whatever change (a) left of them) walk `Path(__file__).resolve().parents` looking for a `cla` directory whose parent is `plugins`. That walk is deliberately position-independent and contains no `parents[N]`, so grep for `parents[` will not find it — and from `plugin-tests/` it finds **no such ancestor at all**. They need the plugin root resolved explicitly.
- [ ] 6.4 **`pythonpath` audit.** For each of the 9 entries in `plugin-tests/pyproject.toml`, confirm the directory exists and that at least one moved test actually imports a module from it. An entry pointing nowhere is silent (it just contributes nothing to `sys.path`) — which is exactly how `launcher-checks`'s dead `scripts` entry survived. Report the entry→importing-test mapping.
- [ ] 6.5 **Module-collision check across the union.** Nine directories now share one `sys.path`. Confirm no two of them export the same module basename: list `*.py` across all nine and check for duplicates. The test-basename measurement in task 2.3 does **not** answer this — it is about test files; this is about the scripts they import. If a collision exists, stop: the single scope is not viable as designed and the change needs re-shaping, not a workaround.
- [ ] 6.6 Update `check_script_drift.py`'s own `PLUGIN_ROOT` (a `parents[2]` walk) for its new home at `plugin-tests/scripts/`, plus every path in its two path lists that named a moved file.
- [ ] 6.7 Any test that referenced `run_tests.py` other than the deleted marker guard (e.g. `test_runner_stream_encoding.py`, `test_mutate.py`) — read each and decide: re-point at `mutate.py`'s new home, or delete if its subject was the deleted runner. State the decision per file with the reason. Do not silently leave a test importing a file that no longer exists.

## 7. Fix the dangling references the move creates in shipped files

- [ ] 7.1 **Prune `SOURCE_SCAN_ROOTS` in `skills/_shared/scripts/check_no_project_tokens.py` from 8 entries to 5** — keep `skills`, `agents`, `hooks`, `output-styles`, `lib`; drop `conformance-checks`, `consistency-checks`, `launcher-checks`. This is not cosmetic: the iteration is `if not root.is_dir(): continue`, so a vanished root is skipped **silently** and the shipped guard would scan 5 roots while claiming 8 and still exit 0. Update the accompanying comment in the same edit — the comment is what makes the list checkable.
- [ ] 7.2 Run the pruned guard against this repo and confirm it exits 0 **and reports a non-zero scanned-file count spanning more than one root**. A guard whose scan shrank silently is the exact defect 7.1 exists to prevent, so verify the count, not the exit code.
- [ ] 7.3 Grep the whole shipped tree for any remaining reference to a moved or deleted path: `grep -rnE "run_tests\.py|conformance-checks|consistency-checks|launcher-checks|mutate\.py|SOURCE-REPO-ONLY" .claude/plugins/cla`. Every surviving hit must be justified in one line or fixed. The plugin `README.md` is the one that reaches consumers.

## 8. Doc sync — scoped deliberately

- [ ] 8.1 Fix **only the literal test-invocation commands** this change makes wrong, in two files. In `CLAUDE.md`: the three `run_tests.py` invocations (~lines 60–62), the per-scope `pytest` examples (~lines 64–66), the `mutate.py` invocation (~line 135), the "Before opening a PR | `run_tests.py`, once" gate row (~line 160), and the `node --test` path (~line 171). In the plugin `README.md`: the `run_tests.py` line (~98) and the `pytest .../tests` examples (~104–105). These are commands a person or agent copies and runs; leaving them is not deferral, it is shipping instructions that fail on first use.
- [ ] 8.2 **Everything else defers to change (c) `guard-shipped-asset-boundary`, and must be left alone**: `CLAUDE.md`'s 12-scope enumeration and `pythonpath` list (~74–78), the `conformance-checks`/`consistency-checks` descriptions (~82–85), the source-repo-only paragraph (~87), "portable core that reaches consuming repos" (~92–93), the `run_tests.py` rationale (~97), the no-CI section (~180), the hand-watched-files list (~214), the skill-layout tree (~233–234), the script table rows (~255–258); the `README.md` layout tree (~72–80); all of `DEVELOPER-GUIDE.md` (9 hits), root `.gitattributes` (the comment at ~25 naming `test_pre_push_is_installed.py`), and `TODO.md` (7 hits). Stale but harmless. **State explicitly in the PR body which lines were deliberately left stale**, so (c) inherits a list rather than a search.
- [ ] 8.3 Update `cla.io/overlays/codify-learnings.md` (5 hits: the `run_tests.py` commands ~34–35, the gate prose ~44/47, and `conformance-checks/tests/test_guards_are_not_vacuous.py` ~77). This is a **live overlay the harness reads at runtime**, not archive material — a stale command here is executed, not just read.
- [ ] 8.4 Leave every historical record untouched: `cla.io/decisions/`, `cla.io/lessons-learned/`, `cla.io/feedback/`, `cla.io/checkpoints/`, `cla.io/retro/*.jsonl`. They record what was true when written.

## 9. Verify

- [ ] 9.1 Run the new gate: `pytest` from the repo root against `plugin-tests/`. Record `collected_after`.
- [ ] 9.2 **Assert the count gate: `collected_after == collected_before − 9`**, using task 1.3's and 1.4's recorded numbers. Show the arithmetic in the PR body. Any other delta is a dropped test and blocks the change — **a green suite is not the evidence here; the subtraction is.** If the delta is wrong, `git diff --stat --find-renames` against the base and reconcile file by file.
- [ ] 9.3 Run the Node suite at its new path and confirm **70** tests, unchanged.
- [ ] 9.4 Confirm `python3 .claude/plugins/cla/run_tests.py` now fails with "no such file", and that nothing in the repo still invokes it (task 7.3 plus `grep -rn "run_tests" --exclude-dir=.git .` reviewed against task 8.2's deliberate-stale list).
- [ ] 9.5 Re-read task 6.2's resolution audit and task 6.4's `pythonpath` mapping as a block before opening the PR. These two, plus 9.2, are the change's real gate — the suite cannot catch a walk that now resolves to a plausible wrong directory and scans nothing.
- [ ] 9.6 Confirm the scope boundary held: no test was edited for any reason other than re-pointing a path; no test was merged, split, or deleted except `test_source_only_markers.py` and its batch; `lib/log_run.py` is still in the plugin; `skills/project-review/scripts/mechanical-checks.mjs` is still in the plugin; and the two guards change (a) promoted are still in the plugin.
- [ ] 9.7 Note in the PR body that `guard-shipped-asset-boundary` (change c) depends on this one, and hand it: task 8.2's deliberately-stale line list, the `check_script_drift.py` vacuity finding if change (a)'s task 3.10 recorded one, and design.md's open question about `test_guards_have_mutant_batches.py`.
