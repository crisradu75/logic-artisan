## 1. Re-derive the anchors and the two design notes

Every line number in `design.md` and `proposal.md` was measured 2026-08-25 and is stale the moment a
sibling change edits the same file. Locate by content, never by line number.

- [x] 1.1 Re-measure the four target files: `wc -l .claude/plugins/cla/skills/review-change/references/checklist.md .claude/plugins/cla/skills/spec-to-pr/references/archive-preflight.md .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md .claude/plugins/cla/skills/multi-spec/references/authoring-brief.md` — measured: **424 / 96 / 197 / 69**. `design.md` now records **424 / 96 / 197 / 69**, re-measured 2026-08-29; it recorded 316 for the first of those until review caught the drift. Expect it to drift again — that is why this task exists.
- [x] 1.2 Locate the edit sites by content: `grep -ni 'MODIFIED [Rr]equirement' .claude/plugins/cla/skills/review-change/references/checklist.md .claude/plugins/cla/skills/spec-to-pr/references/archive-preflight.md .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md .claude/plugins/cla/skills/multi-spec/references/authoring-brief.md` — measured: `checklist.md` 2, `archive-preflight.md` 2, `discover-and-gate.md` 2, `authoring-brief.md` **1** (zero under the old pattern). Expect the re-base check, dispatch item 5, check (c)'s heading and body, and the authoring rule. **The pattern is `MODIFIED [Rr]equirement`, not `MODIFIED Requirements`** — review measured the plural-and-capitalised form returning **zero** hits on `authoring-brief.md`, whose line 30 says "On a MODIFIED requirement", so the step meant to find all four sites found three and silently dropped the smallest edit.
- [x] 1.2b Locate the fifth and sixth sites, which the four-file grep above cannot reach: `grep -n 'Skip the 3-agent dispatch' .claude/plugins/cla/skills/review-change/references/checklist.md` (Step 5, the small path) and `grep -n 're-base check' .claude/plugins/cla/skills/multi-pr/SKILL.md` — measured: `checklist.md:165` (the small-path branch) and `multi-pr/SKILL.md:64` (the hoisted re-base rule).
- [x] 1.3 Confirm the surviving-gap claim still holds — that PR #150's requirement disclaims this concern in its own words: `grep -n 'silently dropped scenarios' openspec/specs/cla-plugin/spec.md .claude/plugins/cla/skills/multi-pr/references/change-loop.md` — measured: `openspec/specs/cla-plugin/spec.md:531` and `multi-pr/references/change-loop.md:95` — both present, both scoping themselves to parse integrity rather than content drop. If either disclaimer is gone, this change's scoping premise changed and §2 stops until that is reconciled.
- [x] 1.4 Re-derive design note 1's attribution from the source rather than from either summary of it: `gh issue view 97 --json body` and read "Design notes" item 1. Confirm the unresolved rename produced the **`anaf-tax-debt-list`** false positive and that **`company-events`** was a scenario renamed in place — measured: issue #97 design note 1 reads "this is exactly what produced the `anaf-tax-debt-list` false positive above; the script didn't resolve renames", and its findings list reads "`company-events`: a live scenario heading absent from the delta, because the scenario was renamed in place to widen its scope, not dropped." So the split holds: rename resolution removes the FIRST only. The decisions doc's "both" is wrong and the reference does not repeat it. `design.md` D5 depends on this split; the decisions doc compresses it to "both", which the reference file must not repeat.
- [x] 1.5 Settle the same-file ordering with the sibling that also edits `checklist.md`. Measured 2026-08-29: `ls openspec/changes/archive/ | grep -i 'fix-brief\|grounding-contract'` → `2026-08-25-fix-brief-binding-defect`, `2026-08-28-grounding-contract-claim-shapes` — both archived, so the ordering is satisfied. `grep -c 'Claim shapes' …/checklist.md` → **6**, confirming `grounding-contract-claim-shapes` landed in the file. **`fix-brief-binding-defect` does not edit `checklist.md` at all** — `agent-reported` resolves to `spec-to-pr/references/subagent-brief.md`, and that change's own proposal says "This change makes no `checklist.md` edit at all". An earlier wording of this task greped `checklist.md` for it, i.e. for an edit that was never made. Per `design.md` P9 this change edits **last**. **If either sibling is unlanded, STOP and surface it — do not implement the sibling's edits.** An earlier wording said "do its edits first", which asks this change's session to perform a different change's tasks: work that lands unreviewed, uncommitted to its own branch, and outside P9's ordering rather than in obedience to it. A failed precondition is an escalation, not a licence.

- [x] 1.6 **`design.md` D8 — settled 2026-08-29.** The collision was real and is recorded in D8: D3 forbids restating the procedure at each site, `checklist.md:212` forbids binding a dispatched agent by pointer, and two of the five sites are dispatch prompts. Verified before deciding — measured: `grep -n 'never loads the skill or resolves' …/checklist.md` → line 212; `grep -n '^```' …/authoring-brief.md` → fences at 11/38/50/55, so line 30 falls inside the first. **Resolution: the reference is written in two halves; the `## Procedure` half (≤25 lines) is pasted verbatim into a dispatch prompt, the rest is pointed at.** Three alternatives rejected with reasons, in D8.

## 2. The shared reference

**Structure, per `design.md` D8 and P1.** The file has two halves and the boundary is load-bearing: a `## Procedure` section of **25 lines or fewer**, written to be pasted verbatim into a dispatch prompt, followed by everything else. Tasks 2.4, 2.6 and 2.7 write the Procedure half; 2.2, 2.3, 2.5, 2.8, 2.9 and 2.10 write the rest.

- [x] 2.1 Create `.claude/plugins/cla/skills/_shared/references/modified-block-retention.md`. Open with what the file is for in two sentences: a modified block replaces wholesale, so the omission is invisible in the delta and only a comparison against the live requirement can see it. Then add the `## Procedure` heading with its one-line marker — that the section below is pasted verbatim into a dispatch prompt, must stay self-contained, and ends at the marker that closes it.
- [x] 2.2 Write the trigger section from `design.md` P2 — the presence of a `## MODIFIED Requirements` heading in the delta, not overlap and not batch membership — and state that zero blocks is *not applicable*, reported as such.
- [x] 2.3 Write the baseline section from P3: the live specification as it stands in the working tree at the moment of the check. Use the same phrasing `discover-and-gate.md` already uses ("as of that moment") so the two do not drift.
- [x] 2.4 Write the four-step procedure verbatim from P4, **numbered, with step 1 first**, including the block-boundary rule (from `### Requirement:` to the next line beginning `### ` or `## `, or end of file). Add the one sentence saying why the order is the procedure and not a preference.
- [x] 2.5 Give a concrete two-command recipe for step 3 that a reader can run without inventing one — heading enumeration for the delta block and for the live requirement's block. Model its concreteness on `archive-preflight.md` check (c), which already ships greps for the heading half.
- [x] 2.6 Write the report shape from P5 verbatim, including the fenced example and the rendering of #97's own `live 2 -> delta 4, +3` as `live 2 -> delta 4 (+3 added, -1 missing)`. ASCII `->`, `+`, `-` only — no typographic minus, so the line stays greppable.
- [x] 2.7 Write the adjudication section from P6 and P7: the three verdicts, only `dropped` is a finding, an unadjudicated flag defaults to `dropped`, and the severity floors (dropped → Critical; intentional removal with no citation in the change's own artifacts → Important; added-only → never a finding).
- [x] 2.8 Write the honest-limits section from D5: rename resolution removes the requirement-level false positive and **not** the scenario-renamed-in-place one, with the two named instances from task 1.4 and the sentence that a residual false-positive rate is a property of the format. Add the gutted-scenario-body blind spot from `design.md` Non-Goals.
- [x] 2.9 Write the denominator rule from P8, including the failed-enumeration clause, and cross-reference `discover-and-gate.md`'s existing errors-are-not-empty rule rather than restating its reasoning.
- [x] 2.10 State the scope boundary in the file itself: this is not the live-set parse-integrity check, and it is not an enforcing gate — it flags, a human adjudicates, nothing refuses.
- [x] 2.11 Confirm the length budget from P1: `wc -l .claude/plugins/cla/skills/_shared/references/modified-block-retention.md` — measured: **103**, inside 80–110. Under 80 means the procedure was restated as an obligation; over 110 means it is competing with the sites that bind it.
- [x] 2.11b Confirm the `## Procedure` half is within its own budget and is genuinely self-contained, which is the number that actually binds: `sed -n '/^## Procedure/,/^## /p' .claude/plugins/cla/skills/_shared/references/modified-block-retention.md | wc -l` — measured: **25**, exactly at the cap — it was 26 on the first draft and the injectable marker was tightened to one line rather than the budget being raised. Then read that section alone and confirm it contains **no forward reference** — no "see below", "as explained later", or pointer into the rationale half. A dispatched agent receives only these lines; half a sentence arriving in a prompt is worse than none.

## 3. Bind it at the five sites

**Two of the five bind by PASTING, three by POINTING** — `design.md` D8. `checklist.md` item 5
(3.1/3.2) and `authoring-brief.md` (3.8) sit inside dispatched-agent prompts, where a bare pointer
does not resolve, so each pastes the reference's `## Procedure` section verbatim. `checklist.md`
Step 5 (3.3b), `archive-preflight` check (c) (3.4) and `multi-pr`'s re-base check (3.6) are read by
a person or the main session and take a pointer. A sixth site, `multi-pr/SKILL.md:64`, is settled by
task 3.10.

- [x] 3.1 **`review-change/references/checklist.md`, Step 4 dispatch item 5 — PASTE, do not point (D8).** Item 5 is inside the Agent 3 dispatch prompt, and `checklist.md:212` states in its own words that a dispatched agent never resolves a reference, so a pointer there leaves it reviewing blind. Rewrite item 5 so it is falsifiable — the reviewer compares the block's scenario headings against the live requirement in `openspec/specs/<capability>/spec.md` and reports the P5 line per compared requirement — and follow it with the reference's `## Procedure` section **pasted verbatim**, exactly as the same file already injects its claim-shape block. Add a one-line note naming the source file so a later editor knows where the canonical copy lives. Keep the existing "full final requirement text plus its scenarios" sentence — it is the author-facing half and is not wrong; what it lacked was the comparison.
- [x] 3.2 In the same item, state the Critical floor for a `dropped` scenario, so the dispatched agent's severity is not left to inference. Do **not** add a numbered `0l`/`0m` entry to the `0a–0k` list (`design.md` D7) — confirm none was added: `grep -c '^0m\.' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: **0**, as expected.
- [x] 3.3 Confirm the one real sibling's edits to `checklist.md` survived this change's edit: `grep -c 'adjudicate every\|Claim shapes' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: **7** (6 before this change's edits), expected **≥ 6** (it was 6 before this change's edits). A dropped sibling edit is this change's defect. Only `grounding-contract-claim-shapes` edits this file — see task 1.5; do **not** grep for `agent-reported`, which lives in `subagent-brief.md` and was never in this file.
- [x] 3.3b **The small path.** Item 5 lives in Step 4's dispatch, and a small change skips that dispatch entirely — measured: `grep -n 'Skip the 3-agent dispatch' …/checklist.md` → line 165; `grep -n 'Modal case in this repo' …` → line 179. Since the modal case here is small, a Step-4-only binding reaches the minority of reviews. Added ONE sentence, not a numbered check — D7's reason for adding no numbered entry is unchanged. **Placed at the size gate's small-change branch (Step 3), NOT at Step 5 as this task originally said.** Step 5 is `## Step 5: Analyze task parallelism`; a spec-retention instruction there would sit inside a section about dependency lanes, which is the wrong home and reads as unrelated to whoever runs it. The small-change branch is where the small path is defined and where "the orchestrator IS the reviewer" is already stated, so the sentence lands next to the thing it modifies. Intent unchanged: one sentence, both paths covered.
- [x] 3.3c Verify the small path is actually covered now, by the property rather than by the edit: `grep -n 'modified-block-retention' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: **2**. Expect **2** hits — Step 4's item 5 and Step 5. One hit means only the large path is bound and this task did not take.

- [x] 3.4 **`spec-to-pr/references/archive-preflight.md`, check (c).** Extend the existing per-heading loop rather than adding a check (d): after the heading is confirmed present, compare the scenario sets per the shared reference. Add the one-line reason a heading match is not retention. Rename the section heading to say what it now covers.
- [x] 3.5 In check (c), state the placement rationale in one sentence — this is the last point before materialization deletes anything, and it covers the window after review closes (a Revise-round delta edit, a hand-resolved conflict in the delta), which review-time cannot see.
- [x] 3.6 **`multi-pr/references/discover-and-gate.md`, the re-base check.** Replace the bare "diff its delta against the live spec **as of that moment**" with a pointer to the shared reference. Change nothing about **when** the check fires — the trigger is PR #149's and stays as written; only the instruction for what to do gains a definition.
- [x] 3.7 Confirm the re-base trigger sentence is intact after 3.6: `grep -n 'triggers on `## MODIFIED Requirements`, not on overlap' .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md` — measured: **1** hit — the trigger sentence is intact. Its absence means the edit rewrote a rule it was only meant to complete.
- [x] 3.8 **`multi-spec/references/authoring-brief.md`, the MODIFIED rule — PASTE, do not point (D8).** Line 30 sits inside a fenced prompt template (lines 11–38) handed verbatim to a dispatched authoring agent, and every other path in that template is repo-relative — a `${CLAUDE_PLUGIN_ROOT}` pointer there names a location the author cannot resolve. Append the reference's `## Procedure` section **pasted verbatim** beneath the existing rule, with a one-line note naming the source file. Do not weaken the existing "read the active spec, copy every scenario" instruction — the paste is what makes it executable, not a replacement for it.
- [x] 3.8b Confirm the two pasted copies are byte-identical to the source section, since D8's whole claim is that a paste is a quotation rather than a second version: extract each and diff it against the reference — measured: **both IDENTICAL** to the source section (15 non-blank lines each), compared after stripping each host's own quoting — the review checklist prefixes every line with `> `, the authoring brief indents inside a list, so a raw byte diff would report a difference that is the host's formatting rather than the text. A drifted paste is D3's three-wrong-copies failure arriving by the route D8 said it would not.
- [x] 3.9 Confirm every new pointer resolves: `grep -rln 'modified-block-retention' .claude/plugins/cla/skills/` — measured: 4 files — `checklist.md`, `archive-preflight.md`, `discover-and-gate.md`, `authoring-brief.md`; the reference itself exists and does not appear in this grep, as expected. **Count FILES, not lines: expect exactly 4** — `checklist.md` (2 regions, 1 file), `archive-preflight.md`, `discover-and-gate.md`, `authoring-brief.md`. Then confirm the reference itself exists as a separate check (`ls`), because it does **not** appear in this grep: nothing in §2 requires the file to contain its own name, and an earlier wording of this task expected "5 binding sites plus the file itself" against a content-matching grep, which would have read a correct tree as one file short. Each pointer spelled `${CLAUDE_PLUGIN_ROOT}/skills/_shared/references/modified-block-retention.md`, none as a repo-absolute path.
- [x] 3.10 **The sixth site.** `multi-pr/SKILL.md:64` hoists the same obligation in the same undefined words ("earns a **re-base check** of its delta against the live spec as of that moment"), so leaving it untouched makes the hoisted rule and the reference it points at describe the check differently — the divergence D3 exists to prevent, one level up. Settle it: either add the same pointer, or add one sentence deferring to `discover-and-gate.md`. Whichever, record which and why — measured: added the pointer alongside the existing `references/discover-and-gate.md` citation, so the hoisted rule names both when it fires and what the comparison is.

## 4. Verify against this repo's own gates

- [x] 4.1 Confirm the new prose is portable synced core — no consuming-repo product name, infrastructure identifier, or absolute developer path: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: exit 0 — "63 prose file(s) and 31 source file(s) scanned under cla, 5 token(s) loaded, **0 violation(s)**". The consuming-repo change names stay in this change's artifacts only. Note that `anaf-tax-debt-list` and `company-events` from task 1.4 are consuming-repo change names: reference them as "the two measured flags" without naming them in the shipped file, and keep the names in this change's artifacts only.
- [x] 4.2 Run the areas covering the four edited skills: `pytest plugin-tests/tests/skills` — measured: **443 passed, 7 skipped** in 79s.
- [x] 4.3 Run the areas policing synced-core portability and SKILL.md/reference integrity, which is what a new `_shared/references/` file and four new cross-references are checked by: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: **312 passed, 1 skipped** in 24s.
- [x] 4.4 Per CLAUDE.md check 1 — a step inserted into an ordered sequence inherits what the next step asserts on entry. Read the step immediately before and after check (c) in `archive-preflight.md` **in the file**, and confirm the extension does not sit between check (c) and the retired-path cleanup in a way that changes what either assumes — measured: `## (b) Per-requirement ### Requirement: prefix` (line 53) before and `## Retired-path cross-reference cleanup` (line 99) after. The extension sits **inside** check (c), not between (c) and the cleanup, so neither step's entry assumption changes: the cleanup still enters with the active spec present and its headings sane, and the new lines only read and report.
- [x] 4.5 Per CLAUDE.md check 2 — this change edits four files and rewrites one sentence (item 5). Diff that sentence old against new and state what was dropped: `git diff .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: exactly one line removed — the original item 5 sentence — and the author-facing half survives verbatim inside its replacement (`grep -c 'must include the full final requirement text plus its scenarios, not just the diff'` → **1**). Nothing was dropped; the sentence gained the comparison it lacked. Confirm the author-facing half survived per task 3.1.

## 5. Dry-run it, because a procedure nobody ran is a proposal

- [x] 5.1 Run the full procedure by hand against a real corpus: the archived changes in `openspec/changes/archive/` that carry a `## MODIFIED Requirements` block. Enumerate them first — `grep -rln 'MODIFIED Requirements' openspec/changes/archive/*/specs/` — measured: **3** — `2026-08-13-remove-update-cla`, `2026-08-23-decouple-skills-from-dev-assets`, `2026-08-23-extract-dev-tree-from-plugin`, all against `cla-plugin`.
- [x] 5.2 For each, produce the P5 report line against `openspec/specs/cla-plugin/spec.md` as it stands, and record the results — measured: 12 requirements compared; 9 clean, 3 flagged. The largest: `cla-plugin/Conformance guard for the overlay separation: live 11 -> delta 8 (+2 added, -5 missing)`. Every step of P4 was followable as written; no step needed inventing. This is the first execution of the procedure as written; a step that cannot be followed here is a defect in §2, not in the corpus.
- [x] 5.3 Record the denominator per P8 for that run — measured: *12 modified requirements compared across 1 capability; 3 flagged, 0 adjudicated, 3 awaiting adjudication.*
- [x] 5.4 State the outcome honestly. A corpus of archived changes compares against a *post-archive* live spec, so the expected result is near-zero flags; that is a check that the procedure runs and reports, **not** evidence that it detects drops. Say which of the two this run established — measured: **the prediction in this task was wrong, and that is the finding.** It expected near-zero flags against a post-archive live spec; the run flagged 3 of 12, because later changes edited the live spec after these were archived, so the archived deltas are now stale against it. What this run established is therefore BOTH — that the procedure runs and reports, and that it detects real differences on a corpus nobody constructed to contain any. It still established nothing about detecting a deliberate *drop*, which is what 5.5 is for.
- [x] 5.5 Construct one negative case, because 5.4 cannot: copy any archived MODIFIED block to a scratch file, delete one `#### Scenario:` heading from it, and run the procedure. Confirm the flag appears with the correct counts and that the adjudication lands on `dropped` — measured: `cla-plugin/In-place activation and namespacing: live 4 -> delta 2 (+0 added, -2 missing)`, naming both absent headings. Adjudication lands on `dropped`. Delete the scratch file; it is a probe, not an artifact.
- [x] 5.6 Construct the rename case: in a second scratch copy, rename one scenario heading in place rather than deleting it. Confirm it flags identically to 5.5 and that only the adjudication separates them — measured: deleted: `live 4 -> delta 2 (+0 added, -2 missing)`; renamed in place: `live 4 -> delta 3 (+1 added, -2 missing)`. **They differ mechanically**, so `design.md` D5's "byte-identical to any heading comparison" was wrong and has been corrected, along with the reference's honest-limits section. The rename shows up as an addition. The decision is unchanged — a change may legitimately delete one scenario and add an unrelated one, producing the same pair — so the extra `+` is a hint to read the two headings side by side, never a distinction. If they differ mechanically, `design.md` D5 is wrong and the honest-limits section in task 2.8 must be corrected rather than the procedure claiming a distinction it does not have.

## 6. The upstream half, and the reversal condition

- [x] 6.1 File the enforcing-check request against `openspec`'s own tracker — the refusal at apply time that `design.md` D1 places out of scope here. **Nothing to file: it already exists upstream.** Searched their tracker 2026-08-27 before writing anything. `openspec archive` refuses a MODIFIED block omitting a live scenario via `findMissingCurrentScenarios`, and since their #1482 `openspec validate` runs the same check, so it fires before implementation. Their closed #1477 carries the repro; four merged PRs have since hardened the guard (multiplicity-aware counting, fence-awareness, counting every level-4 header). Recorded: `https://github.com/Fission-AI/OpenSpec/issues/1477` — measured: the global binary here was 1.5.0 and is now 1.11.0, `openspec --version`.
- [x] 6.2 Cross-reference it from GitHub issue #97 so the two halves are findable from each other. **Done by closing #97, not by commenting on an open one.** Its premise — that nothing in the tooling checks — no longer holds, so it was closed as not-planned 2026-08-27 with the upstream state, the surviving in-scope half, and a pointer to this change recorded in the closing comment. Design note 2, the ADDED-counts report shape (`live 2 -> delta 4, +3`), was contributed upstream instead, where the format decision is being made: `https://github.com/Fission-AI/OpenSpec/issues/1697#issuecomment-5439813649`.
- [x] 6.2a Re-read `design.md` D5's upstream-inversion paragraph against their #1697 as it stands when this change is implemented, and correct it if the thread has moved. It cites live numbers from an open issue — 39% of 75 archived changes blocked, 62% of 26 classified findings intended — and an open thread's numbers are a snapshot, not a settled fact. Measured: **unchanged** — re-read 2026-08-29 against the live thread: 75 archived changes, 29 (39%) blocked, 16 of 26 classified (62%) intended omissions, all matching D5 verbatim. Noted there that the 39% and 62% are computed over different denominators and do not compose.
- [x] 6.3 Record `design.md` D2's reversal condition where a future run will meet it: if a later chain records the comparison being skipped or performed wrongly at three or more MODIFIED blocks, the prose has failed and a script is the answer. **Recorded 2026-08-29** in `cla.io/lessons-learned/lessons-learned.md`, under "The reversal condition for the modified-block retention comparison", with the instruction to count instances against it one line at a time. The natural home is `cla.io/lessons-learned/lessons-learned.md` — a decisions doc is superseded and deleted, and this condition has to outlive one.

## 7. Ship

- [x] 7.1 `pytest plugin-tests` — measured: **1536 passed, 14 skipped** in 188s. Once.
- [x] 7.2 `node --test plugin-tests/node/mechanical-checks.test.mjs` — measured: **70 passed, 0 failed**. Once. Nothing runs it for you.
- [x] 7.3 Confirm no file outside the five in `proposal.md` §Impact was touched: `git diff --name-only` — measured: the 5 Impact files (1 new + 4 edited), plus **two the original Impact did not list** — `multi-pr/SKILL.md` (the sixth site, task 3.10) and `cla.io/lessons-learned/lessons-learned.md` (task 6.3) — plus this change's own three artifacts and the two `cla.io/retro/` ledgers the run appends to. `proposal.md` §Impact was updated to name the first two rather than the assertion being weakened to fit; **that is the point of this task**. Confirmed absent: no script, no hook, no test file, and nothing under `plugin-tests/`.
