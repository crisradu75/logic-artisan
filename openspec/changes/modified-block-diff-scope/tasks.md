## 1. Re-derive the anchors and the two design notes

Every line number in `design.md` and `proposal.md` was measured 2026-08-25 and is stale the moment a
sibling change edits the same file. Locate by content, never by line number.

- [ ] 1.1 Re-measure the four target files: `wc -l .claude/plugins/cla/skills/review-change/references/checklist.md .claude/plugins/cla/skills/spec-to-pr/references/archive-preflight.md .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md .claude/plugins/cla/skills/multi-spec/references/authoring-brief.md` — measured: `<paste the four counts>`. `design.md` recorded 316 / 96 / 197 / 69.
- [ ] 1.2 Locate the four edit sites by content: `grep -n 'MODIFIED Requirements' .claude/plugins/cla/skills/review-change/references/checklist.md .claude/plugins/cla/skills/spec-to-pr/references/archive-preflight.md .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md .claude/plugins/cla/skills/multi-spec/references/authoring-brief.md` — measured: `<paste the hits>`. Expect the re-base check, dispatch item 5, check (c)'s heading and body, and the authoring rule.
- [ ] 1.3 Confirm the surviving-gap claim still holds — that PR #150's requirement disclaims this concern in its own words: `grep -n 'silently dropped scenarios' openspec/specs/cla-plugin/spec.md .claude/plugins/cla/skills/multi-pr/references/change-loop.md` — measured: `<paste the hits>`. If either disclaimer is gone, this change's scoping premise changed and §2 stops until that is reconciled.
- [ ] 1.4 Re-derive design note 1's attribution from the source rather than from either summary of it: `gh issue view 97 --json body` and read "Design notes" item 1. Confirm the unresolved rename produced the **`anaf-tax-debt-list`** false positive and that **`company-events`** was a scenario renamed in place — measured: `<paste the two sentences>`. `design.md` D5 depends on this split; the decisions doc compresses it to "both", which the reference file must not repeat.
- [ ] 1.5 Settle the same-file ordering with the two siblings that also edit `checklist.md`. Check whether their edits have landed: `grep -n 'agent-reported\|Claim shapes\|^0l\.' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hits, or "none">`. Per `design.md` P9 this change edits **last**; if either sibling is unlanded, do its edits first.

## 2. The shared reference

- [ ] 2.1 Create `.claude/plugins/cla/skills/_shared/references/modified-block-retention.md`. Open with what the file is for in two sentences: a modified block replaces wholesale, so the omission is invisible in the delta and only a comparison against the live requirement can see it.
- [ ] 2.2 Write the trigger section from `design.md` P2 — the presence of a `## MODIFIED Requirements` heading in the delta, not overlap and not batch membership — and state that zero blocks is *not applicable*, reported as such.
- [ ] 2.3 Write the baseline section from P3: the live specification as it stands in the working tree at the moment of the check. Use the same phrasing `discover-and-gate.md` already uses ("as of that moment") so the two do not drift.
- [ ] 2.4 Write the four-step procedure verbatim from P4, **numbered, with step 1 first**, including the block-boundary rule (from `### Requirement:` to the next line beginning `### ` or `## `, or end of file). Add the one sentence saying why the order is the procedure and not a preference.
- [ ] 2.5 Give a concrete two-command recipe for step 3 that a reader can run without inventing one — heading enumeration for the delta block and for the live requirement's block. Model its concreteness on `archive-preflight.md` check (c), which already ships greps for the heading half.
- [ ] 2.6 Write the report shape from P5 verbatim, including the fenced example and the rendering of #97's own `live 2 -> delta 4, +3` as `live 2 -> delta 4 (+3 added, -1 missing)`. ASCII `->`, `+`, `-` only — no typographic minus, so the line stays greppable.
- [ ] 2.7 Write the adjudication section from P6 and P7: the three verdicts, only `dropped` is a finding, an unadjudicated flag defaults to `dropped`, and the severity floors (dropped → Critical; intentional removal with no citation in the change's own artifacts → Important; added-only → never a finding).
- [ ] 2.8 Write the honest-limits section from D5: rename resolution removes the requirement-level false positive and **not** the scenario-renamed-in-place one, with the two named instances from task 1.4 and the sentence that a residual false-positive rate is a property of the format. Add the gutted-scenario-body blind spot from `design.md` Non-Goals.
- [ ] 2.9 Write the denominator rule from P8, including the failed-enumeration clause, and cross-reference `discover-and-gate.md`'s existing errors-are-not-empty rule rather than restating its reasoning.
- [ ] 2.10 State the scope boundary in the file itself: this is not the live-set parse-integrity check, and it is not an enforcing gate — it flags, a human adjudicates, nothing refuses.
- [ ] 2.11 Confirm the length budget from P1: `wc -l .claude/plugins/cla/skills/_shared/references/modified-block-retention.md` — measured: `<paste the count>`, must be 80–110. Under 80 means the procedure was restated as an obligation; over 110 means it is competing with the sites that bind it.

## 3. Bind it at the three sites

- [ ] 3.1 **`review-change/references/checklist.md`, Step 4 dispatch item 5.** Rewrite it so it is falsifiable: the reviewer compares the block's scenario headings against the live requirement in `openspec/specs/<capability>/spec.md` per `${CLAUDE_PLUGIN_ROOT}/skills/_shared/references/modified-block-retention.md`, and reports the P5 line per compared requirement. Keep the existing "full final requirement text plus its scenarios" sentence — it is the author-facing half and is not wrong; what it lacked was the comparison.
- [ ] 3.2 In the same item, state the Critical floor for a `dropped` scenario, so the dispatched agent's severity is not left to inference. Do **not** add a numbered `0l`/`0m` entry to the `0a–0k` list (`design.md` D7) — confirm none was added: `grep -c '^0m\.' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste>`, expected 0.
- [ ] 3.3 Confirm both siblings' edits to `checklist.md` survived this change's edit: `grep -n 'agent-reported\|adjudicate every\|Claim shapes' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hits>`. A dropped sibling edit is this change's defect; all three land in one batch and this one edits last.
- [ ] 3.3b **The small path — `checklist.md` Step 5, which BOTH paths reach.** Item 5 lives in Step 4's dispatch, and a small change skips that dispatch entirely: `grep -n 'Skip the 3-agent dispatch' .claude/plugins/cla/skills/review-change/references/checklist.md` and `grep -n 'Modal case in this repo' …` — measured: `<paste both>`. Since the modal case here is small, a Step-4-only binding reaches the minority of reviews. Add ONE sentence at Step 5 requiring the same comparison when the change carries a `## MODIFIED Requirements` block, pointing at the shared reference. **One sentence, not a numbered check** — a numbered entry would compete for `0l` with the sibling `grounding-contract-claim-shapes`, and D7's reason for adding none is unchanged.
- [ ] 3.3c Verify the small path is actually covered now, by the property rather than by the edit: `grep -n 'modified-block-retention' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste>`. Expect **2** hits — Step 4's item 5 and Step 5. One hit means only the large path is bound and this task did not take.

- [ ] 3.4 **`spec-to-pr/references/archive-preflight.md`, check (c).** Extend the existing per-heading loop rather than adding a check (d): after the heading is confirmed present, compare the scenario sets per the shared reference. Add the one-line reason a heading match is not retention. Rename the section heading to say what it now covers.
- [ ] 3.5 In check (c), state the placement rationale in one sentence — this is the last point before materialization deletes anything, and it covers the window after review closes (a Revise-round delta edit, a hand-resolved conflict in the delta), which review-time cannot see.
- [ ] 3.6 **`multi-pr/references/discover-and-gate.md`, the re-base check.** Replace the bare "diff its delta against the live spec **as of that moment**" with a pointer to the shared reference. Change nothing about **when** the check fires — the trigger is PR #149's and stays as written; only the instruction for what to do gains a definition.
- [ ] 3.7 Confirm the re-base trigger sentence is intact after 3.6: `grep -n 'triggers on `## MODIFIED Requirements`, not on overlap' .claude/plugins/cla/skills/multi-pr/references/discover-and-gate.md` — measured: `<paste the hit>`. Its absence means the edit rewrote a rule it was only meant to complete.
- [ ] 3.8 **`multi-spec/references/authoring-brief.md`, the MODIFIED rule.** Append a pointer to the shared reference so the author-side rule and the check-side procedure cite one definition. Pointer only — do not restate the procedure, and do not weaken the existing "read the active spec, copy every scenario" instruction.
- [ ] 3.9 Confirm every new pointer resolves: `grep -rn 'modified-block-retention' .claude/plugins/cla/skills/` — measured: `<paste the hits>`, expected **5** binding sites plus the file itself — `checklist.md` Step 4 item 5, `checklist.md` Step 5 (task 3.3b), `archive-preflight.md` check (c), `discover-and-gate.md`, `authoring-brief.md` — each spelled `${CLAUDE_PLUGIN_ROOT}/skills/_shared/references/modified-block-retention.md` and none as a repo-absolute path.

## 4. Verify against this repo's own gates

- [ ] 4.1 Confirm the new prose is portable synced core — no consuming-repo product name, infrastructure identifier, or absolute developer path: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `<paste the exit status and summary line>`. Note that `anaf-tax-debt-list` and `company-events` from task 1.4 are consuming-repo change names: reference them as "the two measured flags" without naming them in the shipped file, and keep the names in this change's artifacts only.
- [ ] 4.2 Run the areas covering the four edited skills: `pytest plugin-tests/tests/skills` — measured: `<paste the summary line>`.
- [ ] 4.3 Run the areas policing synced-core portability and SKILL.md/reference integrity, which is what a new `_shared/references/` file and four new cross-references are checked by: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `<paste the summary line>`.
- [ ] 4.4 Per CLAUDE.md check 1 — a step inserted into an ordered sequence inherits what the next step asserts on entry. Read the step immediately before and after check (c) in `archive-preflight.md` **in the file**, and confirm the extension does not sit between check (c) and the retired-path cleanup in a way that changes what either assumes — measured: `<paste the two surrounding headings>`.
- [ ] 4.5 Per CLAUDE.md check 2 — this change edits four files and rewrites one sentence (item 5). Diff that sentence old against new and state what was dropped: `git diff .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `<paste the hunk>`. Confirm the author-facing half survived per task 3.1.

## 5. Dry-run it, because a procedure nobody ran is a proposal

- [ ] 5.1 Run the full procedure by hand against a real corpus: the archived changes in `openspec/changes/archive/` that carry a `## MODIFIED Requirements` block. Enumerate them first — `grep -rln 'MODIFIED Requirements' openspec/changes/archive/*/specs/` — measured: `<paste the list and its count>`.
- [ ] 5.2 For each, produce the P5 report line against `openspec/specs/cla-plugin/spec.md` as it stands, and record the results — measured: `<paste one line per compared requirement>`. This is the first execution of the procedure as written; a step that cannot be followed here is a defect in §2, not in the corpus.
- [ ] 5.3 Record the denominator per P8 for that run — measured: `<paste the R/C/F/A/D line>`.
- [ ] 5.4 State the outcome honestly. A corpus of archived changes compares against a *post-archive* live spec, so the expected result is near-zero flags; that is a check that the procedure runs and reports, **not** evidence that it detects drops. Say which of the two this run established — measured: `<paste the one-sentence conclusion>`.
- [ ] 5.5 Construct one negative case, because 5.4 cannot: copy any archived MODIFIED block to a scratch file, delete one `#### Scenario:` heading from it, and run the procedure. Confirm the flag appears with the correct counts and that the adjudication lands on `dropped` — measured: `<paste the report line>`. Delete the scratch file; it is a probe, not an artifact.
- [ ] 5.6 Construct the rename case: in a second scratch copy, rename one scenario heading in place rather than deleting it. Confirm it flags identically to 5.5 and that only the adjudication separates them — measured: `<paste both report lines side by side>`. If they differ mechanically, `design.md` D5 is wrong and the honest-limits section in task 2.8 must be corrected rather than the procedure claiming a distinction it does not have.

## 6. The upstream half, and the reversal condition

- [x] 6.1 File the enforcing-check request against `openspec`'s own tracker — the refusal at apply time that `design.md` D1 places out of scope here. **Nothing to file: it already exists upstream.** Searched their tracker 2026-08-27 before writing anything. `openspec archive` refuses a MODIFIED block omitting a live scenario via `findMissingCurrentScenarios`, and since their #1482 `openspec validate` runs the same check, so it fires before implementation. Their closed #1477 carries the repro; four merged PRs have since hardened the guard (multiplicity-aware counting, fence-awareness, counting every level-4 header). Recorded: `https://github.com/Fission-AI/OpenSpec/issues/1477` — measured: the global binary here was 1.5.0 and is now 1.11.0, `openspec --version`.
- [x] 6.2 Cross-reference it from GitHub issue #97 so the two halves are findable from each other. **Done by closing #97, not by commenting on an open one.** Its premise — that nothing in the tooling checks — no longer holds, so it was closed as not-planned 2026-08-27 with the upstream state, the surviving in-scope half, and a pointer to this change recorded in the closing comment. Design note 2, the ADDED-counts report shape (`live 2 -> delta 4, +3`), was contributed upstream instead, where the format decision is being made: `https://github.com/Fission-AI/OpenSpec/issues/1697#issuecomment-5439813649`.
- [ ] 6.2a Re-read `design.md` D5's upstream-inversion paragraph against their #1697 as it stands when this change is implemented, and correct it if the thread has moved. It cites live numbers from an open issue — 39% of 75 archived changes blocked, 62% of 26 classified findings intended — and an open thread's numbers are a snapshot, not a settled fact. Measured: `<paste the figures as they read on the day, or "unchanged">`.
- [ ] 6.3 Record `design.md` D2's reversal condition where a future run will meet it: if a later chain records the comparison being skipped or performed wrongly at three or more MODIFIED blocks, the prose has failed and a script is the answer. The natural home is `cla.io/lessons-learned/lessons-learned.md` — a decisions doc is superseded and deleted, and this condition has to outlive one.

## 7. Ship

- [ ] 7.1 `pytest plugin-tests` — measured: `<paste the summary line>`. Once.
- [ ] 7.2 `node --test plugin-tests/node/mechanical-checks.test.mjs` — measured: `<paste the summary line>`. Once. Nothing runs it for you.
- [ ] 7.3 Confirm no file outside the five in `proposal.md` §Impact was touched: `git diff --name-only` — measured: `<paste the list>`. In particular no script, no hook, no test file, and nothing under `plugin-tests/`.
