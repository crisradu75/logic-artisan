## 1. Re-derive the anchors before editing

- [x] 1.1 Re-run the anchor measurements and record the values inline, because a line number measured at proposal time is stale the moment another change lands: `wc -l .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md .claude/plugins/cla/skills/spec-to-pr/SKILL.md .claude/plugins/cla/skills/spec-to-pr/references/revise.md` — measured: `115 subagent-brief.md, 431 SKILL.md, 135 revise.md, 83 handoff.md, 67 review-gate.md`
- [x] 1.2 Locate every edit site by content, not by the line numbers in `design.md`: `grep -n '^### 2\. Task\|^### 5\. Done when' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md`, `grep -n 'Round N (N ≥ 2)\|No capitulation on discharge\|Fix-delegate default' .claude/plugins/cla/skills/spec-to-pr/references/revise.md` — measured: `subagent-brief.md :37 (### 2. Task), :78 (### 5. Done when); revise.md :68 (Round N (N >= 2)), :97 (Fix-delegate default), :101 (No capitulation on discharge)`
- [x] 1.3 Re-run the terminal-contract site census and confirm it still returns five hits in the five files the proposal's Impact table names: ``grep -rn 'explicit `done`\|`done`/`blocked`' .claude/plugins/cla/skills/`` — measured: `5 hits — subagent-brief.md:83, SKILL.md:239, SKILL.md:337, revise.md:97, multi-spec/references/authoring-brief.md:46. No sixth site.`. **Two of the five are out of scope by classification, not oversight** — `SKILL.md:239` (Implement's coding delegate) and `multi-spec/references/authoring-brief.md:46` (authors proposals). Neither is edited. If a sixth site now exists, classify it as fix-dispatch or not and add it to task 3.4 before proceeding.
- [x] 1.4 Re-run the blast-radius check and confirm both citing sites still name the brief by slot list: `grep -rn 'subagent-brief' .claude/plugins/cla/` — measured: `2 hits BEFORE this change's edits — spec-to-pr/SKILL.md:234, lite-pr/SKILL.md:141, both by slot list. No third pre-existing site.` **After this change the same command returns 4**, because tasks 3.1/3.3 add two more citing sites of their own: `revise.md:109` and `SKILL.md:340`, both citing §2/§5 by section rather than by slot list. Recorded so a later re-run reads 4 and knows why. The blast-radius conclusion is unaffected — no slot was renamed, renumbered or added (task 2.6), so every citing site stays correct, old and new. If a third citing site now exists, add it to task 2.6 before proceeding.
- [x] 1.5 Run the SECOND census — the orchestrator-applied-fix sites, which the terminal-contract grep cannot see because they say nothing about `done`/`blocked`: `grep -rn 'finding via direct\|finding as a direct\|deferred Critical/Important findings' .claude/plugins/cla/skills/` — measured: `3 hits — spec-to-pr/SKILL.md:215, multi-spec/references/review-gate.md:55, multi-lite/references/candidate-loop.md:22,23. No fourth site.`. **The pattern is an alternation because the three sites word the same action three different ways; a single literal finds one of them.** Expect three: `spec-to-pr/SKILL.md:215` (Review's fix loop), `multi-spec/references/review-gate.md:55` (in scope — has a change dir with `design.md`, no delegate), `multi-lite/references/candidate-loop.md:22,23` (out of scope — enforces `lite-pr` candidates, which have no change directory). If a fourth exists, classify it by whether it operates on a change directory and add it to group 5 before proceeding.
- [x] 1.6 Locate the return-handling and reporting sites the new status must reach: `grep -n 'Exit gate\|Applied\|Deferred-Known-Issue' .claude/plugins/cla/skills/spec-to-pr/references/revise.md` and `grep -n 'Deferred' .claude/plugins/cla/skills/spec-to-pr/references/handoff.md` — measured: `revise.md :82 Applied, :83 Deferred-Known-Issue, :134 Exit gate; handoff.md :13 Deferred Known Issues, :24 Deferred to TODO.md. 'On a `blocked`' in revise.md returns NO hits — the fix-delegate had no return handler at all.`. Confirm `revise.md` has NO return-handling prose for its fix-delegate today: `grep -n 'blocked. return\|On a `blocked`' .claude/plugins/cla/skills/spec-to-pr/references/revise.md` returns nothing — measured: `no hits — confirmed absent`.

## 2. The brief's authority contract (`subagent-brief.md`)

- [x] 2.1 In §2 ("Task — one sentence, one deliverable"), add the fix-brief form: keep the existing one-sentence form as the unmarked default for a build-this dispatch, and add the three named fields for a dispatch whose purpose is to remedy a defect — `**Defect (binding).**`, `**Facts this defect rests on (checkable).**` with the row shape `- <claim> — source: <path:line | command> — [agent-reported | orchestrator-verified]`, and `**Candidate remedy (rejectable).**`. Use the field names verbatim from `design.md` "Pinned implementation parameters".
- [x] 2.2 In §2, state the three authority levels in one short table or list — binding / rejectable / checkable, with who may overturn each and how — so the contract is legible from the slot itself rather than only from the terminal contract.
- [x] 2.3 In §2, state the three fact-row outcomes: all rows hold → proceed; a row is wrong and the defect does not survive its correction → return the remedy rejected without implementing; a row is wrong but the defect survives → correct it, proceed, and report the correction. State that re-resolving the rows is the delegate's **first** action, before any edit — `design.md` Decision 3 and the delta spec's "its first action is to re-resolve each row" both require the ordering, not just the outcomes.
- [x] 2.3a In §2, state the resolving-quote-or-NOT-FOUND rule **inline** rather than citing `review-change/references/checklist.md` for it. A dispatched coding delegate reads the brief and never loads that checklist, so a cross-skill citation reaches nobody at the moment it binds.
- [x] 2.3b In §2, state the provenance tag's scope in one line: the tag is a field on a brief's fact row and nothing else — it does not travel into a review report's claim table or `### Verified claims`. `design.md` pins this; without it in the shipped prose a later reader widens it by assumption.
- [x] 2.4 In §5 ("Done when"), add the fix-brief terminal-contract block verbatim from `design.md` "Pinned implementation parameters" — the `done` / `blocked` / `remedy-rejected` statuses, the defect-check evidence requirement, the explicit "evidence the remedy was applied is NOT evidence the defect is gone" line, and the never-omitted `Fact corrections:` field. Also land the **required return fields in their pinned order** (`status`, defect-check output, test-run summary line, ticked-task count, `Fact corrections:`) — it is a separate pinned parameter from the quoted block and is not carried by quoting the block alone.
- [x] 2.4a In §5, add the set-level return shape: a dispatch briefed over more than one finding returns one overall status PLUS one outcome row per finding, so fourteen-applied-and-one-rejected is sayable; a single-finding dispatch returns a one-row list under the same contract.
- [x] 2.4b In §5, retrigger the EXISTING default block so the two blocks do not both claim the same dispatches. It currently reads as "the default for anything that edits code", and a fix dispatch also edits code — restate it as the form for a **non-fix** dispatch. Do not delete it; it remains the build-this default.
- [x] 2.5 In §5, state that a brief unable to name a defect check is a brief whose defect is not grounded, rather than a dispatch exempt from the contract.
- [x] 2.6 Confirm no slot was renamed, renumbered or added: `grep -n '^### [0-9]\.' .claude/plugins/cla/skills/spec-to-pr/references/subagent-brief.md` returns the same five headings as task 1.2 recorded — measured: `5 headings — :31 Scope, :37 Task, :91 Do not touch, :114 Report, :124 Done when. Same five, none renamed or renumbered.`.
- [x] 2.7 Confirm the added prose is portable synced core — no repo token, no dev-tree path, no absolute developer path: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `exit 0 — 61 prose file(s) and 31 source file(s) scanned under cla, 5 token(s) loaded, 0 violation(s)`.

## 3. The sites that restate the terminal contract (`spec-to-pr`)

A rule added only to the brief leaves every FIX-dispatch site briefing against the old two-status
contract. Task 1.3's census is the authority for which sites these are — **two of its five hits are
deliberately not edited**, and confirming they were not edited is part of the group.

- [x] 3.1 In `references/revise.md`, at the fix-delegate default (`:97`), state that a fix-set delegated here IS a fix dispatch, so its brief takes the fix-brief form and `remedy-rejected` is one of its three valid returns.
- [x] 3.2 In `references/revise.md`, **write the fix-delegate's return-handling branch, which does not exist today.** Task 1.6 confirms `revise.md` has no prose at all for what the orchestrator does when its fix-delegate returns. Add it: on `done` without the per-finding outcome list, treat as not done and recount inline; on `blocked`, resolve the blocker or apply the remaining fixes inline, recording the blocker as a Handoff issue; on `remedy-rejected`, read the per-finding list and route each rejected finding per group 4. This is the branch the change's own Decision 5 argument demands, at the site that actually receives the return.
- [x] 3.3 In `SKILL.md`'s Revise stub, update the fix-delegate invariant line to name the three-status contract and the per-finding outcome list, since the stub must stay self-sufficient when `revise.md` is not reloaded.
- [x] 3.4 Confirm no fix-dispatch site still states the two-status contract alone, and that **both** non-fix sites are untouched: re-run task 1.3's grep — measured: `census now: authoring-brief.md:46 (out of scope, unchanged), revise.md:109 (three-status), subagent-brief.md:129 (non-fix block) + :143 (fix block), SKILL.md:242 (Implement, unchanged). git diff --stat on authoring-brief.md is EMPTY; git diff -U0 on SKILL.md shows hunks at 217 and 340 only, none in Implement (239-247).`. `multi-spec/references/authoring-brief.md` and `SKILL.md:239`/`:243` must all be unchanged. **`git diff --stat .claude/plugins/cla/skills/multi-spec/references/authoring-brief.md` must be empty**, and `SKILL.md`'s Implement phase must show no diff hunk — measured: `git diff --stat on authoring-brief.md is EMPTY; git diff -U0 on SKILL.md shows @@ -216,0 +217,3 @@ (Review) and @@ -337 +340,4 @@ (Revise stub) only — no hunk in Implement (239-247)`.
- [x] 3.5 Record why Implement is excluded, in one line, at the point a future reader would ask: Implement's delegate implements enumerated tasks under a rule requiring full enumeration because it "will NOT invent an omitted task", so it has no discretion to reject a plan and cannot carry a contract whose third status is a reasoned rejection. Put this in `design.md`'s Decision 1 area or the proposal's site table — wherever task 1.3's classification is stated — not in the shipped prose, which should not carry the reasoning for an edit it does not receive.

## 4. `remedy-rejected` gets a receiving branch (`revise.md`, `SKILL.md`)

- [x] 4.1 In `references/revise.md`'s triage step (`:82`–`:85`), add the third outcome beside Applied and Deferred-Known-Issue: a delegate return of `remedy-rejected` is a **successful** return that discharges the round's attempt at that finding without discharging the finding — the orchestrator re-decides the remedy and the finding stays open.
- [x] 4.2 **Split the exit gate's counter in two** (`revise.md:134`). It currently reads "count *un-triaged* Critical and Important findings … If 0 untriaged → status `ok`, exit loop", and Applied and Deferred-Known-Issue are both triaged AND closed, so one counter served for both predicates. A rejected finding is the first that is triaged and **open**. Rewrite the gate to count *untriaged* (in no outcome) and *open* (in an outcome, not closed) separately, require **both** zero to exit `ok`, and route non-zero-open-with-budget back into the loop. Without this, a round whose only live finding was rejected exits clean with a Critical outstanding, and Handoff's all-✓ gating then prints `gh pr merge`.
- [x] 4.2a Confirm the gate defect is actually gone rather than merely edited: re-read the rewritten gate and trace the state *one rejected Critical, zero untriaged, budget remaining* through it — it must re-enter the loop, not exit — measured: `gate text: untriaged=0, open=1 (Remedy-Rejected citing the remedy is OPEN), budget remaining -> 'both are 0' is FALSE so no exit; 'open > 0 with budget remaining -> re-enter the loop' fires. The pre-edit gate exited ok on this same state.`.
- [x] 4.3 Add the **disproved-defect** branch, in `revise.md` beside the triage: a rejection whose stated reason is that a fact row was wrong and the defect does not survive its correction **closes** the finding, recorded as closed-by-disproof with the corrected row as evidence — it is not re-attempted and is not recorded as fixed. A rejection citing the remedy leaves the finding open per 4.1. State that the orchestrator picks the branch by reading the rejection's reason.
- [x] 4.4 In `references/handoff.md`, add the third deferred bucket to the terminal report: a rejected-but-open finding, distinct from *Deferred Known Issues* (`:13`, conscious deferrals) and *Deferred to TODO.md* (`:24`, Suggestions plus cap-exhausted residue). Follow the report's existing named-subsection convention rather than pooling it into either. Confirm the section prints "(none)" when empty, like its siblings — measured: `new section at handoff.md:24 'Rejected remedies, still open' — prints (none) when empty, matching its sibling at :12 (Issues encountered) and :14 (Deferred Known Issues).`.
- [x] 4.5 State explicitly, in both `SKILL.md` and `revise.md`, that a rejection does not consume an extra round and raises no cap. Confirm no cap moved: `grep -n 'pr-rounds\|review-rounds' .claude/plugins/cla/skills/spec-to-pr/SKILL.md .claude/plugins/cla/skills/spec-to-pr/references/revise.md` shows the same defaults as before the edit — measured: `SKILL.md:193 --review-rounds default 1; SKILL.md:332 and revise.md:5 --pr-rounds default 2. Unchanged.`.
- [x] 4.6 In `SKILL.md`'s Revise stub, add the one-line invariants for the third outcome, the two-counter exit gate, and the disproved-defect close — the stub carries the load-bearing rules for when `revise.md` is not reloaded, and an exit gate that silently releases a live Critical is load-bearing.

## 5. Orchestrator-specified remedies (`spec-to-pr`)

- [x] 5.1 In `SKILL.md`'s Review phase, in the step that applies each Critical and Important finding by direct Edit/Write and then re-validates that the fix landed, add the orchestrator-specified-remedy check: where the orchestrator decided the remedy itself, the re-validation additionally reads the change's own `design.md` rejected-alternatives content and confirms the applied remedy does not reintroduce one.
- [x] 5.2 State, in the same step, that the check reads the rejected-alternatives content **as captured at the start of the fix round**, before any of that round's edits — because a document edited by the remedy cannot adjudicate the remedy. **Do NOT write `git show HEAD:<path>`.** The phase order is Precheck → Propose → Review → Implement → Test → Ship, so at Review the change directory is not yet committed and the git form errors with path-does-not-exist on the common case. Confirm the ordering before writing the rule: `grep -n '^Order:' .claude/plugins/cla/skills/spec-to-pr/SKILL.md` — measured: `SKILL.md:179 — Order: Precheck -> Propose -> Review -> Implement -> Test -> Ship -> Revise -> Archive -> Handoff. Review precedes Ship, so the change dir is untracked at Review.`.
- [x] 5.3 State that a skill applying orchestrator fixes with no OpenSpec change directory has no rejected-alternatives document to read, so neither the check nor the marking obligation binds it, and its lacking the document is not a breach. Do not name a skill that does not exist in a consuming repo; state the condition, not the roster.
- [x] 5.4 **State the response when the check fires**, not only the check. A hit — the applied remedy reintroduces something the design rejected — is a **Critical finding against that remedy**: the remedy is withdrawn or re-specified and does not stand on having resolved the original finding; where the rejection itself is now judged wrong, `design.md` is amended explicitly and "the fix brief said so" is not an amendment. The delta spec requires this consequence and no earlier task landed it. Write it in both `SKILL.md`'s Review step and `revise.md`.
- [x] 5.5 Put the marker where a structure exists to hold it: on the orchestrator's own triage record for the finding — the record already carrying Applied / Deferred-Known-Issue — **not** on a "finding row", which is not a structure Revise has (round-1 findings arrive as a schema'd `{severity, file, line, summary}` object; confirm with `grep -n 'severity' .claude/plugins/cla/skills/spec-to-pr/references/revise.md` — measured: `revise.md:47 — // FINDINGS_SCHEMA: object {findings: [{severity:"Critical"|"Important"|"Suggestion", file, line, summary}]} — a schema'd object, no finding-row table.`). Require per-remedy marking on the **Revise** path only, where delegated and orchestrator-applied remedies mix in one round; on the Review path there is no delegate at all, so every remedy is orchestrator-specified and a per-remedy mark discriminates nothing — state that fact once for the phase instead. The check itself runs on both paths.
- [x] 5.6 In `references/revise.md`, extend the INT-CAP no-capitulation step (`:101`) so an orchestrator-specified remedy's re-read includes the same rejected-alternatives check, with the same round-start-snapshot rule and the same marker.
- [x] 5.7 In `references/revise.md`, in the round N ≥ 2 scoping section (`:68`), require the dispatch to name which hunks in the previous fix commit carry the marker and to check each against the same rejected-alternatives content.
- [x] 5.8 **Extend the rule to `multi-spec`'s review gate**, the second orchestrator-applied artifact-fix site — task 1.5's census finds it at `references/review-gate.md:55` ("Apply every Critical/Important finding as a direct `Edit`/`Write` fix to the relevant change's artifacts"), operating per change with a `design.md` present and no delegate. It is inside this rule's stated scope; add the same check, the same snapshot rule, and the same firing response there. Do not add it to `multi-lite/references/candidate-loop.md` — that loop enforces `lite-pr` candidates, which have no change directory, and is exempt by 5.3's condition.
- [x] 5.9 State explicitly, in `SKILL.md`, `revise.md` and `review-gate.md`, that this control is weaker than an independent reader and raises no round cap.
- [x] 5.10 Confirm the added prose is portable synced core across all edited files: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `exit 0 — 61 prose file(s) and 31 source file(s) scanned under cla, 5 token(s) loaded, 0 violation(s)`.

## 6. Verify

- [x] 6.1 Run the area suite for the skills touched: `pytest plugin-tests/tests/skills/spec-to-pr` — measured: `54 passed in 22.45s`. **Record that this is a weak gate for this change, not a strong one:** that area holds only `test__git_common.py` and `test_probe_state.py`, neither of which reads the prose this change edits, so it cannot fail on any edit here. It is run to prove nothing regressed, not to prove anything landed. The real gates are 6.2 and 6.6.
- [x] 6.2 Run the conformance and consistency areas, which police synced-core portability and SKILL.md reference integrity — these are the gates that can actually fail on this change: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `281 passed, 1 skipped in 29.33s`.
- [x] 6.3 Confirm every reference this change adds resolves: re-run task 1.2's greps against the edited files and confirm each cross-reference (`design.md` rejected alternatives, the brief's §2/§5) is named by content that exists — measured: `re-ran 1.2's greps against the edited files: subagent-brief.md fix-brief form at :46 and fix terminal contract at :141; revise.md fix-delegate at :97 and return handler at :109. Every cross-reference resolves.`.
- [x] 6.4 Author a fix brief in the new form against a real historical defect, and confirm every required field has content that is not `n/a`. Source the defect from a real `fix:` commit rather than a stored brief — no brief text is archived anywhere in this repo, so one must be constructed. **Use `git log --all --grep 'fix: review round' --oneline`** — measured 2026-08-25, the anchored form (`'^fix: review round'`) returns 1 commit from HEAD and 2 across all refs, while the unanchored form across all refs returns 5, which is the corpus worth drawing from. Pick one commit, and where that commit fixes several findings in one go, **pick one triaged finding from it** — the brief's `Defect` field is singular. Read its diff and the PR body it belongs to for the defect it fixed, and fill slot 2's three fields and slot 5's five return fields from that. **If a field can only be filled with `n/a`, the contract is wrong and this task fails rather than the field being waived.** — measured: commit `02f5db9` (`fix: review round 1`), finding 1 of its three. **Slot 2 —** `Defect (binding).` `plugin-tests/mutate.py` aborts an entire batch when one mutant's anchor fails its preflight, so a batch that mutated NOTHING reports the same way as one that ran, and the commit carried a mutation-evidence trailer for a run that never happened. `Facts this defect rests on (checkable).` - mutant 4's anchor spans a line ending — source: the batch file at that commit — [orchestrator-verified]; - `mutate.py`'s preflight rejects a bare 
 on a CRLF checkout — source: `mutate.py` preflight — [orchestrator-verified]; - the preflight aborts the whole batch rather than skipping the one mutant — source: `mutate.py` — [orchestrator-verified]. `Candidate remedy (rejectable).` Anchor the mutant within a single line, so the preflight has no line ending to reject. **Slot 5 —** status; defect-check output (the batch's own run line, showing a non-zero mutant count); test-run summary line; ticked-task count; `Fact corrections:`. **Every field has real content; none is `n/a`.**.
- [x] 6.5 Confirm the dry-read's defect check is a real command: the brief drafted in 6.4 must name a check that exhibits the defect and is runnable as written. If the historical defect has no such check, say so — that is a finding about the contract's reach, not a task failure — and record which commit it was — measured: the check is `python3 plugin-tests/mutate.py plugin-tests/mutants/consistency/test_measurement_names_its_command.py`, runnable as written. It exhibits the defect directly: before the fix it reported the batch aborted having mutated nothing; the commit's own trailer records `9 of 9 mutants killed, and the batch now runs at all` afterwards. So this historical defect DOES have a real defect check, and the contract reaches it..
- [x] 6.6 Confirm every branch the new status can reach is written, by walking the status from dispatch to report: the brief defines it (§5), the fix-delegate's return handler routes it (`revise.md`, task 3.2), the triage has a bucket (4.1), the disproved-defect case closes (4.3), the exit gate counts it open and re-loops (4.2/4.2a), and the terminal report prints it (4.4). Name the file and line for each of the six — measured: `(1) subagent-brief.md:143 defines it; (2) revise.md:115 routes the return; (3) revise.md:86 triage bucket; (4) revise.md:93 disproved-defect close; (5) revise.md:160 two-count exit gate; (6) handoff.md:24 report bucket + :59 merge gating. Six of six — no branch missing.`. A status reaching a point with no branch is this change's own stated failure mode, so this walk is the check that it did not repeat it.
- [x] 6.7 Before the PR: `pytest plugin-tests` and `node --test plugin-tests/node/mechanical-checks.test.mjs`, each once — measured: `pytest plugin-tests: 1307 passed, 8 skipped in 172.43s. node --test: tests 70, pass 70, fail 0.`.
