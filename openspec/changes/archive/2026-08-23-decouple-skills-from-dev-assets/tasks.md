## 1. Promote `check_fact_paths.py` (the simpler guard first)

- [x] 1.1 Before writing anything, enumerate the originals by grepping — not from memory: `grep -nE '^def ' .claude/plugins/cla/conformance-checks/tests/test_project_facts_paths.py`. Record the full list of `def test_*` functions; it is the checklist task 1.6 audits against.
- [x] 1.2 Create `.claude/plugins/cla/skills/sync-context/scripts/` (the skill is `SKILL.md`-only today, so the directory does not exist).
- [x] 1.3 Write `.claude/plugins/cla/skills/sync-context/scripts/check_fact_paths.py`, carrying every non-test module-level constant and function across **byte-identical, with the same names and signatures** — `_repo_root_from_here`, `_strip_wrapping`, `_strip_line_col`, `_strip_emphasis`, `_clean_candidate`, `_looks_like_placeholder_or_glob`, `_clean_git_env`, `_tracked_top_level_names`, `_top_level_names`, `_keep_if_path`, `extract_path_candidates`, `_iter_scanned_files`, `scan`, `find_stale_paths`, and every regex/constant they read. These are accumulated edge-case heuristics; do not "improve" them while moving them.
- [x] 1.4 Add `main()` carrying the assertion from `test_no_stale_paths_in_project_facts_or_overlays`, plus `if __name__ == "__main__": sys.exit(main())`. Implement the pinned exit contract from design.md: **0** clean (printing what was scanned and the file count), **1** stale paths found (every one named, one per line, never stopping at the first), **2** could-not-run (bad args, or a present-but-unreadable input) to stderr. An absent `cla.io/project-facts.md` exits **0** with a stated reason.
- [x] 1.5 Add the `--repo-root <path>` optional argument (default: existing self-resolution) so the CLI layer is testable against `tmp_path`. Confirm `_repo_root_from_here` is carried across unchanged — it is already fully position-independent (git, then a `.claude` walk that refuses the global `~/.claude`, then cwd) and needs no depth fix.
- [x] 1.6 **Rewrite audit (required — this is the "rewrote a file rather than edited it" check).** Diff old against new and produce a table classifying **every** `def` from task 1.1 as: carried into `main()` / kept as a unit test / deliberately dropped with a reason. State the result explicitly, including "nothing was dropped" if that is the finding. An empty dropped-column is a claim, so name the command that produced the enumeration.
- [x] 1.7 Re-point `conformance-checks/tests/test_project_facts_paths.py` at the new module via `importlib.util.spec_from_file_location` (per design.md — not a `pythonpath` entry in `conformance-checks/pyproject.toml`). The file keeps only its `test_*` functions; its implementation now lives in the promoted script. Keep `test_the_repo_root_is_the_process_repo_not_the_users_home` as a test.
- [x] 1.8 **Add CLI-layer tests for `main()`** in `conformance-checks/tests/test_project_facts_paths.py`, driving the script with `--repo-root <tmp_path>`: exit **0** on a clean seeded repo (asserting the printed scanned-file count is non-zero), exit **0** with its stated reason when `cla.io/project-facts.md` is absent, exit **1** on a seeded stale path (asserting the output **names that path**), exit **1** reporting **all** stale paths when two are seeded, and exit **2** on a bad argument. Without these, task 1.9's mutants have nothing that can kill them and the batch reports survivors that are really coverage holes.
- [x] 1.9 **Write the mutant batch at `.claude/plugins/cla/conformance-checks/mutants/test_project_facts_paths.py`** — the pairing `consistency-checks/tests/test_guards_have_mutant_batches.py` enforces (batch basename equals the guard-test basename, one directory up under `mutants/`; it also requires a `MUTANTS` list and refuses an absolute path, so resolve from `Path(__file__)` like `conformance-checks/mutants/test_skill_lint.py` does). Root `CLAUDE.md`'s rule — "Adding a new script, skill, or hook → the four checks above, in full" — makes this mandatory, and `check_fact_paths.py` is a new script. **Mutate the two branches where a wrong choice yields a silent clean report:** (i) the **exit-code selection** — return `0` where the code returns `1` on a non-empty violation list, and return `1` where it returns `2` on the could-not-run path; (ii) the **violation-accumulation path** — turn the accumulate-then-report loop into an early `return`/`break` after the first stale path, and make the "absent facts file" trivial-pass branch swallow a *present* file too. Mutate what the promotion **touches**, not only `main()`: also break one carried-over heuristic (e.g. the `:line[:col]` strip) and confirm a unit test fails.
- [x] 1.10 Run the batch: `python3 .claude/plugins/cla/mutate.py .claude/plugins/cla/conformance-checks/mutants/test_project_facts_paths.py`. Record survivors — a survivor is a finding, not a formality. Per root `CLAUDE.md`, a clean run is evidence about the mutants you thought of and nothing else, so record it as one input to the ship decision rather than as the decision.
- [x] 1.11 With the batch landed, check whether `test_project_facts_paths.py`'s `"grandfathered"` line can leave `_EXEMPT` in `consistency-checks/tests/test_guards_have_mutant_batches.py` (that block's own comment: *"Delete a line here the moment its batch lands."*). **Determine by reading and running, not by assuming** — remove the line, run `pytest .claude/plugins/cla/consistency-checks/tests -q`, and confirm both `test_every_guard_file_has_a_mutant_batch_beside_its_scope` and `test_the_grandfather_list_only_shrinks` pass. `TODO.md`'s "12 grandfathered guards" section goes stale as a result; leave it to `extract-dev-tree-from-plugin` per task 5.3 and hand it forward in 6.5.
- [x] 1.12 Run `pytest .claude/plugins/cla/conformance-checks/tests -q`. Then run the promoted script directly against this repo and confirm it exits 0 **and prints a non-zero scanned-file count** — a zero-file scan reporting "0 stale paths" is the vacuous-pass bug, not a pass.

## 2. Promote `check_no_project_tokens.py` (four repo-level checks, not one)

- [x] 2.1 Enumerate the original the same way: `grep -nE '^def ' .claude/plugins/cla/conformance-checks/tests/test_no_project_tokens.py`. Confirm against design.md D2 that exactly four functions assert against the **real repo**: `test_no_project_tokens_in_synced_core`, `test_no_project_tokens_in_synced_source`, `test_no_absolute_developer_paths_in_synced_source`, `test_every_scanned_file_is_actually_readable`.
- [x] 2.2 Write `.claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py`, carrying every non-test constant and function across byte-identical — including both scanners and their distinct constant sets (`SOURCE_SCAN_ROOTS`, `CACHE_DIRS`, `EXCLUDED_SUBTREES`, `ABS_PATH_EXEMPT_MARKER`, `PLACEHOLDER_USERS`, `MANGLED_WIN_PATH`, `WIN_ABS_PATH`, `HOME_ABS_PATH`, `PLACEHOLDER_PATH_HINTS`, `MAX_EXCERPT`, `TOKEN_LIST_RELPATH`) and every function they read.
- [x] 2.3 **Fix `_plugin_root()`'s positional fallback (design.md D3).** From the new home `skills/_shared/scripts/`, `parents[2]` is `skills/`, not the plugin root — the correct depth is `parents[3]`. Update the depth **and** the stale `# Fallback ... tests/ -> conformance-checks/ -> cla/` comment together; the comment is what makes the depth checkable. The primary walk still succeeds normally, so this defect is silent and surfaces only in the unexpected-layout case the fallback exists for.
- [x] 2.4 Add `main()` running **all four** checks from task 2.1, accumulating violations across them and **not** short-circuiting on the first that fails. Same pinned exit contract: 0 clean (with a scanned-file count), 1 violations (each named with repo-relative path, matched token, line number and excerpt), 2 could-not-run. Absent token list → exit 0 with a stated reason; **present-but-empty token list → exit 1** with the message that deleting the file is how to intentionally disable it.
- [x] 2.5 Do not drop check (d), `test_every_scanned_file_is_actually_readable`. It reads like plumbing and is the one check keeping the other three from passing vacuously — an unreadable file is silently "clean" to every scanner. Add `--repo-root` as in 1.5.
- [x] 2.6 **Rewrite audit**, same form as 1.6: table of every `def`, classified carried / kept-as-test / dropped-with-reason, with the enumerating command named. Pay particular attention to the four repo-level checks — this is the specific defect this task exists to catch.
- [x] 2.7 Re-point `conformance-checks/tests/test_no_project_tokens.py` at the new module via `importlib.util.spec_from_file_location`, keeping every unit test — including the non-vacuity ones (`test_the_scanner_is_not_vacuous`, `test_source_scan_of_the_real_plugin_is_non_vacuous`, `test_the_readability_check_can_actually_fail`).
- [x] 2.8 **Re-point `consistency-checks/tests/test_token_list_is_curated_here.py` (design.md D7) — the promotion's one consumer outside `conformance-checks/`, and the one that breaks silently.** It hardcodes `_GUARD = _PLUGIN_ROOT / "conformance-checks" / "tests" / "test_no_project_tokens.py"` (line 25), puts `_GUARD.parent` on `sys.path`, and does `import test_no_project_tokens as guard` (line 40). Point `_GUARD` at `skills/_shared/scripts/check_no_project_tokens.py` and load it the same way task 2.7 does, by `importlib.util.spec_from_file_location`. **Three symbols must survive the promotion as module-level names on the promoted script** — not folded into `main()`, not renamed: `_repo_root()`, `TOKEN_LIST_RELPATH`, and `load_tokens`. Verify each by name in the new file before running the test. Its `_load_guard` docstring states why it asks the guard instead of recomputing — *"a version of this check that computed its own repo root passed happily while the guard's anchor was off by one level and the guard skipped"* — so leaving it pointed at a module whose implementation has moved breaks the guard that guards the token list.
- [x] 2.9 Run `pytest .claude/plugins/cla/consistency-checks/tests -q` and confirm `test_the_conformance_guard_is_not_silently_disabled_in_this_repo` passes **against the promoted module**. Then prove it can still fail: temporarily rename `cla.io/project-tokens.local.md`, confirm the test goes red, restore it. A test that passes because it imported something and asserted nothing meaningful is exactly what its own docstring warns about.
- [x] 2.10 **Add CLI-layer tests for `main()`** in `conformance-checks/tests/test_no_project_tokens.py`, driving the script with `--repo-root <tmp_path>`: exit **0** on a clean seeded tree (asserting a non-zero scanned-file count), exit **0** with its stated reason when the token list is absent, exit **1** when the token list is present but yields no tokens (asserting the message names deleting the file as the way to disable it), exit **1** with violations from **more than one** of the four checks reported in a single run, and exit **2** on a bad argument. These are what task 2.11's mutants get killed by.
- [x] 2.11 **Write the mutant batch at `.claude/plugins/cla/conformance-checks/mutants/test_no_project_tokens.py`** — same pairing rule and same `Path(__file__)`-relative resolution as task 1.9. **Mutate the two silent-clean-report branches:** (i) the **exit-code selection** — return `0` on a non-empty accumulated violation list, and return `1` instead of `2` on the could-not-run path; (ii) the **violation-accumulation path** — make `main()` short-circuit after the first of the four checks that reports, and make it skip check (d), `test_every_scanned_file_is_actually_readable`, entirely. That last mutant is the one this batch most exists for: design.md D2 names check (d) as the one most likely to be dropped as "not really a check", and its absence is invisible to every other assertion. Also mutate one carried-over heuristic in each of the two scanners (prose and absolute-path) so the batch covers what the promotion **touches**, not only `main()`.
- [x] 2.12 Run the batch: `python3 .claude/plugins/cla/mutate.py .claude/plugins/cla/conformance-checks/mutants/test_no_project_tokens.py`. Record survivors with the same reading as 1.10.
- [x] 2.13 With the batch landed, check whether `test_no_project_tokens.py`'s `"grandfathered"` line can leave `_EXEMPT` — same procedure and same caveats as task 1.11. `TODO.md` names this one as the highest-value of the twelve; that section's reconciliation still belongs to `extract-dev-tree-from-plugin`.
- [x] 2.14 Run `pytest .claude/plugins/cla/conformance-checks/tests -q`. Then run the promoted script directly against this repo: confirm exit 0 with a non-zero scanned-file count, and confirm it reports on files from **more than one** scan root (a scan reaching only `skills/` would pass while covering a fraction of what it claims).

## 3. Rename the two duplicated `aggregate.py` pairs

Eleven subtasks over ~10 files and three concerns, so they are grouped below for review. **The
numbering is deliberately left as 3.1–3.11 rather than split into three sections**, because
`extract-dev-tree-from-plugin` cites "change (a)'s tasks 3.7 and 6.1" and "task 3.10" by number;
renumbering would silently falsify those references. Review the three groups independently; commit
them separately if that reads better.

**Why this rename at all:** not a collision that bites today — design.md D0 records the
counterexamples (both test files already load their aggregator under distinct module names via
`spec_from_file_location`, and `run_tests.py` runs every scope as its own subprocess). It is a
forward dependency on `extract-dev-tree-from-plugin`, whose single consolidated pytest scope breaks
on the duplicate `test_aggregate.py` basename. Do not restate the old "one process cannot import
both" rationale in any file this task touches.

**Group A — rename plus its immediate consumers (3.1–3.4).**

- [x] 3.1 Rename to the names pinned in design.md: `codify-retro/scripts/aggregate.py` → `codify_aggregate.py`; `spec-to-pr-retro/scripts/aggregate.py` → `spec_to_pr_aggregate.py`; `codify-retro/tests/test_aggregate.py` → `test_codify_aggregate.py`; `spec-to-pr-retro/tests/test_aggregate.py` → `test_spec_to_pr_aggregate.py`. Use `git mv` so the rename is visible as a rename.
- [x] 3.2 Update the `SCRIPT = Path(__file__).resolve().parents[1] / "scripts" / "aggregate.py"` line in each renamed test file, and the two module docstrings that open `"""Tests for aggregate.py …"""`.
- [x] 3.3 Update the `_import_default_log_path` comments in both test files (`codify-retro/tests/test_aggregate.py:259–260`, `spec-to-pr-retro/tests/test_aggregate.py:533–534`) — each says the load is by explicit path under a unique name "so codify-retro's and spec-to-pr-retro's identically-named `aggregate.py` can't collide", which is no longer the situation. Reword to state what the load actually buys now (loading a script that is not on `sys.path` as a module), **not** that it averts a collision. Keep the explicit-path load itself — the reason it is still correct is that these are `scripts/` files reached by path, not that a collision looms.
- [x] 3.4 Update the two skill invocation lines: `skills/codify-retro/SKILL.md:29` and `skills/spec-to-pr-retro/SKILL.md:27`.
**Group B — `consistency-checks` and the docstring/comment updates (3.5–3.8).**

- [x] 3.5 Update `consistency-checks/scripts/check_script_drift.py` — **two** path lists (lines ~52–53 and ~60–61), the check `name` string `"retro aggregate.py record loading"`, and the module docstring's "retro skills' `aggregate.py`" mention.
- [x] 3.6 Update `consistency-checks/tests/test_ledger_names_agree.py` — two `_PLUGIN_ROOT / "skills" / … / "scripts" / "aggregate.py"` constructions plus its docstring — and `consistency-checks/tests/test_check_script_drift.py`'s expected-paths fixture.
- [x] 3.7 Update `run_tests.py`'s module docstring, which justifies running each scope as its own process by naming the collision: *"multiple scopes ship a top-level `scripts/aggregate.py` and `scripts/log_run.py`"* (lines ~7–9). **Both halves of that sentence need checking, not just the half this task renames.** `find .claude/plugins/cla -name log_run.py -not -path '*__pycache__*'` returns exactly one file today (`lib/log_run.py`, whose scope declares `pythonpath = ["."]`), so the `scripts/log_run.py` clause appears to be already stale — re-run that command yourself and write the docstring to whatever it actually returns. Do not restructure the runner; `extract-dev-tree-from-plugin` deletes it. If the `log_run.py` clause is indeed stale, that is a finding for 6.5's handoff: the successor's D1 measures duplicate *test* basenames and explicitly flags the two-`scripts/`-dirs-on-one-`pythonpath` question as unanswered by that measurement.
- [x] 3.8 Update `lib/log_run.py:86`'s "the consumers (the retro skills' `aggregate.py`)" comment and the identical line inside each renamed aggregator (~line 113 / ~134).
**Group C — sweep and verify (3.9–3.11).**

- [x] 3.9 Sweep for survivors: `grep -rn "aggregate\.py\|test_aggregate\|import aggregate" .claude/plugins/cla` must return no hit naming the bare old name as a path. Check `_shared/references/retro-skeleton.md` and `run-log-schema.md` separately — their "the calling skill's `aggregate.py`" mentions are **role**-shaped, not path-shaped; confirm that reading rather than assuming it, and change only what is path-shaped.
- [x] 3.10 **Run `check_script_drift.py` and confirm it reports on the renamed files**, not merely that it exits 0 (design.md D5 + Open Questions). Its subject is a divergence that is silent by construction — `CLAUDE.md` records "the retro reports zero runs, which reads as a cold start" — so a drift checker whose path list points at nothing would pass vacuously. If it does pass vacuously on a missing path, record that as a finding for `extract-dev-tree-from-plugin`.
- [x] 3.11 Run the three affected scopes: `pytest .claude/plugins/cla/skills/codify-retro/tests`, `.../skills/spec-to-pr-retro/tests`, `.../consistency-checks/tests`.

## 4. Rewrite the mutation gate as prose

- [x] 4.1 Copy the current step 2b blocks verbatim to scratch before editing: `skills/lite-pr/SKILL.md:132` and `skills/spec-to-pr/references/revise.md:97`. They are the baseline for the 4.4 diff.
- [x] 4.2 Rewrite `lite-pr/SKILL.md` step 2b: remove `python3 ${CLAUDE_PLUGIN_ROOT}/mutate.py <batch.py>` and only the words that exist to operate it, replacing them with the manual procedure — break what the fix touches, run the affected test, confirm it fails, restore. Keep the heading "**Mutation gate (required before the commit in step 3).**"
- [x] 4.3 Rewrite `spec-to-pr/references/revise.md` step 2b identically — but keep its **Handoff report** wording where `lite-pr` says **final report**. The two blocks differ there deliberately; do not normalise them.
- [x] 4.4 **Verbatim-preservation diff (required).** Compare old 2b against new 2b line by line in both files and confirm every sentence except the invocation survives: the step-3 ordering; "a change like any other and earns the same evidence the original code needed"; "'the reviewer's finding is now handled' is not that evidence"; "what the fix touches, not only what it targets"; the default-path/overlay-path no-op precedent; "fix a surviving mutant, or name it in the … report with a reason"; and "a clean run is evidence about the mutants you thought of and nothing else — two commits in this repo each recorded 'three mutations checked, all caught' and each shipped a critical a later review found." State which sentences changed and why.
- [x] 4.5 Confirm no `${CLAUDE_PLUGIN_ROOT}/mutate.py` reference remains in any shipped skill: `grep -rn "mutate\.py" .claude/plugins/cla/skills/` returns nothing. (`README.md`'s layout line and `mutate.py` itself stay — `extract-dev-tree-from-plugin` moves them.)

## 5. Doc lines this change falsifies

- [x] 5.1 `skills/sync-context/SKILL.md:151` — re-point the staleness-guard reference from `${CLAUDE_PLUGIN_ROOT}/conformance-checks/tests/test_project_facts_paths.py` to `${CLAUDE_PLUGIN_ROOT}/skills/sync-context/scripts/check_fact_paths.py`. More than a path swap: the checker is now this skill's own script, so say so.
- [x] 5.2 `skills/_shared/references/skill-authoring.md:39` — replace `python3 -m pytest ${CLAUDE_PLUGIN_ROOT}/conformance-checks/tests/test_no_project_tokens.py -q` MUST pass with `python3 ${CLAUDE_PLUGIN_ROOT}/skills/_shared/scripts/check_no_project_tokens.py` MUST **exit 0**. This is a shipped file instructing a consumer to run a pytest scope that is about to vanish — the coupling this change exists to cut.
- [x] 5.3 `CLAUDE.md:259` — update the "Every script, and why it exists" row that names `` `codify-retro`, `spec-to-pr-retro` `scripts/aggregate.py` `` to the two new distinct names. **Scoped deliberately to lines this change falsifies: the layout/scope-count reconciliation is deferred to `extract-dev-tree-from-plugin`.** Specifically leave alone `CLAUDE.md:93` ("portable core that reaches consuming repos"), `CLAUDE.md:96` ("several scopes ship same-named helper modules" — weakened by task 3 but describing a 12-scope tree that still exists until the successor lands), the layout tree at line 234, `DEVELOPER-GUIDE.md`, the plugin `README.md`, root `.gitattributes`, and `TODO.md`.
- [x] 5.4 Consider whether the two promoted checkers now warrant rows in `CLAUDE.md`'s script table (they are scripts, and the table's rule is that each earns its place in one line). If added, keep to one line each; if deferred, say so and leave it to `extract-dev-tree-from-plugin`.

## 6. Verify and hand off

- [x] 6.1 Run `python3 .claude/plugins/cla/run_tests.py` once — the shipping gate. Every scope plus the Node suite.
- [x] 6.2 Confirm the scope inventory is unchanged: this change moves nothing, so `run_tests.py` must still discover the same 12 pytest scopes plus the Node suite, with no near-miss failures from the new `skills/sync-context/scripts/` directory (a dir with scripts but no `tests/` and no pytest-configured `pyproject.toml` is not a scope; confirm the discovery logic agrees).
- [x] 6.3 Re-read the two rewrite audits (1.6, 2.6) and the verbatim diff (4.4) as a block before opening the PR. These three are the change's real gate; the test suite cannot catch a dropped assertion that was rewritten away along with its test.
- [x] 6.4 Confirm the scope boundary held: `git status` shows **no file moved out of `.claude/plugins/cla/`**, `run_tests.py` and `mutate.py` both still exist, the `release` skill has not moved, and the three plugin-data conformance guards (`test_no_hardcoded_plugin_paths.py`, `test_skill_lint.py`, `test_guards_are_not_vacuous.py`) are still in `conformance-checks/tests/`.
- [x] 6.5 Note in the PR body that `extract-dev-tree-from-plugin` depends on this change landing first, and carry forward for `extract-dev-tree-from-plugin`: any finding from 3.10 (drift-check vacuity), 3.7's `scripts/log_run.py` docstring finding, the `_EXEMPT`/`TODO.md` grandfathered-guard count left stale by tasks 1.11 and 2.13, and design.md's open question about what restores an automatic trigger for the two promoted checkers.
