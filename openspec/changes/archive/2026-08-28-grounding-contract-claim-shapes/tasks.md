## 1. Re-derive the anchors, and settle the same-file ordering

- [x] 1.1 Re-measure the target file, because a line number recorded at proposal time is stale the moment anything else lands in it: `wc -l .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `316 .claude/plugins/cla/skills/review-change/references/checklist.md`. `design.md` recorded 316 on 2026-08-25 and it still reads 316, because no sibling touched this file — but re-run it rather than trusting that.
- [x] 1.2 Locate all six edit sites **by content, not by line number**: `grep -n '^### Grounding contract\|^### Context brief\|^0k\. \*\*Deferred-guess\|^### Applies when the change touches\|generic 0a–0e and 0j–0k\|^\*\*Cost offload for large changes\|^## Step 6\|^Deduplicate findings' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured (pre-edit):
  ```
  54:0k. **Deferred-guess-with-artifact-already-available check** — ...
  56:### Applies when the change touches allocation math, mock data, or i18n
  60:All checks above (generic 0a–0e and 0j–0k here, plus the overlay's 0f–0i and 1–9) are run by the orchestrator (you), not by agents. Record results in the **context brief** below.
  64:**Cost offload for large changes (default — thin-orchestrator discipline).** ...
  68:### Grounding contract — every claim resolves to evidence or NOT-FOUND
  72:### Context brief — standard format
  247:## Step 6: Aggregate and report
  249:Deduplicate findings. When multiple findings trace to one root cause, group them: "Root cause: X — fixing this resolves N of M findings."
  ```
  Matches `design.md`'s own 2026-08-25 measurement exactly.
- [x] 1.3 Confirm the sibling change `fix-brief-binding-defect` did NOT edit this file, and that nothing here waits on it: `git log --oneline -3 -- .claude/plugins/cla/skills/review-change/references/checklist.md` and `grep -n 'agent-reported\|orchestrator-verified' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured:
  ```
  3bccef7 fix: carry a chain obligation into the dependent's own review (#141)
  7265132 fix: say that fact-gatherer cannot run commands (#93)
  96bc7e0 fix: multi-pr argument binding, dangling slash-command lint, size-gate contradiction (#77)
  (grep: no matches, exit 1)
  ```
  Zero provenance-tag hits confirmed, pre-edit. The sibling shipped without touching this file (confirmed independently in task 5.5's dry run against its archived proposal/design). This is the **correct** state, not a dropped sibling edit; this change must not add the tag. No ordering constraint to settle.
- [x] 1.4 Read §"Grounding contract" (heading through the paragraph that ends before `### Context brief`) as it stands right now, not as `design.md` describes it — measured first sentence: `"Every row in the context brief below (and every finding derived from one) MUST resolve to one of exactly two things: **the verbatim evidence** ... OR an explicit **NOT-FOUND** (`not found: <what you searched, where>`)."` — last sentence: `"When a claim genuinely cannot be resolved either way in the budget available, say so (`unresolved: <why>`) rather than guessing a ✓ or ✗ — an honest gap is adjudicable; a fabricated verdict is not."` Shape 1 (task 2.3) is worded consistently with this clause: `unresolved` for not-having-searched, `NOT PRODUCIBLE` only once searched and nothing found.

## 2. The claim-shape list inside the Grounding contract

- [x] 2.1 Insert a `####` subsection — heading verbatim from `design.md` "Pinned implementation parameters": `#### Claim shapes — four sentences that are claims and do not look like claims` — immediately after §"Grounding contract"'s paragraph and before `### Context brief — standard format`. `####` not `###`, so the list sits inside the contract; that nesting is the change's central decision, not a formatting preference.
- [x] 2.2 Open the subsection with the one-sentence reason: these are recognition failures, not procedure failures — in each reported instance the reviewer held the grounding rule and the license to read source, and did not engage either, because the sentence presented as an explanation, a comparison, an output specification, or a trade.
- [x] 2.3 Write **Shape 1 — Producible state**, carrying `design.md` D2 verbatim: trigger, resolution steps (a)/(b)/(c), the failure-mode distinction, and the Critical floor with its reason — such a requirement does not fail loudly, it gets satisfied dishonestly. **State the distinction the way `design.md` now words it: searching and finding nothing is `NOT PRODUCIBLE`; not having searched is `unresolved`.** An earlier draft read "an unnameable producing path resolves NOT PRODUCIBLE, not `unresolved`", which reverses the Grounding contract's own closing clause three lines above the insertion point and, paired with the Critical floor, obliges a reviewer who merely ran out of budget to emit a Critical. Only `NOT PRODUCIBLE` carries the floor; an `unresolved` row carries none.
- [x] 2.4 Write **Shape 2 — Precedent strictness** from D2, including the sentence saying why no other check finds it: every other check asks whether the artifact is strong *enough*; this one asks whether it is stronger than it needs to be.
- [x] 2.5 Write **Shape 3 — Guarantee class** from D2, including that leaving the guarantee unclassified *is* the failure, and the two-level severity floor (deployment property with no named trigger → Important; deployment property described as a code property → Critical).
- [x] 2.6 Write **Shape 4 — Compensating coverage and exclusion reach** from D2, both triggers under one shape, including the asymmetry sentence: the given-up half is visible in the diff, the replacement is a promise.
- [x] 2.7 Close with the openness paragraph: the common signature (truth depends on something outside the artifact even where the grammar is not assertive) plus the four hiding grammars, ending with the statement that a matching sentence is in scope whether or not it is listed. Write it as a signature, not as a trailing disclaimer.
- [x] 2.8 Confirm every shape meets the executability bar — trigger, resolution steps, failure mode, severity floor, all four present. Count **occurrences, not lines**, and match the emphasis the shapes are actually written with: `grep -o 'Trigger' .claude/plugins/cla/skills/review-change/references/checklist.md | wc -l`, and the same for `Resolution`, `Failure mode`, `Severity floor` — measured: `Trigger: 15, Resolution: 11, Failure mode: 5, Severity floor: 4` (all counted post-2b.2, so the Design Reviewer and Spec & Codebase Reviewer prompts' embedded copies of the shapes contribute too). Each ≥ 4.

## 2b. Route the shapes to the agents who actually produce findings

Step 2's sweep is orchestrator-only — `checklist.md:60`: *"All checks above … are run by the
orchestrator (you), not by agents."* On the large path the findings come from the three Step-4
agents, and `checklist.md:144` tells them *"Do NOT re-read these files — use only the content
provided."* So a shape list that lives only in the Step-2 contract reaches the orchestrator and
never reaches the reviewers. Without this group the change is a rule nobody applying it can see.

- [x] 2b.1 Confirm the gap before fixing it, so the fix is aimed at something real: `grep -n 'run by the orchestrator (you), not by agents' .claude/plugins/cla/skills/review-change/references/checklist.md` and `grep -n 'Do NOT re-read these files' …` — measured (pre-edit, matches design.md's own line 60/144 measurement): line 60 carried "run by the orchestrator (you), not by agents"; line 144 carried "Do NOT re-read these files — use only the content provided." Both confirmed present before 2b.2's edit, establishing the gap the group closes.
- [x] 2b.2 Add the shape list to the **Design Reviewer** and **Spec & Codebase Reviewer** prompts in Step 4 — those two adjudicate artifact claims; the Task Reviewer scope is task feasibility and does not need it. **Paste the shape text into each prompt in full — names, triggers AND resolution steps. Do NOT point at the contract subsection.** Done: both prompts gained a new "6. **Claim shapes**" check item carrying all four shapes' names, triggers, resolution steps, failure signatures and severity floors verbatim (condensed prose, full content) — not a pointer to §"Grounding contract".
- [x] 2b.3 The agents are told not to re-read change artifacts but MAY read source (`checklist.md:144`). Confirm the shape resolutions only ever require reading **source**, never re-reading the change artifacts — measured: **Shape 1** reads the production function/query's source; **Shape 2** reads the named precedent's actual mechanism at its source path; **Shape 3** reads/quotes the enforcing source line (classification is applied to the artifact text already provided in-context, not re-read from disk); **Shape 4** reads the named replacement file/mechanism and enumerates reachable components in source (e.g. router config). All four resolve against source or the artifact text already pasted into the prompt — none requires re-reading a change artifact from disk, so all four are delegable to Step 4.
- [x] 2b.4 Verify the routing landed: `grep -c 'Producible state' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `3` (the contract subsection, plus the Design Reviewer prompt, plus the Spec & Codebase Reviewer prompt). ≥ 2 confirmed — the shapes reached the agents who produce findings.

## 3. The one numbered check, and its non-delegability

- [x] 3.1 Insert `0l. **Claim-shape sweep**` immediately after `0k` and before `### Applies when the change touches allocation math, mock data, or i18n`. Its content points at the contract's shape list; it does **not** restate the shapes. Model its brevity on `0f–0i`, the existing one-line entry that delegates four checks elsewhere.
- [x] 3.2 In `0l`, state that unlike `0a–0h` it is not delegable to `fact-gatherer`, because each shape returns a judgement — a comparison of two mechanisms, a classification, an assessment of one test's strength against another's — rather than a pass/fail row.
- [x] 3.3 **Append** one sentence to the cost-offload paragraph naming `0l` as outside the delegable set. Append only — do not rewrite the sentence beginning "You still adjudicate every ✗ row yourself". Done: appended `` `0l`'s claim-shape sweep sits outside this delegable set — each shape returns a judgement, not a pass/fail row a haiku dispatch could produce. `` at the end of the paragraph. Measured after the edit: `grep -c 'adjudicate every' checklist.md` → `1` (the pre-existing sentence, unchanged, still present); `grep -n '0l' checklist.md` → 6 hits (lines 36, 56, 62, 66, 339, plus the shape-name occurrences already counted in 2.8) confirming `0l` is now named throughout.
- [x] 3.4 Update **every** enumeration of the orchestrator's checks so `0l` is named — the delta spec requires all of them, and a stale enumeration is how a check goes quietly unrun. Found (pre-edit) three sites: line 36 `Most verification checks (0a–0k and 1–9 below)`, line 60 `generic 0a–0e and 0j–0k here`, line 302 (INT-SYC) `checks 1 and 0a–0k exist precisely for this`. All three updated to `0a–0l` / `0j–0l`. Confirmed none survives: `grep -c '0j–0k\|0a–0k' checklist.md` — measured: `0`.
- [x] 3.5 Confirm no existing check was renumbered or reworded: `grep -n '^0[a-z]\. \|^0f–0i\.' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `0a`, `0b`, `0c`, `0d`, `0e`, `0f–0i`, `0j`, `0k` all present with identical text to task 1.2's baseline read, plus exactly one new entry, `0l. **Claim-shape sweep**`.

## 4. The severity tie-break at Step 6

- [x] 4.1 After Step 6's existing "Deduplicate findings" sentence, add one paragraph: when two dispatched reports carry the same finding at different severities, **keep the higher severity**, and record on the finding line which report's evidence was implementation-level (a source line, a schema, a migration, a query result) and which rested on the spec delta or artifact text alone. **The evidence class is recorded, never used to lower a severity.**
- [x] 4.2 In the same paragraph, state **why the evidence class is recorded at all**: all three Step-4 agents are told "You MAY read source files to verify claims", so agent role does not identify who read the implementation; the evidence does, and it is present only because the Grounding contract requires every finding to carry it. Recording it lets a reader see who had ground truth without letting that silently change the severity.
- [x] 4.3 State the three reasons the paragraph does NOT demote, since an earlier draft did and each reason is independently sufficient: (a) `spec-to-pr`'s Revise applies **SEV-MAX** to the identical situation — "triaged at the HIGHER severity, always", called "the aggregation analogue of the never-demote-a-real-finding principle" — so demoting here would put two opposite merge rules in one pipeline with nothing explaining the difference; (b) READY requires 0 Critical and 0 Important, so demoting one Important to Suggestion clears the verdict gate with the finding unresolved, which INT-CAP forbids by name; (c) `review-change` runs pre-implementation, where artifact-text evidence is the *correct* evidence class for its core output, so ranking source-line evidence above it demotes exactly what this review exists to produce. Name SEV-MAX and its file, so a reader can see the two rules agree rather than having to trust it.
- [x] 4.4 Require the tie-break to be recorded on the finding it resolved, and state that it adds no report line — §"Report constraints" budgets one line per finding and about one screen total.
- [x] 4.5 Write the evidence sentence honestly and in the file, not only in `design.md`: one overlapping finding out of 18, one change, one chain, one consuming repo; the observed instance was a two-reviewer split under a different workflow, not this checklist's three-agent dispatch; low overlap is the split working as designed, so the rule is expected to fire rarely. Do not write "measured" anywhere in it. Do **not** cite the verdict rubric's "do not shortcut this to 'any Critical → RETHINK'" line as support — that warning governs the FIX-FIRST/RETHINK boundary, which the rubric twice says is not gated on severity label or count; the axis severity inflation actually threatens is READY-vs-FIX-FIRST.
- [x] 4.6 Confirm the report budget did not move: `grep -n 'One line per finding\|~40 lines max' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: line 350 `- One line per finding.`, line 352 `- Total report should fit on one screen (~40 lines max).` — text unchanged from before this change's edits (only line numbers shifted, from the earlier insertions above Step 6).

## 5. Verify

- [x] 5.1 Confirm the added prose is portable synced core — no consuming-repo mechanism, product name, or infrastructure identifier from the five source issues, and no dev-tree or absolute developer path: `python3 .claude/plugins/cla/skills/_shared/scripts/check_no_project_tokens.py` — measured: `check_no_project_tokens: 61 prose file(s) and 31 source file(s) scanned under cla, 5 token(s) loaded, 0 violation(s); source roots reached: agents, hooks, lib, output-styles, skills` — exit status `0`.
- [x] 5.2 Read the four shapes back and confirm by hand that none of them names the reporting incident's own mechanism rather than the shape — the four to be absent by name are the concurrency-control flag, the accessibility scanner, the registry code column, and the route-exclusion constant. Confirmed by hand-read (all three copies — the contract subsection and both agent-prompt embeddings): none of the four shapes, in any of the three copies, names `batchSize`, an accessibility/axe scanner, a registry code column, or a route-allowlist/exclusion constant. Each shape is stated in terms of the generalised property (producibility, precedent strictness, code-vs-deployment guarantee, compensating coverage) with no repo- or incident-specific mechanism name.
- [x] 5.3 Run the area suites that could plausibly fail on this change: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `281 passed, 1 skipped in 26.91s`. **There is no `plugin-tests/tests/skills/review-change/` directory** (confirmed: no such path exists under `plugin-tests/tests/skills/`), so a run of `plugin-tests/tests/skills` would be green independently of anything here and is not evidence for this change — no test in the suite reads the claim-shape prose, the `0l` text or the tie-break paragraph for correctness. The checks that actually gate this change's substance are the greps in 2.8/3.4/3.5/4.6/5.1 and the hand-reads in 5.2/5.5/5.6.
- [x] 5.4 Run the conformance and consistency areas, which police synced-core portability and SKILL.md reference integrity: `pytest plugin-tests/tests/conformance plugin-tests/tests/consistency` — measured: `281 passed, 1 skipped in 26.91s` (same command and result as 5.3; both tasks specify it).
- [x] 5.5 Dry-run the shape list against a real change: take the most recent OpenSpec change in this repo, walk `0l` over its `proposal.md` and `design.md`, and record which shapes fired and what each resolved to. Most recent by commit date touching `openspec/` (`git log --oneline -1 -- openspec/` → `a55f4d9 feat: fix-brief-binding-defect (#155)`, 2026-08-25 22:46:15, later than this change's own proposal batch at 01:33:02) is the archived `openspec/changes/archive/2026-08-25-fix-brief-binding-defect/`. Its `design.md` is dense with explanations and precedent citations (see its own "Rejected alternative" sections). Walked all four shapes over its `proposal.md` + `design.md`:
  - **Producible state:** 0 hits — no demo/fixture/sample-state claims in this change.
  - **Precedent strictness:** 1 hit — `design.md:188-189`: "the delegate's first action on a fix brief is to re-resolve every `Facts this defect rests on` row against its named source, resolving each to verbatim evidence or an explicit NOT-FOUND — the same grounding contract the review checklist already binds reviewers to (`checklist.md` line 68)." Resolved: read `checklist.md`'s Grounding contract — it requires exactly "verbatim evidence or explicit NOT-FOUND." The new rule matches its cited precedent exactly (not stricter) — clean pass, no finding, but the shape fired and resolved correctly.
  - **Guarantee class:** 0 hits — no prevents/controls/serialises/hazard language.
  - **Compensating coverage and exclusion reach:** 0 hits — no coverage trade-off or allowlist/denylist exclusion in this change.
  Not all four fired zero times (Shape 2 fired and resolved), so the triggers are not shown to be too narrow — task passes.
- [x] 5.6 Dry-run the tie-break: take two dispatched review reports from this repo's run notes, find any finding both carry, and apply the rule. Searched `cla.io/retro/*.md` run-notes files and `cla.io/retro/spec-to-pr-runs.jsonl`: the ledger records only aggregate per-phase counts (`verified_claims_count`, `verdict`, agent list), not individual per-agent finding text, and the run-notes `.md` files (multi-lite/multi-pr) carry no per-agent Design/Task/Spec finding breakdowns — the actual n=1 supporting instance behind this rule (design.md D4) is documented as occurring in a different, consuming repo, not in this repo's own records. **No such finding exists in this repo's run notes.** Fallback branch: per Decision 4's own "Alternative rejected" note, "the higher severity always wins" is retained as the fallback when evidence does not discriminate — that is the branch that would apply.
- [x] 5.7 Confirm this change did NOT introduce a provenance tag into the review report's claim table, and that the pre-existing adjudication sentence survived: `grep -c 'agent-reported' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: `0`. `grep -n 'adjudicate every' .claude/plugins/cla/skills/review-change/references/checklist.md` — measured: line 66, `**You still adjudicate every ✗ row yourself** — the agent gathers facts, it does not decide whether a failed claim matters.` (unchanged, still present). First count is `0` as required.
- [x] 5.8 Before the PR: `pytest plugin-tests` and `node --test plugin-tests/node/mechanical-checks.test.mjs`, each once — measured: `1307 passed, 8 skipped in 266.35s (0:04:26)` and `tests 70 / pass 70 / fail 0 / cancelled 0 / skipped 0`.
